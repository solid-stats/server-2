---
phase: 02
plan: 02-01
title: Migration framework and lifecycle/status enum conventions
status: planned
wave: 1
depends_on: [01-03]
autonomous: true
requirements_addressed: [DATA-01]
truths:
  - "Use explicit PostgreSQL migrations managed from TypeScript project scripts."
  - "Prefer readable SQL-shaped schema changes."
files_modified: [package.json, src/infra/db/migrate.ts, src/infra/db/migrations/0001_v1_domain_schema.sql, README.md]
---

# Plan 02-01: Migration Framework

<objective>Add an idempotent SQL migration runner and lifecycle enums.</objective>

<threat_model>
- Migration tampering: store migration checksums in `schema_migrations`.
- Accidental drift: fail when an applied migration checksum changes.
</threat_model>

<tasks>
<task id="02-01-01"><read_first>package.json, src/config/env.ts, src/infra/db/client.ts</read_first><action>Create `src/infra/db/migrate.ts`, `src/infra/db/migrations/0001_v1_domain_schema.sql`, `db:migrate` script, and migration ledger.</action><acceptance_criteria>`npm run db:migrate` exits 0 twice; SQL contains `schema_migrations` and lifecycle enums.</acceptance_criteria></task>
</tasks>
