---
phase: 16-slug-resolution-history-provenance
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/modules/public-stats/routes/schemas.ts
  - src/modules/public-stats/routes/models.ts
  - src/modules/public-stats/routes/empty-read-model.ts
autonomous: true
requirements: [API-01, HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "Detail routes can bind a bounded slug-or-uuid param (maxLength 128, pattern) instead of format:uuid"
    - "Player/squad/rotation summary+profile response schemas carry a slug field"
    - "Singular responses (profile, rotation detail, parity sub-resources, history) carry provenance.lastUpdatedAt"
    - "History responses are a discriminated union of alias/membership and unknown-gap entries with counterparts carrying only {id,slug,name|displayName}"
    - "Every new read-model method has a stub in empty-read-model.ts so boot-without-DB compiles"
  artifacts:
    - path: "src/modules/public-stats/routes/schemas.ts"
      provides: "SlugOrUuidParameters, ProvenanceResponse, slug fields, history union schemas, rotation detail schema"
      contains: "SlugOrUuidParameters"
    - path: "src/modules/public-stats/routes/models.ts"
      provides: "extended PublicStatsReadModel interface + domain payload types with slug/provenance"
      contains: "getRotation"
    - path: "src/modules/public-stats/routes/empty-read-model.ts"
      provides: "boot-without-DB stubs for getRotation + 3 history methods"
      contains: "getRotation"
  key_links:
    - from: "src/modules/public-stats/routes/models.ts"
      to: "src/modules/public-stats/routes/empty-read-model.ts"
      via: "every interface method has a matching stub"
      pattern: "getRotation|getPlayerNameHistory|getPlayerMembershipHistory|getSquadMembershipHistory"
---

<objective>
Объявить ВСЕ контракты фазы (TypeBox схемы + TS domain-типы + расширение read-model интерфейса +
boot-without-DB стабы), чтобы Plan 04 (repository) и Plan 05 (routes) строились против них без
исследования кодовой базы. Interface-first: этот план — единственный, кто трогает schemas.ts /
models.ts / empty-read-model.ts.

Purpose: дать executor'ам Plan 04/05 готовые контракты (param-схема, slug-поля, provenance-конверт,
discriminated-union history-схемы, новые сигнатуры read-model + стабы). Все изменения additive
(backward-compatible для генерируемого клиента web; freeze — Phase 19).

Output: 3 модифицированных файла.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md
@.planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md
@.agents/skills/solidstats-backend-ts-conventions/SKILL.md
@src/modules/public-stats/routes/schemas.ts
@src/modules/public-stats/routes/models.ts
@src/modules/public-stats/routes/empty-read-model.ts
@src/modules/public-stats/routes/history-gaps.ts

<interfaces>
<!-- schemas.ts: one chained `const ... =` block with /* eslint-disable new-cap */ + Static<> exports. -->
<!-- Existing: UuidParameters (line 20), RotationSummaryResponse (52), PlayerSummaryResponse (123), -->
<!--           SquadSummaryResponse (152), PlayerProfileResponse (129), SquadProfileResponse (158), -->
<!--           PlayerReferenceResponse (198), Type.Union/Type.Literal usage (13-17), paginated() (259). -->
<!-- Existing parity sub-resource response schemas (Phase 15): PlayerWeaponsResponse (62), -->
<!--   PlayerVehiclesResponse (66), PlayerRelationshipsResponse (85), PlayerWeeklyResponse (108), -->
<!--   SquadWeaponsResponse (174), SquadRelationshipsResponse (187), SquadWeeklyResponse (193). -->

<!-- models.ts PublicStatsReadModel interface (lines 1-33); PlayerSummary (170), SquadSummary (230), -->
<!--   RotationSummary (96), PlayerProfile (177), SquadProfile (237), PublicPlayerReference (244). -->

<!-- empty-read-model.ts createEmptyPublicStatsReadModel() (lines 17-50): each get* -> Promise.resolve(null). -->

<!-- Bounded slug-or-uuid param (16-RESEARCH Pattern 2; V5 input-validation): -->
<!--   id: Type.String({ minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9-]+$" }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas.ts — SlugOrUuidParameters, slug fields, ProvenanceResponse, rotation detail, history union schemas</name>
  <files>src/modules/public-stats/routes/schemas.ts</files>
  <read_first>
    - src/modules/public-stats/routes/schemas.ts (весь файл — self-extension; chained const block + Static exports)
    - .planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md (Pattern 2 SlugOrUuidParameters lines 236-242; Code Example discriminated-union lines 423-443)
    - .planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md (schemas.ts section lines 189-234)
  </read_first>
  <action>
    В цепном `const`-блоке schemas.ts добавить (additive, не трогая существующие):
    - `SlugOrUuidParameters = Type.Object({ id: Type.String({ minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9-]+$" }) })`.
    - `slug: Type.String()` в `RotationSummaryResponse`, `PlayerSummaryResponse`, `SquadSummaryResponse`
      (профили наследуют через Type.Intersect — не дублировать).
    - `ProvenanceResponse = Type.Object({ lastUpdatedAt: Type.Union([Type.String({ format: "date-time" }), Type.Null()]) })`.
    - Добавить `provenance: ProvenanceResponse` в singular-ответы: `PlayerProfileResponse`,
      `SquadProfileResponse`, новый `RotationDetailResponse`, и в Phase-15 parity-ответы
      `PlayerWeaponsResponse`, `PlayerVehiclesResponse`, `PlayerRelationshipsResponse`,
      `PlayerWeeklyResponse`, `SquadWeaponsResponse`, `SquadRelationshipsResponse`, `SquadWeeklyResponse`.
      (Для parity-ответов provenance добавляется как ещё одно поле объекта — additive.)
    - `RotationDetailResponse = Type.Intersect([RotationSummaryResponse, Type.Object({ provenance: ProvenanceResponse })])`.
    - History counterpart refs: `SquadRefResponse = Type.Object({ id: Type.String(), slug: Type.String(), name: Type.String() })`,
      `PlayerRefSlugResponse = Type.Object({ id: Type.String(), slug: Type.String(), displayName: Type.String() })`.
      (НИКАКИХ Steam64; только id/slug/name|displayName.)
    - `NameHistoryEntry = Type.Union([ {kind:"alias", nickname, from(date-time|null), to(date-time|null), sourceReplayId(uuid|null)}, {kind:"unknown-gap", from, to} ])`.
    - `PlayerMembershipHistoryEntry = Type.Union([ {kind:"membership", squad:SquadRefResponse, from, to}, {kind:"unknown-gap", from, to} ])`.
    - `SquadMembershipHistoryEntry = Type.Union([ {kind:"membership", player:PlayerRefSlugResponse, from, to}, {kind:"unknown-gap", from, to} ])`.
    - `NameHistoryResponse = Type.Object({ entries: Type.Array(NameHistoryEntry), provenance: ProvenanceResponse })`;
      аналогично `PlayerMembershipHistoryResponse`, `SquadMembershipHistoryResponse`.
    - Добавить соответствующие `export type ... = Static<typeof ...>` для каждой новой схемы, используемой в routes
      (SlugOrUuidParametersType, RotationDetailResponseType, NameHistoryResponseType, PlayerMembershipHistoryResponseType, SquadMembershipHistoryResponseType).
    Все date-time поля nullable через `Type.Union([Type.String({ format: "date-time" }), Type.Null()])` (образец RotationSummaryResponse.endsAt).
  </action>
  <verify>
    <automated>pnpm run typecheck &amp;&amp; pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SlugOrUuidParameters" src/modules/public-stats/routes/schemas.ts` находит определение с `maxLength: 128` и `pattern`.
    - `grep -n "ProvenanceResponse" schemas.ts` показывает определение + использование в каждом singular/parity/history ответе.
    - `grep -c "Type.Literal(\"unknown-gap\")" schemas.ts` >= 3 (по одному на каждую history-схему).
    - НИ В одном новом counterpart-объекте нет поля steamId/steamIds (grep пусто по новым refs).
    - `export type SlugOrUuidParametersType`, `RotationDetailResponseType`, `NameHistoryResponseType`, `PlayerMembershipHistoryResponseType`, `SquadMembershipHistoryResponseType` присутствуют.
    - `pnpm run typecheck` и `pnpm run lint` зелёные.
  </acceptance_criteria>
  <done>Все TypeBox-схемы фазы объявлены additive: bounded param, slug-поля, provenance-конверт, rotation detail, discriminated-union history без Steam64.</done>
</task>

<task type="auto">
  <name>Task 2: models.ts domain types + read-model interface; empty-read-model.ts stubs</name>
  <files>src/modules/public-stats/routes/models.ts, src/modules/public-stats/routes/empty-read-model.ts</files>
  <read_first>
    - src/modules/public-stats/routes/models.ts (весь файл — self-extension; interface lines 1-33, payload types)
    - src/modules/public-stats/routes/empty-read-model.ts (весь файл — каждый get* -> Promise.resolve(null))
    - src/modules/public-stats/routes/history-gaps.ts (тип unknown-gap entry — зеркалить в TS union)
    - .planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md (models.ts lines 238-253; empty-read-model lines 256-266; Pitfall 6)
  </read_first>
  <action>
    models.ts (additive):
    - Добавить `slug: string` в `PlayerSummary`, `SquadSummary`, `RotationSummary`.
    - Добавить `provenance: { lastUpdatedAt: string | null }` в `PlayerProfile`, `SquadProfile`,
      и в Phase-15 payload-типы: `PlayerWeaponsPayload`, `PlayerVehiclesPayload`,
      `PlayerRelationshipsPayload`, `PlayerWeeklyPayload`, `SquadWeaponsPayload`,
      `SquadRelationshipsPayload`, `SquadWeeklyPayload`.
    - Новый `RotationDetail extends RotationSummary { provenance: { lastUpdatedAt: string | null } }`.
    - Discriminated-union TS-типы зеркалом TypeBox: `NameHistoryEntry`, `PlayerMembershipHistoryEntry`,
      `SquadMembershipHistoryEntry` (counterparts `{id,slug,name}` / `{id,slug,displayName}` — без Steam64);
      payload-обёртки `NameHistoryPayload`, `PlayerMembershipHistoryPayload`, `SquadMembershipHistoryPayload`
      каждая `{ entries: Entry[]; provenance: { lastUpdatedAt: string | null } }`.
    - Расширить `PublicStatsReadModel` интерфейс новыми сигнатурами:
      `getRotation(id: string): Promise<RotationDetail | null>;`
      `getPlayerNameHistory(id: string): Promise<NameHistoryPayload | null>;`
      `getPlayerMembershipHistory(id: string): Promise<PlayerMembershipHistoryPayload | null>;`
      `getSquadMembershipHistory(id: string): Promise<SquadMembershipHistoryPayload | null>;`
    empty-read-model.ts:
    - Добавить стаб для КАЖДОГО нового метода: `getRotation: () => Promise.resolve(null)`,
      `getPlayerNameHistory: () => Promise.resolve(null)`,
      `getPlayerMembershipHistory: () => Promise.resolve(null)`,
      `getSquadMembershipHistory: () => Promise.resolve(null)` (Pitfall 6 — иначе interface не satisfied).
  </action>
  <verify>
    <automated>pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "getRotation|getPlayerNameHistory|getPlayerMembershipHistory|getSquadMembershipHistory" src/modules/public-stats/routes/models.ts` находит 4 сигнатуры в интерфейсе.
    - Те же 4 имени присутствуют в empty-read-model.ts как `() => Promise.resolve(null)`.
    - `grep -n "slug: string" models.ts` показывает добавление в PlayerSummary/SquadSummary/RotationSummary.
    - `grep -n "provenance" models.ts` показывает добавление в profile + parity payload типы + RotationDetail + history payloads.
    - `pnpm run typecheck` зелёный (createEmptyPublicStatsReadModel удовлетворяет расширенному интерфейсу).
  </acceptance_criteria>
  <done>models.ts расширен slug/provenance/history-типами и 4 новыми сигнатурами; empty-read-model.ts имеет стаб на каждый новый метод (boot-without-DB компилируется).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API param schema | `:id` path segment (untrusted) bounded by SlugOrUuidParameters (V5 input validation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Denial of Service | unbounded `:id` path segment | mitigate | `SlugOrUuidParameters` maxLength:128 + pattern `^[A-Za-z0-9-]+$`. |
| T-16-07 | Information Disclosure | Steam64 leaking via new history counterpart shapes | mitigate | Counterpart schemas/types carry only {id,slug,name|displayName}; no steamId field declared; grep gate in acceptance. |
| T-16-08 | Tampering | boot-without-DB breaking (interface drift) | mitigate | empty-read-model stub added for every new method in the same task as the interface change. |
| T-16-SC | Tampering | npm package installs | accept | No package installs in this plan (schema/type definitions only). |
</threat_model>

<verification>
- `pnpm run typecheck` зелёный (включая empty-read-model удовлетворяет интерфейсу).
- `pnpm run lint` зелёный.
- 4 новые read-model сигнатуры присутствуют и в интерфейсе, и в стабе.
- Ни один новый counterpart-тип/схема не содержит steamId.
</verification>

<success_criteria>
Все контракты фазы (bounded param, slug-поля, provenance-конверт, discriminated-union history-схемы
без Steam64, 4 новые read-model сигнатуры + стабы) объявлены additive в одном месте — Plan 04 и
Plan 05 строятся против них без исследования кодовой базы.
</success_criteria>

<output>
Create `.planning/phases/16-slug-resolution-history-provenance/16-03-SUMMARY.md` when done
</output>
