---
phase: 16-slug-resolution-history-provenance
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-03]
files_modified:
  - src/modules/public-stats/repository.ts
autonomous: true
requirements: [API-01, HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "getPlayer/getSquad/getRotation resolve by UUID and by slug; a non-existent slug returns null (404), never a ::uuid cast error (500)"
    - "Player/squad/rotation mappers emit the slug field and a provenance.lastUpdatedAt computed from returned-row timestamps, never now()"
    - "getPlayerNameHistory returns ascending alias windows with explicit unknown-gap entries"
    - "getPlayer/getSquadMembershipHistory return membership windows with counterparts carrying only {id,slug,name|displayName}, no Steam64"
    - "Phase-15 parity sub-resource payloads carry provenance from their returned rows"
  artifacts:
    - path: "src/modules/public-stats/repository.ts"
      provides: "resolveEntityId branch, slug+timestamp row fields, slug+provenance mappers, 3 history read methods, getRotation"
      contains: "looksLikeUuid"
  key_links:
    - from: "src/modules/public-stats/repository.ts getPlayer/getSquad/getRotation"
      to: "WHERE branch on boolean flag"
      via: "($1::boolean = true and id = $2::uuid) or ($1::boolean = false and slug = $2::text)"
      pattern: "::boolean = true and .*\\.id = \\$.*::uuid"
    - from: "src/modules/public-stats/repository.ts history methods"
      to: "withGaps + maxTimestamp helpers"
      via: "import from history-gaps.js and provenance.js"
      pattern: "withGaps|maxTimestamp"
---

<objective>
Расширить `PgPublicStatsReadModel`: (1) slug-or-uuid резолвер с boolean-flag SQL-branch (Pitfall 2 —
`::uuid` cast только в UUID-ветке → 404 не 500); (2) добавить `slug` + provenance-timestamp поля в
row-интерфейсы и мапперы; (3) provenance в Phase-15 parity-маппинг; (4) три новых history read-метода
(name/player-membership/squad-membership) с темпоральным SQL и `withGaps`; (5) `getRotation` detail.

Purpose: вся серверная логика API-01/HIST-01/HIST-02/HIST-03 в одном файле, который трогает только
этот план. Все assertions против БД — интеграционные (Plan 06), т.к. worktree-executor без живой БД;
здесь — корректность типов/SQL-формы/чистых мапперов.

Output: 1 модифицированный файл (repository.ts).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md
@.planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md
@.agents/skills/solidstats-backend-ts-conventions/SKILL.md
@src/modules/public-stats/repository.ts
@src/modules/public-stats/routes/slug.ts
@src/modules/public-stats/routes/history-gaps.ts
@src/modules/public-stats/routes/provenance.ts
@src/modules/public-stats/routes/models.ts
@src/modules/public-stats/routes/pagination/mask.ts
@src/modules/statistics/repository/parity-sql.ts

<interfaces>
<!-- Existing repository shapes: -->
<!--   PgPublicStatsReadModel class (line 186), getPlayer (298), getSquad (354), listSquadPlayers (724). -->
<!--   Row interfaces: RotationRow (78), PlayerRow (85), SquadRow (97). -->
<!--   Mappers: mapRotation (1013), mapPlayerSummary (1022), mapPlayerProfile (1034), mapSquadSummary (1064). -->
<!--   getPlayer current WHERE: `where players.id = $1` (line 312) — relax to branched form. -->

<!-- Boolean-flag branch (16-RESEARCH Pattern 2, Pitfall 2): -->
<!--   where ($1::boolean = true and players.id = $2::uuid) -->
<!--      or ($1::boolean = false and players.slug = $2::text) -->
<!--   binding [looksLikeUuid(idOrSlug), idOrSlug]. -->

<!-- Temporal source tables (0001): -->
<!--   player_nicknames(nickname, observed_from, observed_to, source_replay_id, player_id) -->
<!--   squad_memberships(squad_id, player_id, valid_from, valid_to) -->
<!-- Counterpart joins: squads(id,slug,name), canonical_players(id,slug,display_name) — NO steam. -->

<!-- rotations has NO updated_at (Open Q2). Rotation provenance = max(player_stats.calculated_at where rotation_id=r.id), floor r.created_at, else null. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: slug-or-uuid resolver + slug/provenance on row interfaces & detail mappers (getPlayer/getSquad/getRotation)</name>
  <files>src/modules/public-stats/repository.ts</files>
  <read_first>
    - src/modules/public-stats/repository.ts (row interfaces 78-106; getPlayer 298-319; getSquad 354-377; listRotations + mapRotation 1013-1020; mapPlayerSummary 1022; mapPlayerProfile 1034; mapSquadSummary 1064)
    - src/modules/public-stats/routes/slug.ts (looksLikeUuid)
    - src/modules/public-stats/routes/provenance.ts (maxTimestamp)
    - src/modules/public-stats/routes/models.ts (RotationDetail + extended summary/profile types from Plan 03)
    - .planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md (Pattern 2 lines 232-260; Pattern 5 lines 318-336; Open Q2 lines 336)
    - .planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md (repository section lines 123-186)
  </read_first>
  <behavior>
    - getPlayer(<uuid>) resolves the row by id; getPlayer(<slug>) resolves the same row by slug; getPlayer(<unknown-slug>) -> null (no thrown ::uuid error)
    - mapPlayerSummary output includes `slug` from PlayerRow.slug
    - mapPlayerProfile.provenance.lastUpdatedAt = max(player_stats.calculated_at returned, canonical_players.updated_at floor); null when no timestamps
    - getSquad same slug-or-uuid + slug field + provenance from squad_stats.calculated_at / squads.updated_at
    - getRotation(<uuid|slug>) returns RotationDetail with slug + provenance from max(player_stats.calculated_at for rotation), floor created_at, else null; unknown -> null
    - mask choke point in mapPlayerProfile (steam_ids.map(maskSteamId)) is UNCHANGED
  </behavior>
  <action>
    1. Импортировать `looksLikeUuid` из `./routes/slug.js`, `maxTimestamp` из `./routes/provenance.js`.
    2. Добавить `slug: string` в `PlayerRow`, `SquadRow`, `RotationRow`; добавить provenance-timestamp
       поля как `Date | null` (например `calculated_at: Date | null`, `updated_at: Date | null` для player/squad;
       `last_calc: Date | null`, `created_at: Date` для rotation).
    3. Релаксировать `getPlayer`/`getSquad` SQL: заменить `where <t>.id = $1` на boolean-flag branch
       `where ($N::boolean = true and <t>.id = $M::uuid) or ($N::boolean = false and <t>.slug = $M::text)`,
       привязка `[..., looksLikeUuid(id), id]`; добавить `players.slug` / `squads.slug` и
       соответствующие timestamp-колонки в select + group by. (rotation $-плейсхолдеры пересчитать аккуратно.)
    4. Расширить `mapPlayerSummary`/`mapSquadSummary`/`mapRotation` полем `slug: row.slug`.
    5. Расширить `mapPlayerProfile`/`mapSquadProfile`(если есть отдельный) полем
       `provenance: { lastUpdatedAt: maxTimestamp([row.calculated_at, row.updated_at]) }`. mask-choke-point не трогать.
    6. Новый `public async getRotation(id: string): Promise<RotationDetail | null>` —
       select rotation row (slug-or-uuid branch) + `(select max(calculated_at) from player_stats ps where ps.rotation_id = r.id) as last_calc`;
       мап в RotationDetail с `provenance: { lastUpdatedAt: maxTimestamp([row.last_calc, row.created_at]) }`.
    Никакой конкатенации slug/uuid в SQL; параметризовать всё.
  </action>
  <verify>
    <automated>pnpm run typecheck &amp;&amp; pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "::boolean = true and .*\\.id = \\$.*::uuid" src/modules/public-stats/repository.ts` находит branch в getPlayer, getSquad и getRotation (>=3 совпадения).
    - `grep -n "looksLikeUuid" repository.ts` показывает использование в bindings всех трёх резолверов.
    - `grep -c "maxTimestamp" repository.ts` >= 3 (player profile, squad profile, rotation detail).
    - Существующий `steam_ids.map((steamId) => maskSteamId(steamId))` в mapPlayerProfile НЕ изменён (grep подтверждает).
    - `slug: row.slug` присутствует в mapPlayerSummary, mapSquadSummary, mapRotation.
    - `pnpm run typecheck` и `pnpm run lint` зелёные.
  </acceptance_criteria>
  <done>Резолвер slug-or-uuid через boolean-flag branch (404 не 500), slug+provenance в detail-мапперах, getRotation реализован; mask choke point не тронут.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: history read methods (name/player-membership/squad-membership) + provenance on parity sub-resources</name>
  <files>src/modules/public-stats/repository.ts</files>
  <read_first>
    - src/modules/public-stats/repository.ts (listSquadPlayers 724-739 — форма; parity sub-resource methods getPlayerWeapons 463, getPlayerWeekly 552, getSquadWeapons 590, getSquadRelationships 625, getSquadWeekly 670)
    - src/modules/statistics/repository/parity-sql.ts (temporal windowing patterns)
    - src/modules/public-stats/routes/history-gaps.ts (withGaps)
    - src/modules/public-stats/routes/provenance.ts (maxTimestamp)
    - src/modules/public-stats/routes/models.ts (NameHistoryPayload, PlayerMembershipHistoryPayload, SquadMembershipHistoryPayload from Plan 03)
    - .planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md (Pattern 3 SQL lines 262-288; Pattern 4 lines 290-316)
  </read_first>
  <behavior>
    - getPlayerNameHistory(<player uuid>): rows from player_nicknames ordered `observed_from asc nulls first, id`; entries via withGaps with makeKnown -> {kind:"alias", nickname, from:observed_from, to:observed_to, sourceReplayId}; provenance = max(observed_to ?? observed_from); empty -> {entries:[], provenance:{lastUpdatedAt:null}}
    - getPlayerMembershipHistory(<player uuid>): squad_memberships join squads; entries {kind:"membership", squad:{id,slug,name}, from:valid_from, to:valid_to}; no Steam64
    - getSquadMembershipHistory(<squad uuid>): squad_memberships join canonical_players; entries {kind:"membership", player:{id,slug,displayName}, from,to}; no Steam64
    - all three: withGaps inserts explicit unknown-gap entries per locked edge-policy
    - parity sub-resources (Phase 15) now return provenance from their returned rows (e.g. max calculated_at / observed timestamps available)
  </behavior>
  <action>
    1. Добавить три метода в PgPublicStatsReadModel (форма listSquadPlayers + parity SQL):
       - `getPlayerNameHistory(id)`: SQL `select n.nickname, n.observed_from, n.observed_to, n.source_replay_id from player_nicknames n where n.player_id = $1::uuid order by n.observed_from asc nulls first, n.id`.
         Сначала проверить существование игрока (resolve уже по id — caller передаёт разрешённый id из route, см. Plan 05; если player не найден -> null). Построить entries через `withGaps(rows, mapAliasWindow)`; provenance `{ lastUpdatedAt: maxTimestamp(rows.flatMap(r => [r.observed_to, r.observed_from])) }`.
       - `getPlayerMembershipHistory(id)`: SQL join squads `select m.valid_from, m.valid_to, s.id as squad_id, s.slug as squad_slug, s.name from squad_memberships m join squads s on s.id = m.squad_id where m.player_id = $1::uuid order by m.valid_from asc nulls first, m.id`. entries через withGaps, counterpart `{id,slug,name}`.
       - `getSquadMembershipHistory(id)`: SQL join canonical_players, counterpart `{id,slug,displayName}` из p.display_name.
       Каждый возвращает `{ entries, provenance } | null` (null если родитель не существует).
       Маппинг from/to через `.toISOString()` с null-safety (Date|null).
    2. Provenance для Phase-15 parity sub-resources: в getPlayerWeapons/getPlayerVehicles/
       getPlayerRelationships/getPlayerWeekly/getSquadWeapons/getSquadRelationships/getSquadWeekly
       добавить в возвращаемый payload `provenance: { lastUpdatedAt: maxTimestamp([...доступные calculated_at/observed timestamps возвращённых строк]) }`.
       Если в текущих parity-запросах нет timestamp-колонки — добавить минимально необходимую
       (например `max(stats.calculated_at)`) в существующий select без изменения числовых агрегатов
       (byte-identical parity чисел ДОЛЖНА остаться зелёной — не трогать формулы/сортировки/значения).
    Никакого Steam64 ни в одном новом маппинге.
  </action>
  <verify>
    <automated>pnpm run typecheck &amp;&amp; pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "getPlayerNameHistory|getPlayerMembershipHistory|getSquadMembershipHistory" repository.ts` находит 3 реализации метода.
    - `grep -c "withGaps" repository.ts` >= 3.
    - `grep -nE "order by .*observed_from asc nulls first|order by .*valid_from asc nulls first" repository.ts` подтверждает ascending nulls-first сортировку.
    - В новых history/counterpart-маппингах нет обращения к steam (grep `steam` в новых методах пусто).
    - Все 7 parity sub-resource методов возвращают payload с полем `provenance`.
    - `pnpm run typecheck` и `pnpm run lint` зелёные.
  </acceptance_criteria>
  <done>Три history read-метода с темпоральным SQL + withGaps + provenance реализованы без Steam64; provenance добавлен в Phase-15 parity payloads без изменения числовой parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param (id/slug) → repository SQL | Untrusted `:id` reaches parameterized SQL; boolean-flag branch keeps ::uuid cast safe. |
| DB rows → payload mapper | Single choke point for masking + provenance; counterparts must never carry Steam64. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering / Injection | slug/id in resolver SQL | mitigate | Parameterized only; boolean-flag branch; never concatenate. |
| T-16-10 | Denial of Service | ::uuid cast 500 on slug input | mitigate | looksLikeUuid branch → slug lookup → null/404, cast only in UUID branch. |
| T-16-11 | Information Disclosure | Steam64 via new history/rotation/parity surfaces | mitigate | Counterparts {id,slug,name|displayName} only; mask choke point unchanged; Plan 06 leak-guard sweeps these routes. |
| T-16-12 | Information Disclosure | provenance via now() | mitigate | maxTimestamp over returned rows only; null when empty; never now(). |
| T-16-SC | Tampering | npm package installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm run typecheck` + `pnpm run lint` зелёные.
- Резолвер использует boolean-flag branch во всех трёх detail-методах.
- Три history-метода сортируют ascending nulls-first и строят gaps через withGaps.
- Ни один новый маппинг не эмитит Steam64; mask choke point не тронут.
- Числовая parity Phase 15 не изменена (только добавлено provenance-поле).
</verification>

<success_criteria>
Вся серверная логика API-01 (slug-or-uuid резолвер + getRotation + slug-поля), HIST-01/02
(history-методы с явными gaps, без Steam64) и HIST-03 (provenance из вернувшихся строк, никогда now())
реализована в repository.ts; byte-identical parity Phase 15 сохранена.
</success_criteria>

<output>
Create `.planning/phases/16-slug-resolution-history-provenance/16-04-SUMMARY.md` when done
</output>
