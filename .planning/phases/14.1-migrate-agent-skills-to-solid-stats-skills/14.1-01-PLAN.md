---
phase: 14.1-migrate-agent-skills-to-solid-stats-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills-lock.json
  - AGENTS.md
  - .planning/config.json
  - .claude/skills/
  - .agents/skills/
autonomous: true
requirements: []  # N/A — developer tooling / GSD workflow phase, no product requirement IDs

must_haves:
  truths:
    - "Пять backend/process skills solidstats-* установлены (есть каталоги в .claude/skills/ и .agents/skills/) и записаны в skills-lock.json с source solid-stats/skills."
    - "Все восемь legacy skills удалены из .claude/skills/, .agents/skills/ и skills-lock.json; openapi-to-typescript сохранён."
    - "Таблица 'Project Skills' в AGENTS.md и .planning/config.json agent_skills ссылаются ровно на удержанный/новый набор (6 skills), без legacy-имён."
    - "npx skills update -p отрабатывает чисто, без dangling/duplicate/unresolved записей."
  artifacts:
    - path: "skills-lock.json"
      provides: "Lockfile с 6 записями: 5 solidstats-* + openapi-to-typescript"
      contains: "solidstats-backend-ts-conventions"
    - path: "AGENTS.md"
      provides: "Project Skills таблица с удержанным набором"
      contains: "solidstats-backend-ts-code-review"
    - path: ".planning/config.json"
      provides: "agent_skills mapping ссылается только на удержанные skills"
      contains: ".agents/skills/solidstats-backend-ts-conventions"
  key_links:
    - from: "skills-lock.json"
      to: ".claude/skills/ и .agents/skills/"
      via: "npx skills add/remove зеркалит lock и обе директории"
      pattern: "solidstats-(process-review-standards|process-testing-standards|backend-ts-conventions|backend-ts-code-review|backend-ts-tests)"
    - from: ".planning/config.json agent_skills"
      to: "AGENTS.md Project Skills таблица"
      via: "одинаковый набор имён skills в обоих файлах"
      pattern: "solidstats-backend-ts-"
---

<objective>
Мигрировать набор agent skills репозитория server-2 на общий репозиторий solid-stats/skills:
установить 5 backend/process skills SolidStats, удалить 8 superseded legacy skills, сохранить
внешний reference openapi-to-typescript, и привести AGENTS.md, skills-lock.json и
.planning/config.json к взаимно согласованному состоянию.

Purpose: server-2 должен использовать единый, поддерживаемый внутри продукта набор backend skills
вместо разрозненных github/estesis источников. Это снижает дрейф ревью/тест-стандартов между
репозиториями SolidStats.

Output: обновлённые skills-lock.json, AGENTS.md (таблица Project Skills + GSD:skills блок),
.planning/config.json (agent_skills), и синхронизированные каталоги .claude/skills/ и .agents/skills/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<facts>
Проверено планировщиком (выполни команды сам, если нужно подтвердить — но это авторитетная карта):

- solid-stats/skills резолвится как публичный github slug (git ls-remote https://github.com/solid-stats/skills отдаёт HEAD). Поэтому форма установки `npx skills add solid-stats/skills/<name>` РАБОТАЕТ; локальный fallback (/home/afgan0r/Projects/SolidGames/skills) НЕ нужен.
- CLI `npx skills` имеет команды: add, remove, update, list. `add` зеркалит skill во ВСЕ agent-директории (.claude/skills/ И .agents/skills/) и обновляет skills-lock.json. `remove -y` удаляет из обеих директорий и из lock. `update -p` обновляет только project lock.
- Skills задублированы в ДВУХ директориях: .claude/skills/<name>/ и .agents/skills/<name>/. CLI управляет обеими автоматически — НЕ редактируй каталоги вручную.
- Текущие 9 skills (в обеих директориях + в skills-lock.json):
  УДАЛИТЬ (8): api-design-principles, fastify-best-practices, nodejs-backend-patterns, javascript-testing-patterns, estesis-process-review-standards, estesis-frontend-react-unit-tests, estesis-backend-vc-swagger-spec-write, estesis-backend-vc-swagger-spec-review
  СОХРАНИТЬ (1): openapi-to-typescript (внешний reference, source softaworks/agent-toolkit)
- УСТАНОВИТЬ (5, все из solid-stats/skills, у всех есть SKILL.md в исходном репо): solidstats-process-review-standards, solidstats-process-testing-standards, solidstats-backend-ts-conventions, solidstats-backend-ts-code-review, solidstats-backend-ts-tests
- В исходном репо ТАКЖE есть rust-* и frontend-react-* skills — их НЕ устанавливать (server-2 — это TS/Fastify backend).
- AGENTS.md содержит: (a) авто-управляемый блок между маркерами GSD:skills-start (source:skills/) и GSD:skills-end (~строки 181-195) с таблицей "## Project Skills"; (b) секцию "## Skills First" (~строка 3) — там НЕТ имён конкретных skills, только общее руководство, её НЕ трогать. GSD-CLI-команды для регенерации skills-блока НЕ найдено — таблицу правим вручную внутри маркеров.
- .planning/config.json содержит agent_skills mapping: 12 GSD-агентов, каждый ссылается на один и тот же список из 5 legacy-путей (.agents/skills/...). Все 12 нужно обновить на новый набор путей.
- openapi-to-typescript install name в исходном репо отсутствует (он внешний reference per README solid-stats/skills) — поэтому openapi-to-typescript НЕ переустанавливаем, просто оставляем его существующую запись в lock нетронутой.
</facts>

<interfaces>
Целевой набор из 6 skills после миграции (имена для AGENTS.md таблицы и config.json путей):
- solidstats-process-review-standards
- solidstats-process-testing-standards
- solidstats-backend-ts-conventions
- solidstats-backend-ts-code-review
- solidstats-backend-ts-tests
- openapi-to-typescript   (сохраняется как был)

Пути в config.json agent_skills имеют форму: .agents/skills/<skill-name>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установить 5 solidstats-* skills и удалить 8 legacy skills через CLI</name>
  <files>skills-lock.json, .claude/skills/, .agents/skills/</files>
  <read_first>
    - skills-lock.json (текущие 9 записей)
    - /home/afgan0r/Projects/SolidGames/skills/README.md (политика install names и external-reference)
    - вывод `ls .claude/skills/` и `ls .agents/skills/` (подтвердить стартовое состояние из 9 skills)
  </read_first>
  <action>
Используй ТОЛЬКО CLI `npx --yes skills`, не редактируй каталоги или lock вручную в этом таске.

Шаг 1 — установить 5 skills (каждый отдельной командой с флагом -y, чтобы пропустить интерактивные подтверждения): выполни `npx --yes skills add solid-stats/skills/<name> -y` для каждого из: solidstats-process-review-standards, solidstats-process-testing-standards, solidstats-backend-ts-conventions, solidstats-backend-ts-code-review, solidstats-backend-ts-tests. Если CLI запросит выбор агентов, цель — обе директории (.claude/skills и .agents/skills), как у существующих skills; используй дефолт (CLI зеркалит все agent-директории). НЕ устанавливай rust-* и frontend-react-* skills.

Шаг 2 — удалить 8 legacy skills одной командой: `npx --yes skills remove api-design-principles fastify-best-practices nodejs-backend-patterns javascript-testing-patterns estesis-process-review-standards estesis-frontend-react-unit-tests estesis-backend-vc-swagger-spec-write estesis-backend-vc-swagger-spec-review -y`. НЕ удаляй openapi-to-typescript — он сохраняется.

Шаг 3 — сохрани captured вывод обеих операций для SUMMARY (особенно строки про затронутые agent-директории и обновление skills-lock.json).

Если `npx skills add solid-stats/skills/<name>` вдруг не резолвит slug (хотя планировщик подтвердил, что резолвит), останови и сообщи — НЕ переключайся молча на локальный путь без отметки.
  </action>
  <verify>
    <automated>test "$(ls -1 .claude/skills | grep -c '^solidstats-')" = "5" && test "$(ls -1 .agents/skills | grep -c '^solidstats-')" = "5" && ! ls .claude/skills | grep -Eq '^(api-design-principles|fastify-best-practices|nodejs-backend-patterns|javascript-testing-patterns|estesis-)' && ! ls .agents/skills | grep -Eq '^(api-design-principles|fastify-best-practices|nodejs-backend-patterns|javascript-testing-patterns|estesis-)' && test -d .claude/skills/openapi-to-typescript && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - В .claude/skills/ И .agents/skills/ присутствуют ровно 5 каталогов solidstats-* (process-review-standards, process-testing-standards, backend-ts-conventions, backend-ts-code-review, backend-ts-tests).
    - Ни одного из 8 legacy-имён нет ни в .claude/skills/, ни в .agents/skills/.
    - Каталог openapi-to-typescript сохранён в обеих директориях.
    - skills-lock.json содержит 6 записей: 5 solidstats-* (source solid-stats/skills, sourceType github, с computedHash) + openapi-to-typescript; ни одной legacy-записи.
  </acceptance_criteria>
  <done>CLI установил 5 новых и удалил 8 legacy skills; обе agent-директории и skills-lock.json синхронны и содержат целевой набор из 6 skills.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить таблицу Project Skills в AGENTS.md под новый набор</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (блок между маркерами GSD:skills-start source:skills/ и GSD:skills-end, ~строки 181-195; и секция "## Skills First", ~строка 3 — только чтобы убедиться, что там нет имён skills)
    - /home/afgan0r/Projects/SolidGames/skills/README.md (описания/назначение каждого нового skill для корректных "when to invoke" триггеров)
    - skills-lock.json (после Task 1 — авторитетный список того, что реально установлено)
  </read_first>
  <action>
Внутри маркеров GSD:skills-start ... GSD:skills-end перепиши таблицу "## Project Skills" так, чтобы она перечисляла РОВНО 6 удержанных/новых skills и ничего из legacy. Колонки те же: Skill | When to Invoke. Имена skills — в backticks, триггеры по-русски, отражают назначение из README исходного репо:

- solidstats-process-review-standards — общий фундамент формата код-ревью (severity-бакеты, формат отчёта, правила вердикта); подключается code-review skills, не используется самостоятельно.
- solidstats-process-testing-standards — общая философия тестов (AAA, изоляция, детерминизм, test doubles, размещение файлов); подключается per-stack test skills.
- solidstats-backend-ts-conventions — архитектура и конвенции TS/Fastify backend (вобрал Fastify/Node/API-design best practices); любой роут, плагин, хук, схема валидации, конфиг клиента БД/очереди/S3, дизайн эндпоинта.
- solidstats-backend-ts-code-review — педантичное код-ревью TS/Fastify backend; делегирует ruleset в conventions, формат — в process-review-standards.
- solidstats-backend-ts-tests — guidance по backend-тестам (unit + integration) поверх process-testing-standards; написание/ревью Vitest-тестов.
- openapi-to-typescript — генерация/обновление OpenAPI схемы; синхронизация типов с клиентом web.

НЕ трогай маркеры-комментарии, заголовок "## Project Skills", секцию "## Skills First", и остальные части AGENTS.md (блоки GSD:architecture, GSD:workflow и т.д.). Не вставляй rust-*/frontend-react-* skills.
  </action>
  <verify>
    <automated>awk '/GSD:skills-start/{f=1} f{print} /GSD:skills-end/{f=0}' AGENTS.md > /tmp/sk.txt && for s in solidstats-process-review-standards solidstats-process-testing-standards solidstats-backend-ts-conventions solidstats-backend-ts-code-review solidstats-backend-ts-tests openapi-to-typescript; do grep -q "$s" /tmp/sk.txt || { echo "MISSING $s"; exit 1; }; done && ! grep -Eq '(api-design-principles|fastify-best-practices|nodejs-backend-patterns|javascript-testing-patterns|estesis-)' /tmp/sk.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Таблица Project Skills внутри маркеров перечисляет ровно 6 skills: 5 solidstats-* + openapi-to-typescript.
    - Ни одного из 8 legacy-имён нет в блоке между маркерами.
    - Каждый skill имеет осмысленный русскоязычный триггер "When to Invoke".
    - Маркеры GSD:skills-start/GSD:skills-end и секция "## Skills First" не изменены.
  </acceptance_criteria>
  <done>Таблица Project Skills в AGENTS.md отражает целевой набор из 6 skills с корректными триггерами; legacy-имён нет.</done>
</task>

<task type="auto">
  <name>Task 3: Обновить agent_skills в .planning/config.json и финальная проверка консистентности</name>
  <files>.planning/config.json, skills-lock.json</files>
  <read_first>
    - .planning/config.json (секция agent_skills, ~строки 83-168 — 12 GSD-агентов, каждый с одинаковым списком из 5 legacy-путей)
    - skills-lock.json и AGENTS.md (после Task 1 и 2 — авторитетный целевой набор)
  </read_first>
  <action>
Шаг 1 — обнови mapping agent_skills в .planning/config.json. Для КАЖДОГО из 12 агентов (gsd-phase-researcher, gsd-planner, gsd-plan-checker, gsd-executor, gsd-verifier, gsd-nyquist-auditor, gsd-code-reviewer, gsd-code-fixer, gsd-debugger, gsd-integration-checker, gsd-security-auditor, gsd-codebase-mapper) замени список из 5 legacy-путей на новый набор путей:
.agents/skills/solidstats-process-review-standards, .agents/skills/solidstats-process-testing-standards, .agents/skills/solidstats-backend-ts-conventions, .agents/skills/solidstats-backend-ts-code-review, .agents/skills/solidstats-backend-ts-tests, .agents/skills/openapi-to-typescript.
Используй одинаковый набор путей для всех 12 агентов (как было раньше — общий список). Сохрани валидный JSON (отступы 2 пробела, как в файле). НЕ меняй другие ключи config.json.

Шаг 2 — прогони `npx --yes skills update -p` и захвати вывод; убедись, что команда завершается чисто, без сообщений про dangling / duplicate / unresolved записи.

Шаг 3 — финальный assert консистентности (см. verify): legacy-имён нет нигде (skills-lock.json, AGENTS.md, .planning/config.json, .claude/skills/, .agents/skills/), а целевой набор присутствует.
  </action>
  <verify>
    <automated>! grep -REq '(api-design-principles|fastify-best-practices|nodejs-backend-patterns|javascript-testing-patterns|estesis-)' skills-lock.json AGENTS.md .planning/config.json && for s in solidstats-process-review-standards solidstats-process-testing-standards solidstats-backend-ts-conventions solidstats-backend-ts-code-review solidstats-backend-ts-tests; do grep -q "$s" skills-lock.json && grep -q "$s" .planning/config.json || { echo "MISSING $s"; exit 1; }; done && node -e "JSON.parse(require('fs').readFileSync('.planning/config.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('skills-lock.json','utf8'))" && npx --yes skills update -p && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Все 12 агентов в .planning/config.json agent_skills ссылаются ровно на 6 целевых путей (.agents/skills/solidstats-* x5 + .agents/skills/openapi-to-typescript), без legacy-путей.
    - config.json и skills-lock.json — валидный JSON.
    - `npx skills update -p` завершается чисто, без dangling/duplicate/unresolved.
    - grep по skills-lock.json, AGENTS.md, .planning/config.json не находит ни одного из 8 legacy-имён; все 5 solidstats-* присутствуют в lock и config.
  </acceptance_criteria>
  <done>config.json agent_skills согласован с lock и AGENTS.md; npx skills update -p чист; ни одного legacy-следа в трёх файлах конфигурации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| внешний источник skills → локальный репо | skills.sh резолвит и копирует SKILL.md-файлы из github-репозитория в локальные agent-директории и lockfile |

Эта фаза не имеет runtime/network/data attack surface в продукте: она лишь подменяет локальные файлы определений skills и записи lockfile. Никакого кода приложения, БД, API или UI не затрагивается.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-01 | Tampering | источник solid-stats/skills (supply chain) | mitigate | skills-lock.json пинит intended source (solid-stats/skills) с integrity-хешем computedHash на каждый skill; источник — собственный доверенный репозиторий пользователя (github:solid-stats/skills, подтверждён git ls-remote). |
| T-14.1-02 | Tampering | openapi-to-typescript (сохраняемый external reference) | accept | существующая запись с computedHash не меняется; источник softaworks/agent-toolkit уже был доверен в предыдущих фазах. |

Block-on-high: high-severity угроз в этой фазе нет.
</threat_model>

<verification>
Фаза считается выполненной, когда (соответствует 4 success criteria ROADMAP):
1. `ls .claude/skills` и `ls .agents/skills` показывают 5 каталогов solidstats-* + openapi-to-typescript, и эти 5 записаны в skills-lock.json с source solid-stats/skills.
2. grep по skills-lock.json, .claude/skills/, .agents/skills/, AGENTS.md, .planning/config.json не находит ни одного из 8 legacy-имён; openapi-to-typescript сохранён.
3. Таблица Project Skills в AGENTS.md и agent_skills в .planning/config.json перечисляют ровно один и тот же набор из 6 skills.
4. `npx skills update -p` завершается чисто (захваченный вывод без dangling/duplicate/unresolved).
5. После doc/commit-шага `git status --short` чист (требование AGENTS.md).
</verification>

<success_criteria>
- 5 solidstats-* backend/process skills установлены в обеих agent-директориях и в skills-lock.json (source solid-stats/skills).
- 8 legacy skills удалены отовсюду; openapi-to-typescript сохранён.
- AGENTS.md (таблица Project Skills) и .planning/config.json (agent_skills) взаимно согласованы и ссылаются ровно на 6 skills.
- `npx skills update -p` резолвит чисто.
- git-дерево чисто после коммита.
</success_criteria>

<output>
Создай `.planning/phases/14.1-migrate-agent-skills-to-solid-stats-skills/14.1-01-SUMMARY.md` по завершении.
</output>
