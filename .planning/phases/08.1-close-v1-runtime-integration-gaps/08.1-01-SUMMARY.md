# Plan 08.1-01 Summary: Runtime Ingest/Parser Wiring and Operations Authorization

## Completed

- Added moderator/admin authorization to `/operations/ingest-staging`, `/operations/ingest-staging/:id`, `/operations/parse-jobs`, `/operations/parse-jobs/:id`, and `/operations/parse-jobs/:id/history`.
- Added route tests proving anonymous users get 401 and authenticated non-privileged users get 403 for operations visibility.
- Added `IntervalTask` for bounded non-overlapping runtime polling.
- Added RabbitMQ parser runtime support for durable parser exchange topology, confirming parse request publishes, and `parse.completed` / `parse.failed` consumers.
- Wired production startup to run ingest promotion and parse-job publishing loops using existing `IngestPromotionService` and `ParseJobPublisher`.
- Wired parser completion/failure consumers to `PgIngestRepository.recordParserCompleted()` and `recordParserFailed()`.
- Added runtime configuration for parser contract version, promotion batch size, publish batch size, and poll interval.
- Updated env examples, README, and deployment docs for the new runtime behavior.

## Remaining Gap Boundaries

- Parser completion currently records terminal parser state but does not yet load parser artifacts from S3 or call aggregate recalculation. That remains Plan 08.1-04.
- Public stats still need PostgreSQL-backed read model wiring. That remains Plan 08.1-02.
- Production auth/request/moderation/audit/workflow persistence is still in-memory/no-op where previously identified. That remains Plan 08.1-03.

## Verification

- `pnpm exec vitest run src/modules/ingest/routes/tests src/modules/ingest/publisher.test.ts src/modules/ingest/service.test.ts src/test/app.test.ts src/infra/storage/client.test.ts --coverage.enabled false` passed.
- `pnpm run lint` passed.
- `pnpm run typecheck` passed.
- `pnpm test` passed: 37 files, 148 tests.
- `pnpm run test:integration` passed: 2 files, 7 tests.
- `pnpm run openapi:check` passed.
- `pnpm run ops:backup:check` passed.
- `pnpm run verify` passed with 41 files, 165 tests, and 100% statements/branches/functions/lines coverage.

## Notes

- Commands emit Node engine warnings because the active shell is Node v22.22.2 while the repo targets Node >=25 <26.
