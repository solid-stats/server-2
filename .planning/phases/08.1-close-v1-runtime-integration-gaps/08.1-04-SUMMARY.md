---
phase: 08.1
plan_id: 08.1-04
title: Recalculation wiring and verification restoration
status: completed
completed_at: "2026-05-10T10:31:00+07:00"
---

# Plan 08.1-04 Summary

## Completed

- Wired parser completion to load parser artifacts from S3-compatible storage, persist the artifact snapshot, and invoke `ParserResultRecalculationService`.
- Replaced production noop audit recalculation with a PostgreSQL-backed recalculator for `parser_result` and `replay` targets.
- Added storage artifact loading tests, ingest runtime recalculation tests, and audit recalculator tests.
- Restored missing verification and validation artifacts for phases 03-08 and 08.1.

## Verification

- `pnpm exec vitest run src/modules/ingest/runtime.test.ts src/modules/ingest/repository/tests/postgres.test.ts src/modules/requests/routes/audit-recalculator.test.ts src/infra/storage/client.test.ts --no-file-parallelism --coverage.enabled false`
- `pnpm run test:coverage`
- `pnpm run verify`

All commands passed. Local pnpm emitted the existing Node engine warning because the shell is Node v22.22.2 while the repo targets Node >=25 <26.

## Notes

- Parser completion now returns the created parser result id; terminal duplicate completions return `null` and skip recalculation.
- Audit patches without a replay/parser-result target are recorded with `no_parser_result_target` recalculation status.
