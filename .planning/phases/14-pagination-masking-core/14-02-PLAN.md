---
phase: 14-pagination-masking-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/public-stats/routes/pagination/mask.ts
  - src/modules/public-stats/routes/pagination/mask.test.ts
  - src/modules/public-stats/repository.ts
  - src/infra/logging/logger.ts
  - src/test/integration/steamid-leak-guard.test.ts
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "mapPlayerProfile emits SteamIDs only as a last-4 masked string (e.g. ...7890), never the raw Steam64."
    - "A 7656119\\d{10} regex over every public response body finds zero matches."
    - "pino redact paths cover any structured-log field that could carry a SteamID."
    - "A reusable expectNoSteam64 helper asserts zero 7656119\\d{10} matches over a payload."
    - "PlayerProfileResponse.steamIds now carries masked `...NNNN` strings instead of full Steam64 — a semantic change the `web` consumer must be told about (same string[] type, different value contract); the SUMMARY records that web must be informed."
  artifacts:
    - path: "src/modules/public-stats/routes/pagination/mask.ts"
      provides: "maskSteamId(steamId) -> last-4 masked string"
      contains: "export function maskSteamId"
    - path: "src/test/integration/steamid-leak-guard.test.ts"
      provides: "zero-Steam64 guard across all public routes + error paths"
      contains: "7656119"
  key_links:
    - from: "src/modules/public-stats/repository.ts (mapPlayerProfile)"
      to: "mask.ts (maskSteamId)"
      via: "steamIds: row.steam_ids.map(maskSteamId)"
      pattern: "steam_ids\\.map\\(maskSteamId\\)"
    - from: "src/infra/logging/logger.ts"
      to: "redact.paths"
      via: "added steamId path"
      pattern: "steamId|steam_id|steamIds"
---

<objective>
Close the live full-Steam64 leak in `PlayerProfileResponse.steamIds`. Mask at the single row->payload choke point (`mapPlayerProfile`), add pino redaction as defense-in-depth, and lock the contract with a reusable zero-match guard test that sweeps every public route and error path.

Purpose: SEC-01/SEC-02 are the freeze-blocking headline threat — a full Steam64 (`7656119\d{10}`) must never leave the server via any response body, cursor token, log line, or error payload. This plan makes the mapper the only place SteamIDs surface, in masked form, and proves it.
Output: `mask.ts` + masked `mapPlayerProfile`; extended pino redact; `steamid-leak-guard.test.ts` integration sweep with an `expectNoSteam64` helper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pagination-masking-core/14-CONTEXT.md
@.planning/phases/14-pagination-masking-core/14-RESEARCH.md
@.agents/skills/javascript-testing-patterns/SKILL.md
@.agents/skills/nodejs-backend-patterns/SKILL.md
@.agents/skills/fastify-best-practices/SKILL.md
@.agents/skills/estesis-frontend-react-unit-tests/SKILL.md

<interfaces>
<!-- The live leak. repository.ts:517-526 -->
function mapPlayerProfile(row, rotationId): PlayerProfile {
  return { ...mapPlayerSummary(row, rotationId), aliases: row.aliases, steamIds: row.steam_ids }; // <-- raw leak
}
<!-- PlayerProfile.steamIds is string[] (models.ts:99-102); PlayerProfileResponse.steamIds is Type.Array(Type.String()) (schemas.ts:60-66). The masked value stays string[], so no schema type change is required. -->
<!-- Current pino redact paths (logger.ts): databaseUrl, rabbitmqUrl, s3.accessKeyId, s3.secretAccessKey, *.password, *.secret. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: maskSteamId + apply at mapPlayerProfile choke point (SEC-02)</name>
  <read_first>
    - .planning/phases/14-pagination-masking-core/14-RESEARCH.md (Pattern 3; Pitfall 4; Open Question 2 — masked field name)
    - src/modules/public-stats/repository.ts:505-526 (mapPlayerSummary / mapPlayerProfile)
    - src/modules/public-stats/routes/models.ts:99-102 (PlayerProfile.steamIds)
  </read_first>
  <behavior>
    - maskSteamId("76561198012347890") === "...7890" (leading ellipsis marker + last 4 digits, returned as a string).
    - maskSteamId on a short/edge value returns "..." + the available trailing chars without throwing.
    - mapPlayerProfile output's steamIds is the input row.steam_ids mapped through maskSteamId; no element matches 7656119\d{10}.
  </behavior>
  <action>
    Create `mask.ts` exporting `maskSteamId(steamId: string): string` returning `` `...${steamId.slice(-4)}` ``. Decision (per 14-RESEARCH Open Question 2): reuse the existing `steamIds: string[]` field holding masked strings (minimizes `web` churn, keeps the array shape; no schema type change). NOTE this is a semantic contract change for `web` — same `string[]` type, but values are now masked `...NNNN`, not full Steam64; record it in the SUMMARY so `web` is informed. In `repository.ts`, change `mapPlayerProfile` line `steamIds: row.steam_ids` to `steamIds: row.steam_ids.map(maskSteamId)` and import `maskSteamId`. Confirm no other mapper or query path emits `steam_id`/`steam_ids` raw (grep `steam_id` in repository.ts; the list/summary mappers do not include steamIds). Write `mask.test.ts` (RITE/AAA, case-table) for the masking function including edge lengths.
  </action>
  <verify>
    <automated>pnpm test -- src/modules/public-stats/routes/pagination/mask.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "steam_ids.map(maskSteamId)" src/modules/public-stats/repository.ts` returns exactly one match.
    - `grep -vn '^\s*//' src/modules/public-stats/repository.ts | grep -c "steamIds: row.steam_ids$"` is 0 (no raw emission remains).
    - `mask.test.ts` asserts `maskSteamId("76561198012347890") === "...7890"`.
  </acceptance_criteria>
  <done>SteamIDs leave the row layer only through maskSteamId at mapPlayerProfile; masked form is the last-4 string; raw emission removed; the web-facing semantic change is flagged for the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pino redaction + zero-Steam64 leak guard sweep (SEC-01)</name>
  <read_first>
    - .planning/phases/14-pagination-masking-core/14-RESEARCH.md (Pattern 3 defense-in-depth; Pitfall 4; Validation Architecture Wave 0)
    - src/infra/logging/logger.ts (existing redact.paths)
    - src/modules/public-stats/routes/tests/players.test.ts (app.inject pattern)
    - .planning/phases/14-pagination-masking-core/14-VALIDATION.md (Wave 0 — expectNoSteam64 helper requirement)
  </read_first>
  <behavior>
    - createLoggerOptions().redact.paths includes a path covering any field that could carry a SteamID (e.g. "*.steamId", "*.steamIds", "*.steam_id").
    - expectNoSteam64(value) serializes value and asserts JSON.stringify(value) has zero 7656119\d{10} matches; it fails loudly when given a payload containing a full Steam64.
    - The guard test boots the app (in-memory read model is sufficient for the route-coverage sweep) and injects every public list + detail request that exists at Wave 1, asserting expectNoSteam64 over each response body and over a captured cursor token.
    - COVERAGE GATE (Wave 1 is graded on its own under the repo's 100% V8 gate): the Wave 1 form of `steamid-leak-guard.test.ts` must be 100% covered on its own — every branch authored in this file must be exercised by Wave 1. The bad-cursor 400 case and the un-exercised real-pg profile path are NOT yet wired (Plan 14-03 Task 1/Task 3 supply the 400 surface and real-pg seed), so they must NOT be authored as dead/skipped branches in the Wave 1 file. Represent the deferred work as an `it.todo("bad-cursor 400 — wired by Plan 14-03 Task 3")` placeholder (a todo entry, which adds no un-covered executable branch) rather than a `it.skip`/`describe.skip` block or an `if`-guarded un-run path. Do not introduce any executable line/branch in this file that Wave 1 cannot exercise.
  </behavior>
  <action>
    In `logger.ts`, extend `redact.paths` with `"*.steamId"`, `"*.steamIds"`, `"*.steam_id"`, `"*.steam_ids"` (keep existing paths and the `censor: "redacted"`). Create `src/test/integration/steamid-leak-guard.test.ts` exporting/defining a reusable `expectNoSteam64(value: unknown)` helper (regex `/7656119\d{10}/`) and a route sweep: `buildApp()` then `app.inject` GET `/stats/players`, `/stats/squads`, `/stats/bounty`, `/stats/leaderboards`, `/stats/players/:id`, `/stats/squads/:id` — for each, call `expectNoSteam64(response.json())` and `expectNoSteam64(response.payload)`. Because this plan runs in Wave 1 before route wiring, scope the sweep to the routes that exist now and the masked-mapper unit path. For the bad-cursor 400 (Plan 14-03 wires the 400) and the real-pg profile assertion (Plan 14-03 Task 3 seeds real pg), add an `it.todo("...")` placeholder ONLY — do NOT author a skipped or un-runnable executable branch, because the repo enforces 100% V8 coverage and Wave 1 grades this file on its own; a skipped/un-exercised branch would dip Wave 1 coverage below 100%. Every executable line in the Wave 1 file must run under Wave 1's own test invocation. Note in code: the in-memory fake read model returns no Steam64, so the strongest assertion is the masked-mapper unit case plus the real-pg guard extension Plan 03 adds to `tests/postgres.test.ts`.
  </action>
  <verify>
    <automated>pnpm test -- src/test/integration/steamid-leak-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "steamId" src/infra/logging/logger.ts` shows the added redact path(s).
    - `grep -n "7656119" src/test/integration/steamid-leak-guard.test.ts` shows the guard regex.
    - The guard test sweeps all six public route shapes and exports/exposes `expectNoSteam64` for reuse by Plan 03.
    - A negative self-test asserts `expectNoSteam64({ x: "76561198012347890" })` throws (the helper actually catches a leak).
    - The deferred bad-cursor/real-pg work is an `it.todo` placeholder (not `it.skip`/`describe.skip`), and `pnpm test -- --coverage src/test/integration/steamid-leak-guard.test.ts` reports 100% for this file on its own (no skipped or un-exercised branch).
  </acceptance_criteria>
  <done>pino redacts SteamID-bearing log fields, and a reusable expectNoSteam64 guard sweeps every public route asserting zero 7656119\d{10} matches; the helper is proven to catch a planted leak; the Wave 1 file is self-contained at 100% coverage with deferred cases held as it.todo placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row -> API payload | Raw Steam64 lives in `row.steam_ids`; this boundary (mapPlayerProfile) is the single choke point where it must be masked. |
| API -> logs | Structured log fields could carry a Steam64 if a raw row is logged. |
| API -> error payload | An error echoing a row/value could carry a Steam64. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | mapPlayerProfile (response body) | mitigate | Mask to last-4 at the mapper choke point; `steamIds` array holds only `...NNNN` strings; guarded by `7656119\d{10}` zero-match sweep. |
| T-14-05 | Information Disclosure | structured logs (pino) | mitigate | Add `*.steamId`/`*.steamIds`/`*.steam_id`/`*.steam_ids` to `redact.paths`; never `log.info(rawRow)` (code review + guard). |
| T-14-06 | Information Disclosure | error payloads | mitigate | BadCursorError (Plan 01) uses fixed-string messages; leak-guard sweep includes an error path (wired by Plan 03); no raw row echoed in handlers. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies this phase (locked); no install task; no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm test -- src/modules/public-stats/routes/pagination/mask.test.ts` and the leak-guard test are green.
- `pnpm run typecheck` passes; `steamIds` remains `string[]` (no schema change needed).
- `grep -rn "row.steam_ids$" src/modules/public-stats/repository.ts` returns no raw (unmasked) emission.
- `pnpm test -- --coverage src/test/integration/steamid-leak-guard.test.ts` reports 100% for the Wave 1 leak-guard file on its own (no skipped/un-exercised branch).
</verification>

<success_criteria>
- mapPlayerProfile emits only masked last-4 SteamIDs (SEC-02).
- pino redacts SteamID-bearing fields; reusable expectNoSteam64 guard proves zero 7656119\d{10} matches across public routes, and is proven to catch a planted leak (SEC-01).
- The Wave 1 leak-guard file is fully covered on its own; deferred bad-cursor/real-pg cases are `it.todo` placeholders for Plan 03.
- Zero new runtime dependencies.
</success_criteria>

<output>
Create `.planning/phases/14-pagination-masking-core/14-02-SUMMARY.md` when done. Record: the chosen masked field shape (`steamIds` reused as masked `string[]`), an explicit "WEB CONSUMER NOTICE" that `steamIds` values are now masked `...NNNN` strings (semantic change — web must be informed), the path to `expectNoSteam64`, and the `it.todo` bad-cursor/real-pg placeholder cases Plan 03 must replace after wiring 400s and real-pg seed.
</output>
</content>
</invoke>
