---
phase: 13-diff-harness-contract-and-boundary-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/statistics/diff/diff-contract.ts
  - src/modules/statistics/diff/tests/diff-contract.test.ts
  - src/operations/check-app-boundary-guards.ts
  - src/operations/check-app-boundary-guards.test.ts
  - package.json
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-05
  - DIFF-06
  - OPS-13
must_haves:
  truths:
    - Diff reports are contract-versioned and always review-required.
    - Strict failure taxonomy includes missing players, missing matches, changed public aggregate totals, parser/export failures, and unexplained differences.
    - The only default known difference is documented teamkill-death duplicate-slot/respawn behavior.
    - App workflow guard scans executable workflows, not docs.
  artifacts:
    - path: src/modules/statistics/diff/diff-contract.ts
      provides: old-vs-new diff report contract
      contains: old-vs-new-diff.v1
    - path: src/operations/check-app-boundary-guards.ts
      provides: CI guard against app-owned orchestration drift
      contains: kubectl
---

<objective>
Implement the Phase 13 diff contract module and workflow boundary guard.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add diff contract module</name>
  <files>src/modules/statistics/diff/diff-contract.ts, src/modules/statistics/diff/tests/diff-contract.test.ts</files>
  <read_first>src/modules/statistics/export/legacy-public-export.ts, docs/legacy-public-export.md</read_first>
  <action>Add a typed `old-vs-new-diff.v1` report contract with input metadata, snapshot metadata, summary counts, strict failure codes, the single known teamkill-death difference code, and `reviewRequired` output.</action>
  <verify>pnpm vitest run src/modules/statistics/diff/tests/diff-contract.test.ts</verify>
  <acceptance_criteria>
    - `reviewRequired` is always true.
    - Known difference policy rejects codes outside the default teamkill-death duplicate-slot/respawn code.
    - Tests cover sample, partial-staging, and full-corpus scope metadata.
  </acceptance_criteria>
  <done>Diff consumers have a backend-owned TypeScript contract to target.</done>
</task>

<task type="auto">
  <name>Task 2: Add app workflow boundary guard</name>
  <files>src/operations/check-app-boundary-guards.ts, src/operations/check-app-boundary-guards.test.ts, package.json</files>
  <read_first>.github/workflows/cd.yml, package.json</read_first>
  <action>Add `pnpm run ops:boundary:check` and include it in `pnpm run verify`. The guard scans `.github/workflows` for staging SSH/SCP/rsync, `kubectl`, Kubernetes Secret mutation, rollout orchestration, and direct Kubernetes config execution patterns.</action>
  <verify>pnpm vitest run src/operations/check-app-boundary-guards.test.ts && pnpm run ops:boundary:check && pnpm run typecheck && pnpm run lint</verify>
  <acceptance_criteria>
    - Current `.github/workflows/cd.yml` passes.
    - Tests prove forbidden workflow snippets fail with actionable findings.
    - `pnpm run verify` runs the boundary guard.
  </acceptance_criteria>
  <done>App CI prevents reintroducing infrastructure-owned orchestration into workflows.</done>
</task>
</tasks>

<verification>
- [ ] pnpm vitest run src/modules/statistics/diff/tests/diff-contract.test.ts src/operations/check-app-boundary-guards.test.ts
- [ ] pnpm run ops:boundary:check
- [ ] pnpm run typecheck
- [ ] pnpm run lint
- [ ] pnpm run test:coverage
</verification>

<success_criteria>
- Diff contract is typed, tested, and review-required.
- Workflow boundary guard is part of `pnpm run verify`.
- OPS-13 has executable CI evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/13-diff-harness-contract-and-boundary-guards/13-01-SUMMARY.md`.
</output>
