---
phase: 08
plan: 08-01
title: Production Docker Compose, environment, and deployment documentation
status: completed
completed_at: "2026-05-10T08:56:56+07:00"
wave: 1
depends_on: [07-05]
autonomous: true
requirements_addressed: [INFRA-04]
truths:
  - "Production deployment has a Docker image that runs compiled JavaScript."
  - "Production Compose starts API dependencies and runs migrations before the API."
  - "Production environment variables are documented separately from local development defaults."
  - "Deployment runbook documents first deploy and health checks."
files_modified:
  - Dockerfile
  - .dockerignore
  - .env.production.example
  - docker-compose.prod.yml
  - tsconfig.build.json
  - package.json
  - README.md
  - docs/deployment.md
---

# Plan 08-01: Production Compose and Deployment Docs

<objective>Add the single-VPS production Docker Compose path and deployment runbook.</objective>

<tasks>
<task id="08-01-01"><read_first>package.json, tsconfig.json, src/server.ts</read_first><action>Add production build/runtime support for compiled TypeScript.</action><acceptance_criteria>`pnpm run build` emits runnable production JavaScript and the Dockerfile runs `node dist/src/server.js`.</acceptance_criteria></task>
<task id="08-01-02"><read_first>docker-compose.yml, .env.example</read_first><action>Add production Compose and environment templates for API, migrations, PostgreSQL, RabbitMQ, and MinIO.</action><acceptance_criteria>Compose config parses with `.env.production` values and persists DB/queue/object-storage volumes.</acceptance_criteria></task>
<task id="08-01-03"><read_first>README.md</read_first><action>Document production deployment commands and health checks.</action><acceptance_criteria>README links to a production deployment runbook with first deploy and verification commands.</acceptance_criteria></task>
</tasks>

<verification>
- `pnpm run build` passed on 2026-05-10.
- `docker compose --env-file .env.production -f docker-compose.prod.yml config` passed on 2026-05-10 using `.env.production.example` copied to `.env.production`.
</verification>
