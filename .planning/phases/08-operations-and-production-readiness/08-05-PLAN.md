# Plan 08-05: Backup and Restore Documentation

## Goal

Document and validate v1 production backup/restore procedures for PostgreSQL and S3-compatible object storage.

## Scope

- Add a production backup/restore runbook for the Compose deployment.
- Cover PostgreSQL dump/restore and MinIO bucket mirror/restore.
- Document restore drill validation.
- Add an automated runbook check and include it in `pnpm run verify`.
- Update README, deployment docs, and planning state.

## Verification

- `pnpm run ops:backup:check`
- Full `pnpm run verify` before commit.
