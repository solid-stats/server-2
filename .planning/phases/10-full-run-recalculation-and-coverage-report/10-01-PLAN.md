---
phase: 10-full-run-recalculation-and-coverage-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/statistics/service/full-run-recalculation.ts
  - src/modules/statistics/service/tests/full-run-recalculation.test.ts
  - src/modules/statistics/repository/full-run.ts
  - src/modules/statistics/repository/tests/full-run.test.ts
  - src/operations/recalculate-statistics.ts
  - package.json
autonomous: true
requirements:
  - OPS-07
  - OPS-08
  - OPS-09
  - OPS-10
  - OPS-11
must_haves:
  truths:
    - Full-run recalculation operates over all current parser results.
    - The command is idempotent because it delegates to existing aggregate replacement methods.
    - Reports include required counts and per-result skip/failure evidence.
    - Dry-run coverage separates lifecycle states and stale current parser results.
  artifacts:
    - path: src/modules/statistics/service/full-run-recalculation.ts
      provides: Full-run orchestration and deterministic summaries
      contains: FullRunRecalculationService
    - path: src/modules/statistics/repository/full-run.ts
      provides: PostgreSQL lifecycle counts and current parser result targets
      contains: PgFullRunStatisticsRepository
    - path: src/operations/recalculate-statistics.ts
      provides: Operator CLI entrypoint
      contains: ops:stats:recalculate
  key_links:
    - from: src/operations/recalculate-statistics.ts
      to: src/modules/statistics/service/full-run-recalculation.ts
      via: FullRunRecalculationService
      pattern: recalculateAllCurrentParserResults
---

<objective>
Implement the full-run recalculation service, PostgreSQL status read model, and CLI command.

Purpose: Phase 10 needs a supported way for operators to recalculate every current parser result and capture coverage evidence without one-off SQL.
Output: Production TypeScript modules, package scripts, and focused tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-full-run-recalculation-and-coverage-report/10-CONTEXT.md
@.planning/phases/10-full-run-recalculation-and-coverage-report/10-RESEARCH.md
@src/modules/statistics/service/recalculation.ts
@src/modules/statistics/repository/repository.ts
@src/operations/check-backup-runbook.ts
@package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add full-run orchestration service</name>
  <files>src/modules/statistics/service/full-run-recalculation.ts, src/modules/statistics/service/tests/full-run-recalculation.test.ts</files>
  <read_first>src/modules/statistics/service/recalculation.ts, src/modules/statistics/service/tests/recalculation.test.ts</read_first>
  <action>Add `FullRunRecalculationService` with dry-run coverage and full recalculation methods. Summaries must include parser result count, recalculated count, skipped count, missing rotation count, missing timestamp count, missing identity count, changed aggregate rows, stale count, and failures. Per-result items must include parser result id, replay id, source identifiers, status, reason code for skips/failures, and aggregate row counts when recalculated.</action>
  <verify>pnpm vitest run src/modules/statistics/service/tests/full-run-recalculation.test.ts</verify>
  <acceptance_criteria>
    - Service tests cover recalculated, missing timestamp, missing rotation, missing identity, stale, and failed outcomes.
    - Service output ordering is deterministic from repository target order.
  </acceptance_criteria>
  <done>Full-run orchestration is isolated from PostgreSQL details and covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add PostgreSQL full-run read model</name>
  <files>src/modules/statistics/repository/full-run.ts, src/modules/statistics/repository/tests/full-run.test.ts</files>
  <read_first>src/modules/statistics/repository/repository.ts, src/modules/statistics/repository/tests/utilities.ts</read_first>
  <action>Add `PgFullRunStatisticsRepository` that extends existing aggregate recalculation behavior with lifecycle counts and current parser result targets. Targets must include replay identifiers, timestamps, parser result creation time, stale state, player count, and conservative missing identity count.</action>
  <verify>pnpm vitest run src/modules/statistics/repository/tests/full-run.test.ts</verify>
  <acceptance_criteria>
    - Repository tests cover lifecycle counts with missing zero states.
    - Repository tests cover target mapping, stale detection, and blank-name identity gaps.
  </acceptance_criteria>
  <done>Full-run status evidence is available through supported repository code.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI operation scripts</name>
  <files>src/operations/recalculate-statistics.ts, package.json</files>
  <read_first>src/operations/check-backup-runbook.ts, src/infra/db/client.ts, src/config/env.ts</read_first>
  <action>Add a thin CLI entrypoint that builds the PostgreSQL repository/service from app config, prints JSON to stdout, and supports dry-run coverage mode. Add package scripts `ops:stats:coverage` and `ops:stats:recalculate`.</action>
  <verify>pnpm run typecheck && pnpm run lint</verify>
  <acceptance_criteria>
    - `package.json` contains both operation scripts.
    - CLI has no SSH, Kubernetes, or deployment orchestration responsibilities.
  </acceptance_criteria>
  <done>Operators have supported commands for status and recalculation.</done>
</task>
</tasks>

<verification>
- [ ] pnpm vitest run src/modules/statistics/service/tests/full-run-recalculation.test.ts src/modules/statistics/repository/tests/full-run.test.ts
- [ ] pnpm run typecheck
- [ ] pnpm run lint
</verification>

<success_criteria>
- Full-run recalculation command exists and delegates to existing aggregate replacement logic.
- Dry-run coverage reports lifecycle counts and stale current parser results.
- Required counts and per-result evidence are included in JSON output.
- Tests prove deterministic behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-run-recalculation-and-coverage-report/10-01-SUMMARY.md`.
</output>
