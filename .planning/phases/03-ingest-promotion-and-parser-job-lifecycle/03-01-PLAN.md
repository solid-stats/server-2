---
phase: 03
plan: 03-01
title: Staging polling, status transitions, and promotion transaction boundaries
status: planned
wave: 1
depends_on: [02-03]
autonomous: true
requirements_addressed: [INGEST-01, INGEST-04, INGEST-05, JOB-01]
truths:
  - "Promotion must be durable in PostgreSQL before RabbitMQ publish is attempted."
  - "Parallel workers must not promote the same staging row twice."
files_modified: [src/infra/db/client.ts, src/modules/ingest/types.ts, src/modules/ingest/repository.ts, src/modules/ingest/service.ts, src/modules/ingest/service.test.ts]
---

# Plan 03-01: Promotion Transaction Boundary

<objective>Create the core ingest service that claims pending staging rows and promotes accepted candidates into canonical replays and queued parse jobs in one transaction.</objective>

<tasks>
<task id="03-01-01"><read_first>src/infra/db/client.ts, src/infra/db/migrations/0001_v1_domain_schema.sql, .planning/phases/03-ingest-promotion-and-parser-job-lifecycle/03-CONTEXT.md</read_first><action>Add reusable DB pool/client typing and repository methods for claiming pending staging rows, updating staging status, inserting replays, and creating parse jobs.</action><acceptance_criteria>Repository SQL uses parameterized queries; claiming uses row locking or equivalent transaction safety; existing health client still works.</acceptance_criteria></task>
<task id="03-01-02"><read_first>src/modules/ingest/repository.ts</read_first><action>Add `IngestPromotionService` with a `promotePending` entry point and per-row promotion transaction that creates `replays` and queued `parse_jobs` before marking staging promoted.</action><acceptance_criteria>Unit tests prove a pending staging row becomes a replay plus queued parse job and no RabbitMQ publish is required for promotion success.</acceptance_criteria></task>
</tasks>
