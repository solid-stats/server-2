---
phase: 19-contract-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/openapi/register-openapi.ts
  - openapi/server-2.openapi.json
  - src/openapi/frozen-contract.test.ts
autonomous: true
requirements: [FREEZE-01, FREEZE-02, FREEZE-04]
must_haves:
  truths:
    - "openapi/server-2.openapi.json carries info.version == 1.0.0"
    - "The regenerated artifact byte-equals the running app's generated schema (openapi:verify passes)"
    - "web can still generate types from openapi/server-2.openapi.json at the unchanged path (openapi:check passes)"
    - "A DB-free test asserts the artifact has version 1.0.0, no full Steam64, and no page/pageSize/total on public /stats/* list top-level metadata"
    - "The frozen-contract test runs in the fast unit suite (pnpm test) without docker services"
  artifacts:
    - path: "src/openapi/register-openapi.ts"
      provides: "Single source of truth for the contract version literal (1.0.0)"
      contains: 'version: "1.0.0"'
    - path: "openapi/server-2.openapi.json"
      provides: "Published 1.0.0 artifact consumed by web via openapi-typescript and used as the diff baseline"
      contains: '"version": "1.0.0"'
    - path: "src/openapi/frozen-contract.test.ts"
      provides: "DB-free static frozen-contract invariants (version, no-Steam64, scoped pagination metadata)"
      min_lines: 40
  key_links:
    - from: "src/openapi/register-openapi.ts"
      to: "openapi/server-2.openapi.json"
      via: "pnpm run openapi:export (createOpenApiSchema)"
      pattern: '"version": "1\.0\.0"'
    - from: "src/openapi/frozen-contract.test.ts"
      to: "openapi/server-2.openapi.json"
      via: "readFile(resolve('openapi/server-2.openapi.json'))"
      pattern: "openapi/server-2\\.openapi\\.json"
---

<objective>
Bump the OpenAPI contract version from `0.1.0` to `1.0.0` at its single source of truth, regenerate and commit the published `openapi/server-2.openapi.json` artifact so it reflects `1.0.0` plus every Phase 14–18 surface, and add a DB-free static "frozen-contract" test that locks three invariants over the committed artifact.

Purpose: Establish the stable `1.0.0` baseline `web` consumes via `openapi-typescript`, and add a fast defense-in-depth test layer that asserts the frozen invariants without booting the app or any docker services. This is the prerequisite baseline for the CI breaking-change gate (Plan 02): the gate must diff against an already-regenerated `1.0.0` artifact (per RESEARCH Pitfall 4 ordering).

Output: One changed version literal, one regenerated committed artifact, one new test file in the fast unit suite.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-contract-freeze/19-CONTEXT.md
@.planning/phases/19-contract-freeze/19-RESEARCH.md
@src/openapi/register-openapi.ts
@src/openapi/export-openapi.ts
@src/openapi/verify-openapi.ts
@src/test/integration/steamid-leak-guard.test.ts
</context>

<artifacts_this_phase_produces>
This plan (19-01) produces:
- **Version bump:** `src/openapi/register-openapi.ts` `info.version` `"0.1.0"` → `"1.0.0"` (keep `openapi: "3.0.3"`).
- **Regenerated artifact:** `openapi/server-2.openapi.json` re-exported via `pnpm run openapi:export`, committed at `1.0.0`.
- **New test file:** `src/openapi/frozen-contract.test.ts` — DB-free unit test (FREEZE-01 (c) version, SEC (b) no-Steam64, FREEZE-03 (a) scoped pagination metadata).

Plan 19-02 produces the CI `contract-diff` job, the README bump-policy section, the FREEZE-04 PG-gate confirmation, and the branch-protection checkpoint.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump contract version to 1.0.0 and regenerate the committed artifact</name>
  <files>src/openapi/register-openapi.ts, openapi/server-2.openapi.json</files>
  <read_first>
    - src/openapi/register-openapi.ts (the version literal lives at the `info` block; line ~12 `version: "0.1.0"`)
    - src/openapi/export-openapi.ts (regeneration entry — writes openapi/server-2.openapi.json via createOpenApiSchema())
    - src/openapi/verify-openapi.ts (the byte-equality drift gate that openapi:verify runs)
    - package.json (scripts: openapi:export, openapi:verify, openapi:check)
    - .planning/phases/19-contract-freeze/19-RESEARCH.md (Pitfall 4: version-only diff is not a breaking change; A1: do NOT bump package.json version)
  </read_first>
  <action>
    In `src/openapi/register-openapi.ts`, change the `info.version` literal from `"0.1.0"` to `"1.0.0"` (this is the single source of truth per FREEZE-01). Keep `openapi: "3.0.3"` and `title: "server-2"` unchanged. Do NOT change `package.json` `version` (it stays `0.1.0`, `private`; per RESEARCH A1 the contract version lives only in register-openapi.ts to keep one source of truth). Then regenerate the committed artifact by running `pnpm run openapi:export`, which rewrites `openapi/server-2.openapi.json` via `createOpenApiSchema()`. Commit the regenerated artifact. The published artifact path stays exactly `openapi/server-2.openapi.json` (FREEZE-02 — cross-app contract with `web`; do NOT introduce a second/duplicate path). Because this phase makes no route or response-shape change, the regenerated artifact must differ from the prior committed file ONLY in `info.version` (`0.1.0` → `1.0.0`); if any path/schema/param shape also changed, stop and investigate — that signals an unexpected drift, not a version-only bump.
  </action>
  <verify>
    <automated>grep -q '"version": "1.0.0"' openapi/server-2.openapi.json && pnpm run openapi:verify</automated>
  </verify>
  <acceptance_criteria>
    - `src/openapi/register-openapi.ts` contains `version: "1.0.0"` and no longer contains `version: "0.1.0"`; `openapi: "3.0.3"` is unchanged.
    - `package.json` `version` is still `"0.1.0"` (unchanged).
    - `openapi/server-2.openapi.json` `info.version` is `"1.0.0"`.
    - `pnpm run openapi:verify` exits 0 (regenerated-from-code byte-equals the committed artifact — no drift).
    - `git diff openapi/server-2.openapi.json` shows changes confined to the `info.version` string (no path/schema/param shape diff).
  </acceptance_criteria>
  <done>The contract version is 1.0.0 at its single source of truth and the committed artifact byte-matches the regenerated schema with only the version string changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the DB-free static frozen-contract test</name>
  <files>src/openapi/frozen-contract.test.ts</files>
  <behavior>
    - Test (c) FREEZE-01: parsing the committed artifact, `info.version` equals `"1.0.0"`.
    - Test (b) SEC: stringifying the whole artifact, the `/7656119\d{10}/u` pattern finds zero matches.
    - Test (a) FREEZE-03: for every PUBLIC list response (path starts with `/stats/`, GET, 200, application/json schema whose top-level `required`/`properties` exposes `items`), the schema's TOP-LEVEL `properties` does NOT include `page`, `pageSize`, or `total`. Must NOT recurse into nested item schemas (domain `stats.deaths.total` lives there) and must NOT inspect non-`/stats/` paths (`/operations/*` legitimately uses offset pagination). On the current correct artifact, the offenders list is empty and the test passes.
    - Negative-control sanity (per RESEARCH "not a vacuous assertion"): the scoped walk must be written so that an injected top-level `page` property on a `/stats/*` list response WOULD be reported — verifiable by reasoning over the code, not by committing a planted leak.
  </behavior>
  <read_first>
    - .planning/phases/19-contract-freeze/19-RESEARCH.md ("Code Examples → Static frozen-contract test" — the verified scoped implementation; "Pitfall 1" — why a naive scan fails; "Pattern 3" — pure JSON walk, fast suite placement)
    - src/test/integration/steamid-leak-guard.test.ts (STEAM64_PATTERN `/7656119\d{10}/u` and the eslint-disable header convention to mirror for no-magic-numbers/id-length)
    - src/openapi/export-openapi.ts and src/openapi/verify-openapi.ts (the `readFile(resolve("openapi/server-2.openapi.json"), "utf8")` path idiom to reuse)
    - package.json (the `test` script excludes `src/test/integration/**` and `src/**/tests/postgres.test.ts` — place the new file OUTSIDE those globs so it runs in the fast suite)
    - .claude/skills/solidstats-backend-ts-tests/SKILL.md and .claude/skills/solidstats-backend-ts-conventions/SKILL.md (Vitest AAA, isolation, DB-free placement; ESLint 10 `all` + Unicorn + Prettier compliance)
  </read_first>
  <action>
    Create `src/openapi/frozen-contract.test.ts` as a pure-JSON-walk Vitest suite that loads the committed artifact via `readFile(resolve("openapi/server-2.openapi.json"), "utf8")` + `JSON.parse` (same path idiom as export-openapi.ts). Implement the three assertions per the `<behavior>` block, following the verified scoped implementation in RESEARCH "Code Examples". Mirror the leak-guard's `STEAM64_PATTERN = /7656119\d{10}/u` (no `g` flag) for assertion (b). Scope assertion (a) STRICTLY to `/stats/`-prefixed paths, GET 200 application/json schemas that expose a top-level `items`, asserting only TOP-LEVEL `properties` exclude `page`/`pageSize`/`total` — do NOT recurse and do NOT touch `/operations/*` (RESEARCH Pitfall 1). Place the file at `src/openapi/` (NOT under `src/test/integration/` or `**/tests/postgres.test.ts`) so it stays in the fast `pnpm test` suite with no docker services. Add an eslint-disable header for `no-magic-numbers`/`id-length` mirroring the leak-guard file if the linter requires it. Do NOT boot the Fastify app or open any DB/queue/S3 connection.
  </action>
  <verify>
    <automated>pnpm test -- src/openapi/frozen-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/openapi/frozen-contract.test.ts` exists and runs under `pnpm test` (fast suite) with no docker services started.
    - All three tests pass against the current committed `1.0.0` artifact: version is `"1.0.0"`, zero `/7656119\d{10}/u` matches, empty offenders list for scoped pagination metadata.
    - The pagination assertion inspects only `/stats/`-prefixed GET 200 list responses and only their top-level `properties` (no recursion into nested item schemas; `/operations/*` not inspected).
    - The file does not import `buildApp`, `Pool`, or any DB/queue/S3 client.
    - `pnpm run lint` and `pnpm run format` pass for the new file (ESLint 10 `all` + Prettier).
  </acceptance_criteria>
  <done>A DB-free frozen-contract test in the fast suite locks version 1.0.0, zero full Steam64, and cursor-only pagination metadata on public /stats/* lists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → committed artifact | The OpenAPI document generated from Fastify swagger config crosses into the committed `openapi/server-2.openapi.json` that `web` consumes. |
| committed artifact → test assertion | The static test reads the committed file from disk (no untrusted runtime input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | openapi/server-2.openapi.json (published artifact) | mitigate | Static `/7656119\d{10}/u` assertion (b) over the whole artifact JSON catches any full Steam64 leaking via example/enum/description; verified zero matches today. Defense-in-depth on top of the existing real-pg `steamid-leak-guard.test.ts`. |
| T-19-02 | Tampering | openapi/server-2.openapi.json (stale baseline) | mitigate | Task 1 keeps the existing `openapi:verify` byte-equality drift gate green so the committed artifact can never silently diverge from code. |
| T-19-03 | Spoofing | frozen-contract test (vacuous assertion) | mitigate | Assertion (a) scoped per RESEARCH Pitfall 1 so it is not a no-op; the walk would report an injected top-level `page` on a `/stats/*` list (reasoned negative control). |
</threat_model>

<verification>
- `grep -q '"version": "1.0.0"' openapi/server-2.openapi.json` succeeds.
- `pnpm run openapi:verify` exits 0 (no drift between code and committed artifact).
- `pnpm run openapi:check` exits 0 (drift gate + `web` type generation from the unchanged path).
- `pnpm test -- src/openapi/frozen-contract.test.ts` passes (all three invariants) in the fast suite.
- `pnpm run lint && pnpm run format` pass for the new test file.
</verification>

<success_criteria>
- `info.version` is `1.0.0` at its single source of truth (`register-openapi.ts`); `package.json` version unchanged.
- The committed `openapi/server-2.openapi.json` is regenerated at `1.0.0` with only the version string changed (no shape diff), at the unchanged path `web` consumes.
- A DB-free frozen-contract test in the fast suite locks version `1.0.0`, zero full Steam64, and cursor-only pagination metadata on public `/stats/*` lists.
</success_criteria>

<output>
Create `.planning/phases/19-contract-freeze/19-01-SUMMARY.md` when done.
</output>
