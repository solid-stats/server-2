---
phase: 15-profile-parity-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/statistics/repository/parity-sql.ts
  - src/modules/statistics/parity-formulas.ts
  - src/modules/statistics/repository/legacy-export.ts
  - src/modules/statistics/export/legacy-public-export.ts
  - src/modules/statistics/repository/tests/parity-sql.test.ts
  - src/modules/statistics/parity-formulas.test.ts
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, PARITY-06]
must_haves:
  truths:
    - "CLI legacy export stdout JSON is byte-identical to today after SQL extraction"
    - "A shared parity-sql module exports the entity CTE plus one builder per surface, each producing scoped and unscoped SQL"
    - "The unscoped form of every builder is byte-identical to the original legacy-export SQL constant"
    - "Pure formulas (kdRatio, totalScore, weeklyScore, killsFromVehicleCoef) live in one module imported by both legacy export and future API mappers"
    - "Row mappers (mapWeapons/mapWeeks/mapRelationships/playerStats/squadStats) and final sort/format helpers (sortWeapons/sortWeeks/sortRelationships/weekExport) are EXPORTED so Plans 02/03 can reuse them byte-identically"
  artifacts:
    - path: "src/modules/statistics/repository/parity-sql.ts"
      provides: "PLAYER_ENTITY_CTE + per-surface SQL builders (scoped/unscoped) returning { sql, values }"
      contains: "player_entities as"
    - path: "src/modules/statistics/parity-formulas.ts"
      provides: "kdRatio, totalScore, weeklyScore, killsFromVehicleCoef pure functions"
      exports: ["kdRatio", "totalScore", "weeklyScore", "killsFromVehicleCoef"]
    - path: "src/modules/statistics/repository/legacy-export.ts"
      provides: "EXPORTED mappers mapWeapons/mapWeeks/mapRelationships/playerStats/squadStats (export keyword only, signatures unchanged)"
      exports: ["mapWeapons", "mapWeeks", "mapRelationships", "playerStats", "squadStats"]
    - path: "src/modules/statistics/export/legacy-public-export.ts"
      provides: "EXPORTED final sort/format helpers sortWeapons/sortWeeks/sortRelationships/weekExport (export keyword only)"
      exports: ["sortWeapons", "sortWeeks", "sortRelationships", "weekExport"]
    - path: "src/modules/statistics/repository/tests/parity-sql.test.ts"
      provides: "assertion that each unscoped builder equals the legacy SQL constant"
  key_links:
    - from: "src/modules/statistics/repository/legacy-export.ts"
      to: "src/modules/statistics/repository/parity-sql.ts"
      via: "imports unscoped builders"
      pattern: "from \"./parity-sql"
    - from: "src/modules/statistics/export/legacy-public-export.ts"
      to: "src/modules/statistics/parity-formulas.ts"
      via: "re-exports / imports pure formulas"
      pattern: "parity-formulas"
---

<objective>
Извлечь весь parity-SQL (общий `PLAYER_ENTITY_CTE` + пять запросов) из `legacy-export.ts` в новый
разделяемый модуль `parity-sql.ts`, который умеет отдавать как **unscoped**-форму (для CLI-экспорта,
byte-identical), так и **scoped**-форму с параметризованным per-entity предикатом (для будущих
API-чтений). Параллельно вынести чистые формулы (`kdRatio`/`totalScore`/`weeklyScore`/
`killsFromVehicleCoef`) в модуль `parity-formulas.ts`, импортируемый обоими потребителями. И, наконец,
сделать `export` пяти row-мапперов в `legacy-export.ts` и четырёх финальных sort/format-хелперов в
`legacy-public-export.ts`, чтобы Plans 02/03 могли импортировать их и оставаться byte-identical
(БЕЗ реимплементации). Только добавление `export` — поведение и сигнатуры не меняются.

Purpose: единый источник истины для CLI-экспорта и новых per-entity API-роутов (Plans 02/03). Без
этой фазы новые роуты дублировали бы SQL, формулы, мапперы и сортировки и неизбежно дрейфовали бы от
byte-identical-контракта.
Output: `parity-sql.ts`, `parity-formulas.ts`, отрефакторенные и частично-реэкспортированные
`legacy-export.ts` / `legacy-public-export.ts`, новые assertion-тесты. Никаких новых роутов — это фундамент.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-profile-parity-stats/15-CONTEXT.md
@.planning/phases/15-profile-parity-stats/15-RESEARCH.md

<interfaces>
<!-- Извлечено из кодовой базы. Исполнитель использует напрямую — без повторного исследования. -->

Источник извлечения — src/modules/statistics/repository/legacy-export.ts:
- `PLAYER_ENTITY_CTE` (строки 159-205): `player_entities as ( select distinct on (counter.parser_result_id, counter.observed_player_ref) ... coalesce(steam_player.id::text, nickname_player.id::text, display_player.id::text, ...) as player_id, ... )`. ВАЖНО: `player_id` — TEXT coalesce, НЕ uuid.
- `PLAYER_STATS_SQL` (207-258): `with ${PLAYER_ENTITY_CTE}, counter_totals as (...), last_games as (...), latest_squads as (...) select player.id, ... order by kills desc, player.display_name, player.id`. Джойнит `canonical_players player` напрямую (есть `player.id::uuid`). `counter_totals` CTE (209-213) считает `kills_from_vehicle` и `vehicle_kills` — vehicle-счётчики живут ТОЛЬКО здесь, в этом запросе.
- `SQUAD_STATS_SQL` (260-297): `select squad.id, squad.name, ... players ... from squads squad ... order by kills desc, squad.name, squad.id`.
- `RELATIONSHIPS_SQL` (352-397): `with ${PLAYER_ENTITY_CTE}, kill_events as (...), resolved as (...), pairs as (...) select source_player_id, ... where source_player_id is not null and target_player_id is not null group by ... order by source_player_name, relationship_type, count desc, target_player_name`.
- `WEAPONS_SQL` (399-416): `with ${PLAYER_ENTITY_CTE} select entity.player_id, entity.player_name, case when event.event_type = 'destroyed_vehicle' then 'vehicles' else 'firearms' end as weapon_group, ... group by player_id, player_name, weapon_group, weapon_name order by player_name, weapon_group, kills desc, weapon_name`.
- `WEEKS_SQL` (418-442): `with ${PLAYER_ENTITY_CTE} select entity.player_id, ... group by player_id, player_name, week, start_date, end_date order by player_name, week desc`.
- Row-мапперы — все приватны сегодня, Task 4 делает им `export` (НЕ перемещать, НЕ менять сигнатуры): `playerStats` (444), `squadStats` (460), `mapRelationships` (486), `mapWeapons` (521), `mapWeeks` (553). `playerStats(row)` уже читает `kills_from_vehicle`→`killsFromVehicle` (450) и `vehicle_kills`→`vehicleKills` (456) из строки PLAYER_STATS.

Формулы — src/modules/statistics/export/legacy-public-export.ts:
- `kdRatio(kills, deathsTotal)` (260-265): deathsTotal===0 → round(kills); иначе round(kills/deathsTotal).
- `killsFromVehicleCoef(killsFromVehicle, kills)` (267-275): kills===0 → 0; иначе round(killsFromVehicle/kills).
- `totalScore(kills, teamkills)` (277-279): round(kills - teamkills).
- `weeklyScore(kills, teamkills, totalPlayedGames)` (281-290): games===0 → 0; иначе round((kills-teamkills)/games).
- `round` (510-513): private — `Math.round(value*100)/100`. При выносе формул `round` тоже переносится (или дублируется приватно в parity-formulas.ts).
- Финальные sort/format-хелперы — приватны сегодня, Task 4 делает им `export` (НЕ менять): `weekExport` (364 → `LegacyWeekExport`, ВКЛЮЧАЯ `totalPlayedGames` поле, см. LegacyWeekExport интерфейс 194-207), `sortRelationships` (431), `sortWeapons` (442), `sortWeeks` (462). `relationshipsForPlayer` (475) НЕ нужен Plans 02/03 (они используют `mapRelationships(rows)[0]`, который уже даёт 4 списка) — оставить приватным.

Byte-identical гард — src/modules/statistics/repository/tests/legacy-export.test.ts (ScriptedLegacyExportPool, строки 288-313): роутит запрос по ПОДСТРОКАМ:
- `startsWith("select current_database()")`
- `includes("counter_totals")` → PLAYER_STATS
- `startsWith("select\n  squad.id")` → SQUAD_STATS
- `startsWith("select\n  rotation.id")` → ROTATION_STATS
- `includes("kill_events")` → RELATIONSHIPS
- `includes("case when event.event_type")` → WEAPONS
- else → WEEKS
Эти подстроки/префиксы НЕЛЬЗЯ ломать при параметризации.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Вынести чистые формулы в parity-formulas.ts</name>
  <read_first>
    - src/modules/statistics/export/legacy-public-export.ts (kdRatio/killsFromVehicleCoef/totalScore/weeklyScore/round — строки 260-290, 510-513; и все их call-sites в playerExport/weekExport/squadExport)
    - src/modules/statistics/export/tests/legacy-public-export.test.ts (какие формульные кейсы покрыты — чтобы не сломать)
  </read_first>
  <behavior>
    - kdRatio(0,0) === 0; kdRatio(5,0) === 5; kdRatio(3,2) === 1.5 (round до 2 знаков)
    - totalScore(10,3) === 7; totalScore(2,5) === -3
    - weeklyScore(10,2,4) === 2; weeklyScore(1,0,0) === 0
    - killsFromVehicleCoef(0,0) === 0; killsFromVehicleCoef(3,10) === 0.3; killsFromVehicleCoef(5,0) === 0
  </behavior>
  <action>
    Создать `src/modules/statistics/parity-formulas.ts`, экспортирующий `kdRatio`, `killsFromVehicleCoef`,
    `totalScore`, `weeklyScore` с ровно той же семантикой round (`Math.round(value * 100) / 100`) — перенести
    приватный `round` сюда. В `legacy-public-export.ts` УДАЛИТЬ локальные определения этих четырёх функций и
    `round` (если он больше нигде не используется — проверить `perGame`, который тоже зовёт `round`: оставить
    `round` локально ИЛИ импортировать из parity-formulas; решение Claude's Discretion, но `perGame` должен
    остаться byte-identical). Импортировать формулы из `./parity-formulas.js` (ESM `.js`-суффикс, NodeNext) и
    реэкспортировать те, что уже были экспортированы (`kdRatio`, `killsFromVehicleCoef`, `totalScore`,
    `weeklyScore`), чтобы существующие импортёры export-модуля не сломались. Это допустимая доменная утилита
    (Q4 CONTEXT: чистые функции — не требуют service-контракта). Создать `src/modules/statistics/parity-formulas.test.ts`
    с unit-кейсами выше.
  </action>
  <verify>
    <automated>pnpm test src/modules/statistics/parity-formulas.test.ts src/modules/statistics/export/tests/legacy-public-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `parity-formulas.ts` экспортирует все 4 формулы; `parity-formulas.test.ts` зелёный
    - `legacy-public-export.test.ts` остаётся зелёным БЕЗ изменений (формулы дают идентичный результат)
    - `grep -n "parity-formulas" src/modules/statistics/export/legacy-public-export.ts` находит импорт
  </acceptance_criteria>
  <done>Формулы живут в одном модуле, импортируются export-модулем, все формульные тесты зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Извлечь parity-sql.ts с scoped/unscoped builder'ами</name>
  <read_first>
    - src/modules/statistics/repository/legacy-export.ts (PLAYER_ENTITY_CTE + 5 SQL-констант + loadPlayerStats/loadSquadStats/loadRelationships/loadWeapons/loadWeeks — строки 128-156, 159-442)
    - src/modules/statistics/repository/tests/legacy-export.test.ts:288-317 (ScriptedLegacyExportPool — load-bearing подстроки)
    - 15-RESEARCH.md § Pattern 1 + Pitfall 1, 2 (TEXT player_id; не оборачивать внешним SELECT; сохранить опорные подстроки)
  </read_first>
  <behavior>
    - weaponsSql() (unscoped) .sql === оригинальная WEAPONS_SQL-строка (byte-identical), .values пустой
    - weaponsSql({ scopeId }) добавляет `and entity.player_id = $1::text` в существующий WHERE, .values === [scopeId]
    - аналогично weeksSql / relationshipsSql (scope по player_id::text / source_player_id::text)
    - playerStatsSql({ scopeId }) скоупит по `player.id = $1::uuid` (прямой canonical_players join)
    - squadStatsSql({ scopeId }) скоупит по `squad.id = $1::uuid`
    - каждый unscoped builder сохраняет load-bearing подстроки: PLAYER_STATS содержит `counter_totals`; SQUAD начинается с `select\n  squad.id`; RELATIONSHIPS содержит `kill_events`; WEAPONS содержит `case when event.event_type`; WEEKS — fallback
  </behavior>
  <action>
    Создать `src/modules/statistics/repository/parity-sql.ts`. Экспортировать `PLAYER_ENTITY_CTE` (строка,
    идентичная сегодняшней) и пять builder-функций: `playerStatsSql`, `squadStatsSql`, `relationshipsSql`,
    `weaponsSql`, `weeksSql`. Каждый builder принимает опциональный `{ scopeId: string }` и возвращает
    `{ sql: string; values: string[] }`. Внутри строить SQL вставкой `${predicate}` в точку существующего
    `WHERE`/`HAVING` (НЕ оборачивать запрос внешним SELECT — Pitfall 2 в RESEARCH меняет план/порядок).
    Когда `scopeId` НЕ задан, `predicate` === пустая строка, и результирующий `.sql` ОБЯЗАН быть посимвольно
    равен сегодняшней константе (включая пробелы/переводы строк). Scope-предикаты:
    - `weaponsSql`/`weeksSql`: `and entity.player_id = $1::text`
    - `relationshipsSql`: фильтровать по `source_player_id = $1::text` (Pitfall 4: scoped relationship для одной
      сущности уже даёт все 4 списка) — добавить в финальный `where source_player_id is not null and ...`
    - `playerStatsSql`: `and player.id = $1::uuid` (или внутренний CTE по `player_id::text` — оба byte-identical;
      выбрать форму, при которой unscoped-строка не меняется)
    - `squadStatsSql`: `and squad.id = $1::uuid`
    Параметризация ТОЛЬКО через `$1` — никакой конкатенации id (V5/Tampering, RESEARCH Security Domain).
    Затем в `legacy-export.ts` заменить пять SQL-констант на вызовы unscoped-builder'ов
    (`const PLAYER_STATS_SQL = playerStatsSql().sql;` и т.д.) и импортировать `PLAYER_ENTITY_CTE` из parity-sql.
    `loadPlayerStats`/`loadWeeks`/... продолжают вызывать unscoped-форму. Мапперы и интерфейсы строк
    (`WeaponRow`, `WeekRow`, ...) НЕ трогать (их `export` — отдельная Task 4).
  </action>
  <verify>
    <automated>pnpm test src/modules/statistics/repository/tests/legacy-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `parity-sql.ts` экспортирует `PLAYER_ENTITY_CTE` + 5 builder'ов
    - `legacy-export.test.ts` зелёный БЕЗ изменений теста (ScriptedLegacyExportPool по-прежнему различает все запросы → load-bearing подстроки целы)
    - `grep -n "parity-sql" src/modules/statistics/repository/legacy-export.ts` находит импорт
    - В parity-sql.ts нет шаблонной конкатенации `scopeId` в строку SQL (только `$1` placeholder; `grep -v '^\s*//' | grep -c 'scopeId}' === 0` внутри backtick-SQL)
  </acceptance_criteria>
  <done>SQL живёт в parity-sql; legacy-export потребляет unscoped-форму; byte-identical гард зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assertion-тест unscoped builder == legacy SQL constant</name>
  <read_first>
    - src/modules/statistics/repository/parity-sql.ts (только что созданный)
    - 15-RESEARCH.md § Pitfall 1 (гард сверяет МАППИНГ, не текст SQL — поэтому нужен явный текстовый assert)
    - 15-VALIDATION.md § Wave 0 Requirements (опциональный assert-тест)
  </read_first>
  <behavior>
    - Для каждого из 5 surface: unscoped builder .sql проходит проверку «содержит load-bearing подстроку» (counter_totals / select\n  squad.id / kill_events / case when event.event_type / week desc)
    - scoped builder .sql содержит ровно один `$1` и непустой .values длиной 1
    - scoped builder .sql НЕ оборачивает unscoped запрос внешним `select ... from (` (проверка отсутствия обёртки)
  </behavior>
  <action>
    Создать `src/modules/statistics/repository/tests/parity-sql.test.ts`. Поскольку оригинальные SQL-константы
    больше не существуют отдельно (они теперь = unscoped builder), зафиксировать инвариант косвенно: (1) каждый
    unscoped `.sql` содержит свою load-bearing подстроку из ScriptedLegacyExportPool — это гарантирует, что
    byte-identical-роутинг legacy-export.test.ts не сломается; (2) scoped `.sql` отличается от unscoped ровно
    добавлением `$1`-предиката и `.values.length === 1`; (3) unscoped `.values` пустой. Если исполнитель сохранил
    исходные строковые константы как приватные snapshot'ы — добавить прямой `expect(weaponsSql().sql).toBe(SNAPSHOT)`;
    иначе достаточно подстрочных и структурных проверок (Pitfall 1: текстового snapshot-теста в репозитории нет).
  </action>
  <verify>
    <automated>pnpm test src/modules/statistics/repository/tests/parity-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Тест зелёный; покрывает все 5 builder'ов в scoped и unscoped форме
    - Падает, если кто-то изменит load-bearing подстроку или сломает $1-параметризацию (проверено мысленно: удалить подстроку → тест красный)
  </acceptance_criteria>
  <done>Явный гард на equivalence unscoped-формы; защищает от тихого дрейфа SQL.</done>
</task>

<task type="auto">
  <name>Task 4: Сделать export мапперов и финальных sort/format-хелперов (для reuse в Plans 02/03)</name>
  <read_first>
    - src/modules/statistics/repository/legacy-export.ts:444-602 (мапперы playerStats/squadStats/mapRelationships/mapWeapons/mapWeeks — сейчас приватны)
    - src/modules/statistics/export/legacy-public-export.ts:364-475 (weekExport/sortRelationships/sortWeapons/sortWeeks/relationshipsForPlayer — сейчас приватны)
    - src/modules/statistics/repository/tests/legacy-export.test.ts + src/modules/statistics/export/tests/legacy-public-export.test.ts (существующие byte-identical / формульные гарды — должны остаться зелёными без изменений)
  </read_first>
  <action>
    ДОБАВИТЬ ТОЛЬКО ключевое слово `export` к существующим определениям функций — НЕ менять тело, сигнатуры,
    порядок, имена или импорты этих функций (это сохраняет byte-identical CLI-вывод и зелёные guard-тесты):
    - В `src/modules/statistics/repository/legacy-export.ts`: `export function playerStats(...)`,
      `export function squadStats(...)`, `export function mapRelationships(...)`, `export function mapWeapons(...)`,
      `export function mapWeeks(...)`.
    - В `src/modules/statistics/export/legacy-public-export.ts`: `export function weekExport(...)`,
      `export function sortRelationships(...)`, `export function sortWeapons(...)`, `export function sortWeeks(...)`.
    `relationshipsForPlayer` НЕ экспортировать — Plans 02/03 его не используют (они берут `mapRelationships(rows)[0]`,
    который уже отдаёт 4 списка для одной сущности; Pitfall 4 RESEARCH). Никаких новых файлов; ничего не перемещать.
    Это альтернатива «relocate в shared-модуль» — выбран export-in-place как самый низкорисковый для byte-identical.
  </action>
  <verify>
    <automated>pnpm test src/modules/statistics/repository/tests/legacy-export.test.ts src/modules/statistics/export/tests/legacy-public-export.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function mapWeapons" src/modules/statistics/repository/legacy-export.ts` находит совпадение
    - `grep -n "export function mapWeeks" src/modules/statistics/repository/legacy-export.ts` находит совпадение
    - `grep -n "export function mapRelationships" src/modules/statistics/repository/legacy-export.ts` находит совпадение
    - `grep -n "export function playerStats" src/modules/statistics/repository/legacy-export.ts` находит совпадение
    - `grep -n "export function squadStats" src/modules/statistics/repository/legacy-export.ts` находит совпадение
    - `grep -n "export function sortWeapons" src/modules/statistics/export/legacy-public-export.ts` находит совпадение
    - `grep -n "export function sortWeeks" src/modules/statistics/export/legacy-public-export.ts` находит совпадение
    - `grep -n "export function sortRelationships" src/modules/statistics/export/legacy-public-export.ts` находит совпадение
    - `grep -n "export function weekExport" src/modules/statistics/export/legacy-public-export.ts` находит совпадение
    - `legacy-export.test.ts` + `legacy-public-export.test.ts` остаются зелёными БЕЗ изменений тестов (только добавлен `export`)
    - `pnpm run typecheck` зелёный
  </acceptance_criteria>
  <done>Пять мапперов и четыре финальных хелпера экспортированы; Plans 02/03 могут импортировать их и оставаться byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Этот план — рефактор внутреннего SQL/формул + добавление `export`; новых входных границ нет (роутов нет). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | scoped SQL builder (`$1` injection) | mitigate | Scope-предикат ТОЛЬКО как параметр `$1::text`/`$1::uuid`; никакой строковой конкатенации id (acceptance Task 2 проверяет отсутствие `scopeId}` внутри SQL) |
| T-15-02 | Tampering | byte-identical CLI export drift | mitigate | unscoped builder обязан давать SQL с сохранёнными load-bearing подстроками; Task 4 добавляет ТОЛЬКО `export` (тело не меняется); гарды `legacy-export.test.ts` + `legacy-public-export.test.ts` + `parity-sql.test.ts` зелёные |
| T-15-SC | Tampering | npm/pnpm installs | accept | Новых пакетов нет (RESEARCH § Package Legitimacy Audit: zero new deps); install-задач в плане нет |
</threat_model>

<verification>
- `pnpm test` (unit) зелёный целиком, включая `legacy-export.test.ts`, `legacy-public-export.test.ts`,
  `export-legacy-public-stats.test.ts`, новые `parity-sql.test.ts`, `parity-formulas.test.ts`.
- `pnpm run typecheck` зелёный (ESM `.js`-суффиксы в импортах, строгий TS).
- `git diff` показывает, что тела мапперов/хелперов в legacy-export.ts / legacy-public-export.ts не тронуты —
  изменены только определения SQL-констант (Task 2) и добавлено ключевое слово `export` (Task 4).
</verification>

<success_criteria>
- parity-sql.ts и parity-formulas.ts существуют и потребляются legacy-export / legacy-public-export.
- Пять мапперов и четыре финальных sort/format-хелпера экспортированы (export-in-place) для reuse в Plans 02/03.
- Все три byte-identical гарда + два новых теста зелёные.
- Нет новых роутов и нет новых рантайм-зависимостей.
</success_criteria>

<output>
Create `.planning/phases/15-profile-parity-stats/15-01-SUMMARY.md` when done
</output>
</content>
</invoke>
