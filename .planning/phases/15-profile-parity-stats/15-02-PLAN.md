---
phase: 15-profile-parity-stats
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/modules/public-stats/routes/schemas.ts
  - src/modules/public-stats/routes/models.ts
  - src/modules/public-stats/repository.ts
  - src/modules/public-stats/routes/empty-read-model.ts
  - src/modules/public-stats/routes/routes.ts
  - src/modules/public-stats/tests/postgres.test.ts
  - openapi/server-2.openapi.json
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05]
must_haves:
  truths:
    - "GET /stats/players/:id/weapons returns firearms+vehicles weapon lists matching legacy mapWeapons order"
    - "GET /stats/players/:id/vehicles returns counters (vehicleKills, killsFromVehicle, killsFromVehicleCoef) plus the vehicles weapon-group list"
    - "GET /stats/players/:id/relationships returns four lists (killed/killers/teamkilled/teamkillers) of { player:{id,displayName}, count }"
    - "GET /stats/players/:id/weekly returns week buckets matching legacy weekExport (kdRatio/score/coef/totalPlayedGames included)"
    - "Player profile stats object carries kdRatio, totalScore, totalPlayedGames"
    - "No full Steam64 (7656119\\d{10}) appears in any parity response body or error payload"
    - "Parity reads run per-entity-scoped (no full-corpus parser_events seq scan)"
  artifacts:
    - path: "src/modules/public-stats/routes/schemas.ts"
      provides: "PlayerWeaponsResponse, PlayerVehiclesResponse, PlayerRelationshipsResponse, PlayerWeeklyResponse + extended PlayerStatsResponse"
      contains: "PlayerWeaponsResponse"
    - path: "src/modules/public-stats/repository.ts"
      provides: "getPlayerWeapons/getPlayerVehicles/getPlayerRelationships/getPlayerWeekly + extended player stats mapping"
      contains: "getPlayerWeapons"
    - path: "src/modules/public-stats/routes/routes.ts"
      provides: "4 player sub-resource routes registered under registerPlayerRoutes"
      contains: "/stats/players/:id/weapons"
  key_links:
    - from: "src/modules/public-stats/repository.ts"
      to: "src/modules/statistics/repository/parity-sql.ts"
      via: "scoped builder import"
      pattern: "parity-sql"
    - from: "src/modules/public-stats/repository.ts"
      to: "src/modules/statistics/repository/legacy-export.ts EXPORTED mappers (exported by Plan 01 Task 4) + parity-formulas"
      via: "import mapWeapons/mapWeeks/mapRelationships/playerStats (exported by 15-01 Task 4) + kdRatio/totalScore"
      pattern: "mapWeapons|parity-formulas"
    - from: "src/modules/public-stats/repository.ts"
      to: "src/modules/statistics/export/legacy-public-export.ts EXPORTED sort/format helpers (exported by Plan 01 Task 4)"
      via: "import sortWeapons/sortWeeks/sortRelationships/weekExport"
      pattern: "sortWeapons|weekExport"
    - from: "src/modules/public-stats/routes/routes.ts"
      to: "PgPublicStatsReadModel parity methods"
      via: "options.readModel.getPlayerWeapons(...)"
      pattern: "getPlayer(Weapons|Vehicles|Relationships|Weekly)"
---

<objective>
Подключить четыре per-player parity sub-resource роута (`/stats/players/:id/weapons|vehicles|relationships|weekly`)
как bounded embedded arrays, и расширить `PlayerStatsResponse` полями `kdRatio`/`totalScore`/`totalPlayedGames`.
Все чтения — per-entity-scoped через scoped-builder'ы из Plan 01, маппинг — переиспользование существующих
JS-мапперов и формул (НЕ реимплементация — иначе дрейф от byte-identical).

Purpose: закрыть PARITY-01..05 на public API; web получает уже посчитанные числа и не пересчитывает.
Output: новые TypeBox-схемы, parity read-методы в `PgPublicStatsReadModel`, sub-resource роуты, integration-кейсы
+ Steam64 leak guard, перегенерированный OpenAPI-артефакт.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-profile-parity-stats/15-CONTEXT.md
@.planning/phases/15-profile-parity-stats/15-RESEARCH.md
@.planning/phases/15-profile-parity-stats/15-01-SUMMARY.md

<interfaces>
<!-- Извлечено из кодовой базы. Использовать напрямую. -->

Из Plan 01 (src/modules/statistics/repository/parity-sql.ts): builders `weaponsSql({scopeId})`,
`weeksSql({scopeId})`, `relationshipsSql({scopeId})`, `playerStatsSql({scopeId})` → `{ sql, values }`.

Мапперы (src/modules/statistics/repository/legacy-export.ts): `mapWeapons(rows): LegacyWeaponsInput[]`,
`mapWeeks(rows): LegacyWeeksInput[]`, `mapRelationships(rows): LegacyOtherPlayersInput[]`, `playerStats(row): LegacyPlayerStatsInput`.
ВСЕ ЧЕТЫРЕ УЖЕ ЭКСПОРТИРОВАНЫ Plan 01 Task 4 (export-in-place; сигнатуры не менялись) — импортировать напрямую,
НЕ реимплементировать. `mapWeapons`/`mapWeeks`/`mapRelationships` возвращают массив; для scoped-чтения одной
сущности берётся единственный элемент (Pitfall 4 RESEARCH).

Формулы (src/modules/statistics/parity-formulas.ts): `kdRatio`, `totalScore`, `weeklyScore`, `killsFromVehicleCoef`.
Финальная сортировка/формат — в export-модуле: `sortWeapons`/`sortWeeks`/`sortRelationships` + `weekExport`
(src/modules/statistics/export/legacy-public-export.ts). ВСЕ ЧЕТЫРЕ УЖЕ ЭКСПОРТИРОВАНЫ Plan 01 Task 4 —
импортировать и переиспользовать, не дублировать (Pitfall 3). `weekExport` → `LegacyWeekExport`, который
ВКЛЮЧАЕТ `totalPlayedGames` (см. WARNING 1 ниже / интерфейс LegacyWeekExport).

Существующий профильный роут-шаблон (src/modules/public-stats/routes/routes.ts:170-192):
`app.get<{ Params: UuidParametersType; Querystring: PlayerDetailQueryType }>("/stats/players/:id", { schema: { params: UuidParameters, querystring: PlayerDetailQuery, response: { 200: ..., 404: NotFoundResponse }, tags: ["public-stats"] } }, async (request, reply) => { const item = await options.readModel.getX(request.params.id, rotationFilters(request.query)); return item ?? reply.code(NOT_FOUND).send({ message: "player not found" }); })`.

Схемы (src/modules/public-stats/routes/schemas.ts): `PlayerStatsResponse` (58-66 — расширить),
`PlayerReferenceResponse` ({ id (uuid), displayName }), `UuidParameters`, `PlayerDetailQuery`, `NotFoundResponse`.
Стиль: `Type.Object`/`Type.Array`, camelCase, `Static<typeof X>` экспорт типа.

ReadModel-интерфейс (src/modules/public-stats/routes/models.ts:1-23 + типы payload'ов): добавить методы
`getPlayerWeapons(id)`, `getPlayerVehicles(id)`, `getPlayerRelationships(id)`, `getPlayerWeekly(id)`. ВСЕ методы
интерфейса должны быть реализованы и в `PgPublicStatsReadModel`, и в `createEmptyPublicStatsReadModel`.

Маскирование (src/modules/public-stats/routes/pagination/mask.ts): `maskSteamId`. Parity-поверхности Steam64
вообще НЕ несут (только `{ id, displayName }`), маскирование тут не нужно — нужно отсутствие поля.

Pitfall 2 (RESEARCH): `:id` приходит uuid; weapons/weeks/relationships scope по `entity.player_id = $1::text`
(UUID передаётся как строка). PLAYER_STATS scope по `player.id = $1::uuid`.

ВАЖНО (WARNING 2): vehicle-счётчики (`vehicleKills`, `killsFromVehicle`, `killsFromVehicleCoef`) берутся ИЗ строки
scoped `playerStatsSql` (там есть `counter_totals`-CTE с `kills_from_vehicle`/`vehicle_kills`, которые читает
`playerStats(row)` маппер). Существующий `playerSelectStats()` (repository.ts ~480-489) НЕ селектит эти колонки —
НЕ полагаться на него для vehicle-счётчиков.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Схемы + ReadModel-контракт + расширение PlayerStatsResponse</name>
  <read_first>
    - src/modules/public-stats/routes/schemas.ts (PlayerStatsResponse, PlayerReferenceResponse, paginated, Static-экспорты)
    - src/modules/public-stats/routes/models.ts (PublicStatsReadModel, PlayerStatsPayload, PlayerProfile)
    - src/modules/public-stats/routes/empty-read-model.ts (полный список методов — все надо дополнить)
    - src/modules/statistics/export/legacy-public-export.ts (LegacyWeekExport — ВКЛЮЧАЕТ totalPlayedGames; LegacyRelationshipInput, LegacyWeaponInput — форма полей для схем)
  </read_first>
  <behavior>
    - PlayerStatsResponse дополнительно содержит kdRatio:number, totalScore:number, totalPlayedGames:number
    - PlayerWeaponsResponse = { firearms: {name,kills}[], vehicles: {name,kills}[] }
    - PlayerVehiclesResponse = { vehicleKills, killsFromVehicle, killsFromVehicleCoef, vehicles: {name,kills}[] }
    - PlayerRelationshipsResponse = { killed, killers, teamkilled, teamkillers } each { player:{id,displayName}, count }[]
    - PlayerWeeklyResponse = { weeks: WeekBucket[] } where WeekBucket matches LegacyWeekExport (week, startDate, endDate, kills, deaths{total,byTeamkills}, teamkills, killsFromVehicle, vehicleKills, kdRatio, killsFromVehicleCoef, score, totalPlayedGames)
    - PublicStatsReadModel declares getPlayerWeapons/getPlayerVehicles/getPlayerRelationships/getPlayerWeekly returning Promise<T | null>
  </behavior>
  <action>
    В `schemas.ts` добавить TypeBox-схемы: `PlayerWeaponEntry` ({ name:String, kills:Number }), `PlayerWeaponsResponse`,
    `PlayerVehiclesResponse`, `PlayerRelationshipEntry` ({ player: PlayerReferenceResponse, count:Number }),
    `PlayerRelationshipsResponse` (четыре `Type.Array(PlayerRelationshipEntry)`: killed/killers/teamkilled/teamkillers),
    `PlayerWeekBucket` + `PlayerWeeklyResponse`. `PlayerWeekBucket` ДОЛЖЕН включать `totalPlayedGames` (Type.Number) —
    форма `weekExport`/`LegacyWeekExport` несёт это поле (WARNING 1: parity-of-shape с CLI-недельным бакетом).
    Расширить `PlayerStatsResponse` тремя полями `kdRatio`, `totalScore`, `totalPlayedGames` (Type.Number).
    Экспортировать соответствующие `Static<...>` типы. Relationship-таргеты — ТОЛЬКО `PlayerReferenceResponse`
    ({ id, displayName }), без steamId (SEC-01/02).
    В `models.ts` объявить доменные payload-типы (`PlayerWeaponsPayload`, `PlayerVehiclesPayload`,
    `PlayerRelationshipsPayload`, `PlayerWeeklyPayload`; недельный bucket с `totalPlayedGames`) и добавить 4 метода в
    `PublicStatsReadModel`; расширить `PlayerStatsPayload` полями kdRatio/totalScore/totalPlayedGames. В
    `empty-read-model.ts` реализовать 4 новых метода, возвращающих `Promise.resolve(null)` (профиль не найден → 404),
    и добавить новые поля в любой статичный stats-объект, если такой есть (getPlayer возвращает null — значит
    stats-расширение там не требуется).
  </action>
  <verify>
    <automated>pnpm run typecheck && pnpm test src/modules/public-stats</automated>
  </verify>
  <acceptance_criteria>
    - typecheck зелёный: все методы PublicStatsReadModel реализованы в обоих read-model'ах
    - `grep -n "PlayerWeaponsResponse\|PlayerVehiclesResponse\|PlayerRelationshipsResponse\|PlayerWeeklyResponse" src/modules/public-stats/routes/schemas.ts` находит все 4
    - `grep -n "totalPlayedGames" src/modules/public-stats/routes/schemas.ts` находит поле в PlayerWeekBucket И в PlayerStatsResponse
    - PlayerRelationshipEntry.player ссылается на PlayerReferenceResponse (нет steamId-поля)
  </acceptance_criteria>
  <done>Контракты (схемы + интерфейс) определены; реализации компилируются; downstream-задачи имеют контракт.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parity read-методы в PgPublicStatsReadModel (scoped + reuse мапперов)</name>
  <read_first>
    - src/modules/public-stats/repository.ts (getPlayer scope по players.id = $1; pool.query<Row>; mapPlayerProfile; playerSelectStats — НЕ селектит vehicle-колонки)
    - src/modules/statistics/repository/parity-sql.ts (scoped builders из Plan 01)
    - src/modules/statistics/repository/legacy-export.ts (mapWeapons/mapWeeks/mapRelationships/playerStats — ЭКСПОРТИРОВАНЫ Plan 01 Task 4; Row-интерфейсы WeaponRow/WeekRow/RelationshipRow/PlayerStatRow)
    - src/modules/statistics/export/legacy-public-export.ts (sortWeapons/sortWeeks/sortRelationships/weekExport — ЭКСПОРТИРОВАНЫ Plan 01 Task 4; финальный порядок)
    - 15-RESEARCH.md § Pitfall 2,3,4 (TEXT scope; порядок в JS; relationships для одной сущности)
  </read_first>
  <behavior>
    - getPlayerWeapons(uuid): scoped weaponsSql → mapWeapons(rows)[0] → sortWeapons; пустой игрок → { firearms:[], vehicles:[] }, существующий профиль; несуществующий профиль → null
    - getPlayerVehicles(uuid): vehicleKills/killsFromVehicle/killsFromVehicleCoef из scoped playerStatsSql row (через playerStats маппер) + vehicles-список из weapons
    - getPlayerRelationships(uuid): scoped relationshipsSql → mapRelationships(rows)[0] → 4 списка отсортированы как в export (sortRelationships); пусто → 4 пустых массива
    - getPlayerWeekly(uuid): scoped weeksSql → mapWeeks(rows)[0] → weeks через weekExport + sortWeeks, отсортированы week desc; каждый bucket несёт totalPlayedGames
    - getPlayer(...) теперь возвращает stats с kdRatio/totalScore/totalPlayedGames (PARITY-05)
    - все 4 метода возвращают null, если игрока (canonical_players.id) не существует
  </behavior>
  <action>
    В `repository.ts` импортировать scoped-builder'ы из `../../statistics/repository/parity-sql.js`, мапперы
    (`mapWeapons`/`mapWeeks`/`mapRelationships`/`playerStats`) из `../../statistics/repository/legacy-export.js`
    (ЭКСПОРТИРОВАНЫ Plan 01 Task 4) и сортировки/`weekExport` (`sortWeapons`/`sortWeeks`/`sortRelationships`/`weekExport`)
    из `../../statistics/export/legacy-public-export.js` (тоже ЭКСПОРТИРОВАНЫ Plan 01 Task 4) + формулы из
    `../../statistics/parity-formulas.js`. Реализовать `getPlayerWeapons`/`getPlayerVehicles`/`getPlayerRelationships`/`getPlayerWeekly`:
    - Сначала проверить существование игрока (или полагаться на пустой результат + отдельный existence-чек по
      `canonical_players.id = $1`). Если профиль не существует → вернуть null (роут отдаст 404).
    - Выполнить scoped-запрос (`pool.query<Row>(builder({scopeId:id}).sql, builder({scopeId:id}).values)`), прогнать
      строки через соответствующий маппер, взять единственный элемент массива (или пустой default), затем применить ту же
      финальную сортировку/формат, что и CLI (`sortWeapons`/`sortRelationships`/`sortWeeks` + `weekExport`). НЕ отдавать
      сырой `result.rows` (Pitfall 3). Маппить в payload-форму схем (camelCase). Relationship/weapon payload НЕ несёт Steam64.
    - Для `getPlayerVehicles`: counters берутся ИЗ строки scoped `playerStatsSql` (этот запрос содержит `counter_totals`
      с `kills_from_vehicle`/`vehicle_kills`; `playerStats(row)` маппер уже отдаёт `killsFromVehicle`/`vehicleKills`).
      `killsFromVehicleCoef` — через формулу `killsFromVehicleCoef(killsFromVehicle, kills)`. НЕ использовать
      `playerSelectStats()` для этих счётчиков (он их не селектит — WARNING 2). vehicles-список — `vehicles`-группа
      из weapons-маппинга.
    - Расширить существующий профильный stats-маппинг (`mapPlayerProfile`/`playerStats` потребитель): добавить kdRatio
      (`kdRatio(kills, deaths_total)`), totalScore (`totalScore(kills, teamkills)`), totalPlayedGames (из `replay_count`
      существующего профильного запроса). kills/teamkills/deaths_total/replay_count берутся из текущего профильного
      select (НЕ vehicle-колонки) — они там уже есть.
    Scope: weapons/weeks/relationships — `$1::text`; playerStats — `$1::uuid` (Pitfall 2). Только параметризованно.
  </action>
  <verify>
    <automated>pnpm run typecheck && pnpm test src/modules/public-stats</automated>
  </verify>
  <acceptance_criteria>
    - typecheck + unit зелёные
    - `grep -n "parity-sql" src/modules/public-stats/repository.ts` находит импорт scoped-builder'ов
    - `grep -n "mapWeapons\|mapWeeks\|mapRelationships\|playerStats" src/modules/public-stats/repository.ts` находит импорты экспортированных мапперов
    - getPlayerVehicles берёт vehicleKills/killsFromVehicle из playerStatsSql row, НЕ из playerSelectStats (ревью)
    - Новые методы используют `mapWeapons`/`mapWeeks`/`mapRelationships` (не своя группировка)
  </acceptance_criteria>
  <done>4 scoped parity-метода + расширенный stats-маппинг; переиспользуют CLI-мапперы и формулы.</done>
</task>

<task type="auto">
  <name>Task 3: Sub-resource роуты + integration-кейсы + Steam64 guard + OpenAPI</name>
  <read_first>
    - src/modules/public-stats/routes/routes.ts (registerPlayerRoutes — шаблон роута, NOT_FOUND, options.readModel)
    - src/modules/public-stats/tests/postgres.test.ts:1-80 (seed-харнесс: canonical_players, parser_events, player_stats, replays; truncate+seed)
    - 15-RESEARCH.md § Security Domain (regex 7656119\d{10}); 15-03 14-03 leak-guard паттерн
    - 15-VALIDATION.md § Per-Task Verification Map (PARITY-01..05 + SEC integration)
  </read_first>
  <action>
    В `registerPlayerRoutes` (routes.ts) добавить 4 роута по шаблону существующего `/stats/players/:id`:
    `GET /stats/players/:id/weapons`, `/vehicles`, `/relationships`, `/weekly`. Каждый: `params: UuidParameters`,
    `response: { 200: <SurfaceResponse>, 404: NotFoundResponse }`, `tags: ["public-stats"]`. Хендлер зовёт
    `options.readModel.getPlayerX(request.params.id)`, при null → `reply.code(NOT_FOUND).send({ message: "player not found" })`
    (фиксированная строка — id не эхоится, RESEARCH Security: echo cursor/id в ошибке). Эти роуты — bounded arrays,
    БЕЗ cursor/pagination query (CONTEXT: deferred).
    В `postgres.test.ts` добавить integration-кейсы (real pg, без моков БД — RESEARCH testing-map): для засеянного
    игрока проверить weapons/vehicles/relationships/weekly через `PgPublicStatsReadModel` (или `app.inject`, если
    харнесс поднимает app); сверить значения/порядок с ожидаемыми (kdRatio/score/coef/totalPlayedGames из формул).
    Добавить **leak-guard**: сериализовать тела всех 4 parity-ответов + 404-тело и проверить, что regex
    `/7656119\d{10}/` НЕ находит совпадений ни в одном (PARITY relationship/weapon targets = { id, displayName } only).
    Проверить PARITY-05: `getPlayer(...)` возвращает stats с kdRatio/totalScore/totalPlayedGames.
    После схем выполнить `pnpm run openapi:export` и закоммитить обновлённый `openapi/server-2.openapi.json`
    (Pitfall 5 — иначе `openapi:check` упадёт; изменения additive).
  </action>
  <verify>
    <automated>pnpm run test:integration && pnpm run openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - 4 роута зарегистрированы: `grep -c "/stats/players/:id/" src/modules/public-stats/routes/routes.ts` >= 4
    - integration-кейсы для weapons/vehicles/relationships/weekly зелёные на real pg
    - leak-guard: regex `7656119\d{10}` даёт 0 совпадений по всем parity-телам и 404-телу
    - `openapi/server-2.openapi.json` перегенерирован и закоммичен; `pnpm run openapi:check` зелёный
    - 404-тело — фиксированная строка `"player not found"`, без echo id
  </acceptance_criteria>
  <done>Player parity-роуты живут, покрыты integration + leak-guard; OpenAPI-артефакт актуален.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /stats/players/:id/* | Публичный анонимный вход; `:id` — недоверенный uuid-параметр |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-leak | Information Disclosure | weapons/relationships/weekly payload | mitigate | Parity-таргеты = `{ id, displayName }` only; Steam64 вообще не выбирается; integration regex-guard `7656119\d{10}` = 0 совпадений (Task 3) |
| T-15-sqli | Tampering | scoped per-entity SQL | mitigate | `:id` валидируется `UuidParameters` (format uuid); scope только `$1::text`/`$1::uuid`, без конкатенации |
| T-15-dos | Denial of Service | parity hot path | mitigate | Только per-entity scoped builder'ы (Plan 01); никогда не bulk legacy SQL в роуте |
| T-15-echo | Information Disclosure | 404 error body | mitigate | Фиксированная строка `"player not found"`; id не эхоится |
| T-15-SC | Tampering | npm/pnpm installs | accept | Новых пакетов нет (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `pnpm run test:integration` зелёный (новые parity-кейсы + leak-guard на real pg).
- `pnpm run openapi:check` зелёный; артефакт `openapi/server-2.openapi.json` закоммичен.
- `pnpm test` (unit) + `pnpm run typecheck` зелёные; byte-identical гарды Plan 01 не задеты.
</verification>

<success_criteria>
- PARITY-01..05 закрыты: 4 player sub-resource роута + kdRatio/totalScore/totalPlayedGames на профиле.
- Числа/порядок совпадают с CLI (переиспользование мапперов/формул, не реимплементация).
- Нулевая утечка Steam64; per-entity scoped чтения; OpenAPI актуален.
</success_criteria>

<output>
Create `.planning/phases/15-profile-parity-stats/15-02-SUMMARY.md` when done
</output>
</content>
</invoke>
