---
phase: 18-api-ergonomics-admin-winner-fix
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-04"]
files_modified:
  - src/modules/requests/routes/workflows/tests/index.test.ts
  - src/modules/requests/routes/workflow-applier.test.ts
  - src/test/integration/steamid-leak-guard.test.ts
autonomous: true
requirements: [HIST-04, API-02, API-03, API-04]
must_haves:
  truths:
    - "An integration test freezes that a non-admin/non-moderator caller is rejected (401/403) by the legacy_winner_fix workflow route"
    - "An integration test freezes that an approved stats_correction + legacy_winner_fix flips parser_results.raw_snapshot.side_facts.outcome to status:known + the given winner_side"
    - "A test freezes that commander_side_stats is recalculated downstream (unknown→known reflected)"
    - "A test freezes that a workflow-action audit row is written for the winner-fix"
    - "The Steam64 leak-guard sweep covers the winner-fix workflow response body AND the new /admin/rotations write-route bodies — zero /7656119\\d{10}/ matches"
  artifacts:
    - path: "src/modules/requests/routes/workflows/tests/index.test.ts"
      provides: "verify-and-freeze coverage of the role guard + winner-fix outcome mutation"
      contains: "legacy_winner_fix"
    - path: "src/test/integration/steamid-leak-guard.test.ts"
      provides: "leak-guard sweep extended to winner-fix + admin write-route bodies"
      contains: "expectNoSteam64"
  key_links:
    - from: "winner-fix workflow route (verify, do NOT rebuild)"
      to: "parser_results.raw_snapshot.side_facts.outcome + commander_side_stats recalc"
      via: "approved stats_correction + legacy_winner_fix integration assertion"
      pattern: "side_facts|legacy_winner_fix"
    - from: "steamid-leak-guard sweep"
      to: "/admin/rotations + winner-fix response bodies"
      via: "expectNoSteam64 over write-route responses"
      pattern: "expectNoSteam64"
---

<objective>
HIST-04: VERIFY AND FREEZE the existing `legacy_winner_fix` moderator workflow — do NOT rebuild
it (D-HIST-04 / ROADMAP success criterion 4). The route, applier, and downstream recalc already
exist; this plan adds/confirms integration coverage that freezes current behavior and extends
the Steam64 leak-guard to the winner-fix response and the new admin write-route bodies (closing
the security gate for API-02/API-03/API-04 surfaces added this phase).

Purpose: lock the verify-only winner-fix behavior and guarantee no full Steam64 leaks through
any new or workflow response body before the Phase 19 freeze.
Output: frozen integration tests for the winner-fix path + an extended leak-guard sweep.

Depends on 18-01/18-02 (their OpenAPI/route changes must be landed so the regenerated contract
+ leak sweep reflect the full phase surface) and 18-04 (admin routes must exist to sweep them).
NO endpoint changes unless a verification gap is found — if a real gap surfaces, STOP and surface
it as a finding rather than silently rebuilding the workflow.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-api-ergonomics-admin-winner-fix/18-CONTEXT.md
@.planning/phases/18-api-ergonomics-admin-winner-fix/18-PATTERNS.md

# Verify-and-freeze TARGETS (READ to assert behavior — do NOT modify these source files):
@src/modules/requests/routes/workflows/workflows.ts
@src/modules/requests/routes/workflow-applier.ts

# Test files this plan modifies:
@src/modules/requests/routes/workflows/tests/index.test.ts
@src/modules/requests/routes/workflow-applier.test.ts
@src/test/integration/steamid-leak-guard.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify-and-freeze the legacy_winner_fix workflow (role guard + outcome + recalc + audit)</name>
  <files>src/modules/requests/routes/workflows/tests/index.test.ts, src/modules/requests/routes/workflow-applier.test.ts</files>
  <read_first>
    - src/modules/requests/routes/workflows/workflows.ts (registerRequestWorkflowRoutes ~52-104 — moderatorOnly = requireAnyRole(options.auth, ["admin","moderator"]) ~56; the approved + workflowMatchesRequest gate ~80-87; createWorkflowAction call ~102; workflowMatchesRequest ~131-142 requiring stats_correction for legacy_winner_fix)
    - src/modules/requests/routes/workflow-applier.ts (applyLegacyWinnerFix ~94-122 — the jsonb_set on raw_snapshot side_facts.outcome status→"known" + winner_side; applyWorkflowAction ~37-74 calls recalculateCommanderSideStatsForParserResult per returned id)
    - src/modules/requests/routes/workflows/tests/index.test.ts (existing route test scaffolding: authenticated sessions, app.inject for the workflows endpoint, the moderator/admin session setup, utilities.ts helpers)
    - src/modules/requests/routes/workflow-applier.test.ts (existing applier test style for asserting the jsonb mutation + recalc invocation)
  </read_first>
  <behavior>
    - Test: POST /moderation/requests/:id/workflows with a session that has NEITHER admin NOR moderator → 401 (unauthenticated) or 403 (authenticated without role). At least one negative-authz case per the role guard.
    - Test: an approved stats_correction request + action legacy_winner_fix with { replayId, winnerSide } → parser_results.raw_snapshot.side_facts.outcome becomes status:"known" with winner_side = the given value (assert the jsonb mutation).
    - Test: commander_side_stats for that parser result is recalculated downstream (unknown→known reflected) — assert recalculateCommanderSideStatsForParserResult was invoked for the returned parser-result id (applier test) OR the recalculated counts changed (real-pg test).
    - Test: a workflow-action audit row is written (createWorkflowAction result / listWorkflowActions returns the action).
  </behavior>
  <action>
    Add or confirm integration coverage that FREEZES the current winner-fix behavior. First grep
    the existing tests for these four assertions; ONLY add the ones that are missing (do not
    duplicate). (1) Role guard: assert a caller with no admin/moderator role is rejected
    (401 unauth and/or 403 missing-role) by POST /moderation/requests/:id/workflows — mirror the
    moderator/admin session setup already in index.test.ts. (2) Outcome mutation: with an approved
    stats_correction request, POST action "legacy_winner_fix" payload { replayId, winnerSide } and
    assert parser_results.raw_snapshot.side_facts.outcome === { status: "known", winner_side:
    <value> } (the applier jsonb_set result — assert in the applier test or a real-pg integration
    test). (3) Downstream recalc: assert recalculateCommanderSideStatsForParserResult is called
    for each returned parser-result id (spy/stub in the applier test) or that commander_side_stats
    reflects unknown→known in a real-pg test. (4) Audit: assert a workflow-action row exists
    (createWorkflowAction return value or GET /moderation/requests/:id/workflows lists it). Do NOT
    modify workflows.ts or workflow-applier.ts. If a behavior the success criteria require is NOT
    actually implemented (a real gap), STOP and report it as a finding — do not rebuild.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/modules/requests/routes/workflows src/modules/requests/routes/workflow-applier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A negative-authz test exists proving a non-admin/non-moderator caller is rejected (401 and/or 403) by the winner-fix workflow route.
    - A test asserts raw_snapshot.side_facts.outcome becomes { status: "known", winner_side: <given> } after legacy_winner_fix on an approved stats_correction request.
    - A test asserts recalculateCommanderSideStatsForParserResult runs for the returned parser-result id (or commander_side_stats reflects the unknown→known change in real-pg).
    - A test asserts a workflow-action audit row is written.
    - `git diff --stat src/modules/requests/routes/workflows/workflows.ts src/modules/requests/routes/workflow-applier.ts` shows ZERO changes to those source files (verify-and-freeze, not rebuild).
    - `pnpm exec vitest run src/modules/requests/routes/workflows src/modules/requests/routes/workflow-applier.test.ts` is green.
  </acceptance_criteria>
  <done>The winner-fix role guard, outcome mutation, downstream recalc, and audit-row writing are all frozen by passing integration tests; the workflow source files are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Steam64 leak-guard to winner-fix + admin write-route bodies; full verify</name>
  <files>src/test/integration/steamid-leak-guard.test.ts</files>
  <read_first>
    - src/test/integration/steamid-leak-guard.test.ts (STEAM64_PATTERN ~20; expectNoSteam64 ~27-31; PUBLIC_LIST_ROUTES ~33-38; PUBLIC_DETAIL_ROUTES ~51-62; the GET-only public route sweep describe ~109-126; the real-pg sweeps ~181+ for the buildApp+injected-repo + authenticated-session pattern)
    - src/modules/admin/routes/rotations.ts (the new /admin/rotations routes from 18-04 — the response bodies to sweep)
    - src/modules/requests/routes/workflows/workflows.ts (the WorkflowResponse body shape to sweep)
  </read_first>
  <action>
    Extend the leak-guard sweep to the WRITE-route response bodies added/covered this phase. The
    existing sweeps are GET-only; add a new describe block (or extend a real-pg block) that, with
    an authenticated admin session (and a moderator session for the workflow), injects: (a) POST
    /admin/rotations (success body), PUT /admin/rotations/:id, and a DELETE (204 — assert
    expectNoSteam64 on response.payload, which may be empty), and (b) POST
    /moderation/requests/:id/workflows for an approved stats_correction + legacy_winner_fix
    (assert expectNoSteam64 on both response.json() when JSON and response.payload). Use the
    existing expectNoSteam64 helper and the established buildApp + injected-repo + cookie-session
    pattern from the real-pg sweep blocks. Add a clear comment noting these are write-route bodies
    (POST/PUT/DELETE) — distinct from the GET-only sweep lists. Do NOT weaken the existing
    STEAM64_PATTERN or the negative self-test. After wiring, run the full repo verify to confirm
    the whole phase surface (API-02 breakdown, API-03 side filter, API-04 admin routes,
    HIST-04 winner-fix) is green and leak-free.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/test/integration/steamid-leak-guard.test.ts && pnpm run verify</automated>
  </verify>
  <acceptance_criteria>
    - The leak-guard test now sweeps the winner-fix workflow response AND the /admin/rotations POST/PUT/DELETE bodies with expectNoSteam64 (grep shows the new write-route sweep cases referencing /admin/rotations and the workflow route).
    - The STEAM64_PATTERN (/7656119\d{10}/u) and the negative self-test are unchanged (not weakened).
    - `pnpm exec vitest run src/test/integration/steamid-leak-guard.test.ts` finds ZERO Steam64 matches across all swept bodies (existing GET sweeps + new write-route sweeps) and passes.
    - `pnpm run verify` is GREEN end-to-end (format, lint, typecheck, unit, integration, openapi:check, backup/boundary checks, coverage) — confirming the full phase surface ships clean.
    - The regenerated OpenAPI contract (from openapi:check inside verify) contains the bounty breakdown, the commander-sides side param, and the three /admin/rotations routes.
  </acceptance_criteria>
  <done>The leak-guard sweep covers every write-route body added this phase with zero Steam64 leaks, and `pnpm run verify` passes end-to-end across the whole phase surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → winner-fix workflow route | non-admin/non-moderator callers attempt a privileged state mutation; the role guard is the boundary |
| workflow applier → parser_results / commander_side_stats | a moderator-triggered jsonb mutation + downstream recalc cross into derived stats |
| API → web consumer | winner-fix + admin write-route response bodies must never carry a full Steam64 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16 | Elevation of Privilege | legacy_winner_fix workflow route | mitigate | requireAnyRole(options.auth, ["admin","moderator"]) (pre-existing); FROZEN by the negative-authz test proving non-admin/non-moderator → 401/403 |
| T-18-17 | Tampering | parser_results.raw_snapshot outcome mutation | mitigate | Gated by approved stats_correction + workflowMatchesRequest (pre-existing); frozen by the outcome-mutation + recalc integration tests so a regression is caught |
| T-18-18 | Repudiation | winner-fix action | mitigate | createWorkflowAction writes a workflow-action audit row (pre-existing); frozen by the audit-row test |
| T-18-19 | Information Disclosure | winner-fix + admin write-route bodies | mitigate | Leak-guard sweep extended to these write-route response bodies; expectNoSteam64 asserts zero /7656119\d{10}/ matches |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new runtime dependencies (test-only changes this plan); no install tasks — if any appear, halt and require the package-legitimacy gate |
</threat_model>

<verification>
- `pnpm exec vitest run src/modules/requests/routes/workflows ...workflow-applier.test.ts` — winner-fix behavior frozen (role guard, outcome, recalc, audit).
- `pnpm exec vitest run src/test/integration/steamid-leak-guard.test.ts` — write-route bodies leak-free.
- `pnpm run verify` — full phase surface green end-to-end.
</verification>

<success_criteria>
- The legacy_winner_fix workflow is verified and role-guarded (verify-and-freeze, not rebuilt) per HIST-04 / ROADMAP criterion 4; the workflow source files are unchanged.
- The route is present in the regenerated OpenAPI contract.
- No full Steam64 (/7656119\d{10}/) appears in any winner-fix or /admin/rotations write-route body.
- `pnpm run verify` is green across the whole Phase 18 surface (API-02, API-03, API-04, HIST-04).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 18-05)

- `src/modules/requests/routes/workflows/tests/index.test.ts` (modified) — added/confirmed verify-and-freeze tests for role guard + audit row.
- `src/modules/requests/routes/workflow-applier.test.ts` (modified) — added/confirmed outcome-mutation + downstream-recalc freeze tests.
- `src/test/integration/steamid-leak-guard.test.ts` (modified) — leak-guard sweep extended to winner-fix + /admin/rotations write-route bodies.
- No source/route/schema changes (verify-and-freeze); no new DB columns; no new migration.
</artifacts_produced>

<output>
Create `.planning/phases/18-api-ergonomics-admin-winner-fix/18-05-SUMMARY.md` when done
</output>
