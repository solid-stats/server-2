---
phase: 09-parser-counter-ingestion-and-aggregate-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/statistics/parser-artifact.ts
  - src/modules/statistics/service/service.ts
  - src/modules/statistics/repository/repository.ts
  - src/modules/statistics/parser-artifact.test.ts
  - src/modules/statistics/service/tests/aggregates.test.ts
autonomous: true
requirements:
  - STAT-10
  - STAT-11
  - STAT-12
  - STAT-13
must_haves:
  truths:
    - Parser artifact mapping emits compact counter evidence for every parser player.
    - Public aggregate death totals use parser compact death counters when present.
    - Kill rows continue to drive kills, teamkills, and relationship evidence.
    - Aggregation avoids double-counting deaths when counters and kill rows both exist.
  artifacts:
    - path: src/modules/statistics/parser-artifact.ts
      provides: Parser compact counter event mapping
      contains: player_counter
    - path: src/modules/statistics/service/service.ts
      provides: Counter-driven aggregate death semantics
      contains: applyCounterEvent
    - path: src/modules/statistics/repository/repository.ts
      provides: Stored player_counter event reconstruction
      contains: player_counter
  key_links:
    - from: src/modules/statistics/parser-artifact.ts
      to: src/modules/statistics/service/service.ts
      via: NormalizedParserEvent player_counter union member
      pattern: player_counter
---

<objective>
Implement compact parser counter preservation and aggregate death semantics.

Purpose: Phase 09 needs backend public stats to use parser replay-level counter evidence instead of deriving all deaths from attacker kill rows.
Output: TypeScript mapping/service/repository changes plus focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-parser-counter-ingestion-and-aggregate-semantics/09-CONTEXT.md
@.planning/phases/09-parser-counter-ingestion-and-aggregate-semantics/09-RESEARCH.md
@src/modules/statistics/parser-artifact.ts
@src/modules/statistics/service/service.ts
@src/modules/statistics/repository/repository.ts
@src/modules/statistics/parser-artifact.test.ts
@src/modules/statistics/service/tests/aggregates.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Map compact player counters</name>
  <files>src/modules/statistics/parser-artifact.ts, src/modules/statistics/parser-artifact.test.ts</files>
  <read_first>src/modules/statistics/parser-artifact.ts, src/modules/statistics/parser-artifact.test.ts</read_first>
  <action>Extend PlayerRow with `td`, `tk`, `su`, `nkd`, and `ud`. Add a `player_counter` NormalizedParserEvent variant emitted once per parser player. Payload must include `kills`, `teamkills`, `deaths_total`, `deaths_by_teamkills`, `suicides`, `null_killer_deaths`, `unknown_deaths`, `vehicle_kills`, `kills_from_vehicle`, and the existing player summary. Keep kill events unchanged.</action>
  <verify>pnpm vitest run src/modules/statistics/parser-artifact.test.ts</verify>
  <acceptance_criteria>
    - parser-artifact.ts contains `eventType: "player_counter"`.
    - parser-artifact.test.ts asserts counter payload fields for `d`, `td`, `tk`, `su`, `nkd`, `ud`, `vk`, and `kfv`.
  </acceptance_criteria>
  <done>Mapper emits compact counter events without removing kill, destroyed vehicle, or diagnostic events.</done>
</task>

<task type="auto">
  <name>Task 2: Use counter deaths in aggregates</name>
  <files>src/modules/statistics/service/service.ts, src/modules/statistics/service/tests/aggregates.test.ts</files>
  <read_first>src/modules/statistics/service/service.ts, src/modules/statistics/service/tests/aggregates.test.ts</read_first>
  <action>Update calculatePlayerAndSquadAggregates so `player_counter` events add public death totals from `deaths_total` and `deaths_by_teamkills`. Kill-row victim deaths must be fallback-only for entities without a counter event in the same replay. Kills and teamkills remain driven by kill rows.</action>
  <verify>pnpm vitest run src/modules/statistics/service/tests/aggregates.test.ts</verify>
  <acceptance_criteria>
    - service.ts contains `applyCounterEvent`.
    - aggregate tests prove counter deaths override kill-derived deaths and fallback still works without counter events.
  </acceptance_criteria>
  <done>Aggregates use compact death counters without double counting kill rows.</done>
</task>

<task type="auto">
  <name>Task 3: Reconstruct stored counter events</name>
  <files>src/modules/statistics/repository/repository.ts</files>
  <read_first>src/modules/statistics/repository/repository.ts</read_first>
  <action>Teach parserEventFromRow to return `player_counter` rows from stored parser_events with non-null observed_player_ref. Leave unsupported event rows ignored.</action>
  <verify>pnpm run typecheck</verify>
  <acceptance_criteria>
    - repository.ts contains a `row.event_type === "player_counter"` branch.
  </acceptance_criteria>
  <done>Persisted player_counter rows participate in recalculation.</done>
</task>
</tasks>

<verification>
- [ ] pnpm vitest run src/modules/statistics/parser-artifact.test.ts src/modules/statistics/service/tests/aggregates.test.ts
- [ ] pnpm run typecheck
</verification>

<success_criteria>
- Compact counters are preserved as normalized events.
- Aggregate death stats use counters when present.
- Kill rows remain preserved for kills, teamkills, and relationships.
- Tests cover required death/counter semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/09-parser-counter-ingestion-and-aggregate-semantics/09-01-SUMMARY.md`.
</output>
