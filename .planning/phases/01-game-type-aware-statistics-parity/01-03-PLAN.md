---
phase: 01-game-type-aware-statistics-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/modules/statistics/game-type/is-show.ts
  - src/modules/statistics/game-type/tests/is-show.test.ts
  - src/modules/statistics/repository/repository.ts
  - src/modules/statistics/service/full-run-recalculation.ts
  - src/modules/statistics/repository/tests/postgres.test.ts
autonomous: true
requirements:
  - PARITY-F8-AGGREGATE-PERTYPE
  - PARITY-F8-AGGREGATE-ALLTIME
  - PARITY-F8-ISSHOW
  - PARITY-F8-AUDIT-PRESERVE

must_haves:
  truths:
    - "Recalc groups aggregates by replays.game_type, writing per-rotation rows for sg and all-time (rotation_id NULL) rows for sg, mace, and sm"
    - "Excluded (game_type NULL) replays contribute to no aggregate bucket"
    - "Each player_stats row carries is_show computed at recalc from the player's totalPlayedGames vs the legacy threshold (>=20, or 15% of scope games when scope games < 125) per scope"
    - "The single-replay *ForParserResult audit path and the ops:stats:recalculate report shape are preserved (game_type added, not rewritten)"
    - "moderation audit patches are preserved across the recompute"
  artifacts:
    - path: "src/modules/statistics/game-type/is-show.ts"
      provides: "Pure computeIsShow(totalPlayedGames, scopeGameCount) implementing filterPlayersByTotalPlayedGames"
      contains: "computeIsShow"
      min_lines: 15
  key_links:
    - from: "src/modules/statistics/service/full-run-recalculation.ts"
      to: "src/modules/statistics/repository/full-run.ts"
      via: "full-run service calls classifyGameTypesForCurrentReplays then drives per-type + all-time recalc"
      pattern: "classifyGameTypesForCurrentReplays"
    - from: "src/modules/statistics/repository/repository.ts"
      to: "player_stats.is_show"
      via: "replaceAggregateRows writes game_type + is_show; replaceAggregateRows handles rotation_id NULL all-time bucket"
      pattern: "is_show|game_type"
---

<objective>
Make recalc game-type-aware end-to-end: group current-replay inputs by `replays.game_type`, compute and
persist per-type aggregates for sg per-rotation AND a new all-time (rotation_id NULL) pass for sg, mace, and
sm (D1), skip excluded (NULL game_type) replays entirely (D2), and compute+persist `is_show` on every
player_stats row from the legacy `filterPlayersByTotalPlayedGames` threshold against the *scope's* game count
(D3). Preserve the single-replay `*ForParserResult` audit path and the `ops:stats:recalculate` report shape
(add game_type, do not rewrite), and preserve moderation audit patches across the one-time recompute.

Purpose: This is the core parity work — the new side now computes the same per-type + all-time buckets the
legacy `sg_stats` produces, with the same is_show split, so the export (01-04) and parity test (01-05) can
match legacy.
Output: A pure `computeIsShow`, game_type-aware load/group/replace in the repository, an all-time aggregation
pass, and an updated full-run service that classifies before grouping — all behind preserved report/audit shapes.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-game-type-aware-statistics-parity/CONTEXT.md
@.planning/phases/01-game-type-aware-statistics-parity/RESEARCH.md
@src/modules/statistics/repository/repository.ts
@src/modules/statistics/service/full-run-recalculation.ts
@src/modules/statistics/service/service.ts
@src/modules/statistics/service/recalculation.ts
@src/modules/statistics/game-type/classify-game-type.ts
@src/operations/recalculate-statistics.ts

Project skills (comply, cite): solidstats-server-ts-conventions (repository data-access only, set-based
writes, no business logic in SQL beyond grouping), solidstats-server-ts-tests, solidstats-shared-backend-ts-standards.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure computeIsShow (filterPlayersByTotalPlayedGames port)</name>
  <files>src/modules/statistics/game-type/is-show.ts, src/modules/statistics/game-type/tests/is-show.test.ts</files>
  <behavior>
    - computeIsShow(totalPlayedGames, scopeGameCount): with scopeGameCount >= 125, minGames = 20 →
      totalPlayedGames >= 20 ? true : false.
    - With scopeGameCount < 125, minGames = (15 * scopeGameCount) / 100 (15%, legacy formula, NOT rounded —
      replicate legacy arithmetic exactly: `(15*gamesCount)/100`); condition `totalPlayedGames >= minGames`.
    - Boundary: scopeGameCount = 125 uses the 20 threshold (>=125 path); scopeGameCount = 124 uses 15%.
    - Edge: scopeGameCount = 0 → minGames 0 → every player with >=0 games is shown (matches legacy: 15%*0=0).
    - Returns true/false (a flag), never removes — D3 / RESEARCH B.6.
  </behavior>
  <action>
    Create `src/modules/statistics/game-type/is-show.ts` exporting pure
    `computeIsShow(totalPlayedGames: number, scopeGameCount: number): boolean`. Port the legacy
    `filterPlayersByTotalPlayedGames` arithmetic verbatim: minGamesCount starts 20, and when
    `gamesCount < 125` it becomes `(15 * gamesCount) / 100`. Do not round (legacy does not). This is players-only
    (D3 — squads do NOT get is_show unless 01-04 finds the legacy export filters squads too; default no).
  </action>
  <verify>
    <automated>pnpm vitest run src/modules/statistics/game-type/tests/is-show.test.ts</automated>
  </verify>
  <done>All boundary/edge cases above pass; function is pure and dependency-free.</done>
</task>

<task type="auto">
  <name>Task 2: game_type-aware load/group/replace + all-time pass + is_show persist</name>
  <files>src/modules/statistics/repository/repository.ts</files>
  <action>
    Make the repository data-access game-type-aware while preserving the single-replay audit path's shape
    (CONTEXT must-verify gate (b)).

    1. `loadAggregateReplayInputs(client, rotationId)`: today selects current parser_results
       `where r.rotation_id = $1 and pr.status='current'`. Generalize it to also load each replay's
       `r.game_type`, and add an all-time variant (or a parameterized scope) that selects current results
       filtered by game_type irrespective of rotation. Keep the existing per-rotation signature working for
       the single-replay path. A clean shape: add `loadAggregateReplayInputsForScope(client, scope)` where
       scope is either `{ kind:'rotation', rotationId, gameType }` or `{ kind:'allTime', gameType }`, and
       have the existing per-rotation function delegate so the audit path is unchanged. Excluded replays
       (`game_type is null`) are never selected into any scope (D2).

    2. `calculatePlayerAndSquadAggregates` (service.ts) stays type-agnostic — it just aggregates whatever
       inputs it gets. Grouping by type happens by *which inputs you load per scope*. Do NOT push game_type
       into the pure aggregator; keep the grouping in the repository/service orchestration (conventions:
       repository selects the scoped set, pure calc aggregates it).

    3. is_show: when writing player_stats rows for a scope, compute is_show per player =
       `computeIsShow(playerRow.stats.replay_count, scopeGameCount)` where scopeGameCount is the number of
       distinct replays in that scope (per-rotation games for sg rotations; per-type total games for
       all-time — RESEARCH C "threshold scope"). Resolve scopeGameCount from the loaded inputs
       (distinct replayIds in scope) so it is computed once at recalc, NOT at export (D3 rejects export-time
       is_show).

    4. `replaceAggregateRows(client, scope, aggregates)`: change the delete+insert to be scope+game_type
       keyed. For a rotation scope: `delete from player_stats where rotation_id = $1 and game_type = $2`
       then insert rows with `(rotation_id, player_id, stats, game_type, is_show)`. For an all-time scope:
       `delete ... where rotation_id is null and game_type = $1` then insert with `rotation_id = NULL`.
       Mirror for squad_stats (game_type, no is_show), bounty_points (game_type), and commander rows
       (replaceCommanderRows → add game_type + all-time variant). Use the set-based multi-row unnest insert
       pattern already in the file, not single-row loops where a multi-row insert is natural; if you keep
       the existing per-row loop style, that is acceptable but add game_type/is_show to the column list and
       the delete predicate.

    IMPORTANT — moderation audit preservation (project constraint / RESEARCH C): the recompute rewrites
    derived aggregate rows but MUST NOT touch `audit_patches` / `moderation_actions`. Confirm the delete
    predicates only touch player_stats/squad_stats/bounty_points/commander_side_stats (never audit tables),
    and note this in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm vitest run src/modules/statistics/repository/tests/postgres.test.ts -t "game_type|all-time|is_show"</automated>
  </verify>
  <done>
    Per-rotation sg rows AND all-time (rotation_id NULL) rows for sg/mace/sm are written with game_type;
    excluded replays contribute nothing; player_stats rows carry is_show from the scope game count; delete
    predicates never touch audit tables; the single-replay *ForParserResult path returns its existing shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: full-run service drives classify → per-type per-rotation → all-time, report shape preserved</name>
  <files>src/modules/statistics/service/full-run-recalculation.ts, src/modules/statistics/repository/tests/postgres.test.ts</files>
  <action>
    Update `FullRunRecalculationService.recalculateAllCurrentParserResults` so it:
    1. Calls `assignRotationsForCurrentReplays()` (existing) AND the new
       `classifyGameTypesForCurrentReplays()` (01-02) before grouping, so every current replay has both
       rotation_id and game_type set.
    2. Drives recalc for each scope: for sg → per-rotation rows (existing rotation loop, now passing
       gameType='sg') PLUS an all-time sg pass (rotation_id NULL); for mace and sm → all-time passes only
       (D1 — no per-rotation). The existing `recalculate*ForRotation` repo methods gain a scope/game_type
       parameter (or sibling all-time methods are added on the FullRunRecalculationRepository interface in
       service/full-run-recalculation.ts — extend the interface accordingly). Keep the chronological
       rotation ordering invariant (orderedRotationGroups) for sg per-rotation since bounty reads the
       previous rotation's freshly written player_stats.
    3. Preserve the `FullRunRecalculationReport` shape and the `ops:stats:recalculate` JSON output
       (CONTEXT must-verify gate (b)): keep `mode`, `reportVersion`, `summary`, `results`, `failures`,
       `lifecycle` exactly; per-type/all-time row counts roll up into the existing `aggregateRows`/
       `changedAggregateRows` totals rather than adding a new top-level shape. If a per-type breakdown is
       genuinely needed, add it as an OPTIONAL field that defaults to absent so existing consumers/tests are
       unaffected — do NOT remove or rename existing fields.

    Extend `repository/tests/postgres.test.ts` with a real-pg end-to-end: seed sg + mace + sm + excluded
    replays across rotations, run `recalculateAllCurrentParserResults`, and assert: sg has both per-rotation
    and rotation_id-NULL rows; mace/sm have only rotation_id-NULL rows; excluded replays produced no rows;
    is_show is set; and the report shape still matches the prior schema (mode/reportVersion/summary keys
    unchanged). Also assert the set-based full run equals the expected aggregates (extend the existing
    set-based-vs-perReplay equality style already in this file).
  </action>
  <verify>
    <automated>pnpm vitest run src/modules/statistics/repository/tests/postgres.test.ts -t "full run|all-time|recalculate"</automated>
  </verify>
  <done>
    A full run classifies + assigns, writes sg per-rotation + sg/mace/sm all-time rows with is_show, ignores
    excluded replays, and emits the same report shape (no field removed/renamed). The ops:stats:recalculate
    JSON contract is unchanged for existing keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalc → aggregate tables | DELETE+INSERT rewrites derived rows; must not touch moderation audit tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | recompute deletes | mitigate | Delete predicates scoped to the four aggregate tables + (rotation_id/game_type); audit_patches/moderation_actions never referenced — asserted + SUMMARY-noted |
| T-01-08 | Denial of Service | all-time pass cost | accept | All-time recompute is an offline ops job (ops:stats:recalculate), not a request path; one-time + on demand |
| T-01-09 | Repudiation | report shape drift | mitigate | Report keys preserved; per-type breakdown only as optional additive field; real-pg test pins shape |
</threat_model>

<verification>
- Per-type sg per-rotation + sg/mace/sm all-time rows written; excluded replays excluded.
- is_show computed at recalc per scope game count (124 vs 125 boundary covered).
- ops:stats:recalculate report shape unchanged for existing keys (test asserts).
- audit tables untouched by recompute (test/inspection + SUMMARY note).
- `pnpm verify` (where real-pg available) green.
</verification>

<success_criteria>
Recalc produces game-type-aware per-rotation (sg) and all-time (sg/mace/sm) aggregates with persisted
is_show, preserving the audit path, report shape, and moderation patches.
</success_criteria>

<output>
Create `.planning/phases/01-game-type-aware-statistics-parity/01-03-SUMMARY.md` when done. Note the audit-table
preservation confirmation and any report-shape additive field.
</output>
