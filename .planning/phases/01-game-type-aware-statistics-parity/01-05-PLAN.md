---
phase: 01-game-type-aware-statistics-parity
plan: 05
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/modules/statistics/repository/tests/postgres.test.ts
autonomous: true
requirements:
  - PARITY-F8-PARITY-PROOF

must_haves:
  truths:
    - "The real-pg comparison harness proves per-type aggregates (sg/mace/sm) match the legacy sg-replay-parser semantics for a representative seeded corpus"
    - "The sg all-time bucket and the sg per-rotation rows are both proven against legacy expectations"
    - "mace and sm produce only all-time buckets and exclude their filtered replays (mace<10, sm<2023)"
    - "The is_show split is proven: players below the per-scope threshold are flagged isShow=false in the export"
    - "The single-replay audit path + ops:stats:recalculate report shape remain intact under the extended harness"
  artifacts:
    - path: "src/modules/statistics/repository/tests/postgres.test.ts"
      provides: "Extended real-pg per-type + all-time + is_show parity comparison"
      contains: "game_type"
      min_lines: 1
  key_links:
    - from: "src/modules/statistics/repository/tests/postgres.test.ts"
      to: "src/modules/statistics/export/legacy-public-export.ts"
      via: "harness runs full recalc then asserts LegacyPublicStatsExportService output per type matches expected legacy semantics"
      pattern: "LegacyPublicStatsExport|loadExportData"
---

<objective>
Extend the existing real-pg comparison harness (`repository/tests/postgres.test.ts`) to prove byte-parity of
the new game-type-aware behavior end-to-end: seed a representative multi-type corpus (sg across rotations,
mace with both >=10 and <10 player replays, sm before and after 2023-01-01, an excludeReplays-linked replay,
an includeReplays name-forced replay), run the full recalc, then assert per-type + all-time aggregates and the
is_show split match the legacy sg-replay-parser semantics. Do NOT replace the existing harness — extend it
(byte-parity discipline, RESEARCH C).

Purpose: This is the proof obligation for F8 — without it the per-type work is unverified. It also re-asserts
that the single-replay audit path and the ops report shape survived (must-verify gate (b)).
Output: New real-pg test cases under the existing PgStatisticsRepository describe block proving per-type,
all-time, exclusion, and is_show parity.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/server-2/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-game-type-aware-statistics-parity/CONTEXT.md
@.planning/phases/01-game-type-aware-statistics-parity/RESEARCH.md
@src/modules/statistics/repository/tests/postgres.test.ts
@src/modules/statistics/export/legacy-public-export.ts
@src/modules/statistics/repository/legacy-export.ts

Project skills (comply, cite): solidstats-server-ts-tests (real-pg harness, AAA, deterministic),
solidstats-shared-testing-standards.
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed a representative multi-type corpus in the real-pg harness</name>
  <files>src/modules/statistics/repository/tests/postgres.test.ts</files>
  <action>
    Add seed helpers / extend the existing ones (seedParserResult, seedRotationPeriod, seedPlayer, etc.) to
    build a deterministic multi-type corpus within the existing truncate-per-test isolation:
    - sg replays spread across at least two rotation windows (so sg per-rotation AND sg all-time both have
      content), with mission names like `sg_alpha`.
    - mace replays: one with >=10 distinct players (→ kept, all-time mace), one with <10 (→ excluded).
    - sm replays: one dated 2022-12-31 (→ excluded), one dated 2023-02-01 (→ kept, all-time sm).
    - one replay whose source link matches an excludeReplays entry (→ excluded).
    - one prefix-less mission whose display name matches includeReplays (→ forced sg).
    - enough players-per-scope variety that at least one player falls below the is_show threshold in some
      scope and above it in another (to exercise computeIsShow at both <125 and >=125 scope sizes — seed a
      small scope so the 15% branch is hit, and assert a below-threshold player gets isShow=false).
    Each seeded replay carries `raw_snapshot.replay.mission`, the right `replay_timestamp`, current
    parser_result, and player/event rows consistent with the existing seed style.
  </action>
  <verify>
    <automated>pnpm vitest run src/modules/statistics/repository/tests/postgres.test.ts -t "parity corpus"</automated>
  </verify>
  <done>
    A reusable multi-type corpus seed exists and a smoke test confirms classification assigns the expected
    game_type (incl. nulls) to every seeded replay.
  </done>
</task>

<task type="auto">
  <name>Task 2: per-type + all-time + is_show parity assertions via the export</name>
  <files>src/modules/statistics/repository/tests/postgres.test.ts</files>
  <action>
    After seeding the corpus and running `recalculateAllCurrentParserResults`, drive
    `PgLegacyPublicStatsExportRepository.loadExportData` + `LegacyPublicStatsExportService.export` and assert
    legacy-parity:
    - sg `rotationStats`: per-rotation player/squad rows present only for sg replays; excluded replays do not
      appear; ordering matches the export's stable sort.
    - Global per-type buckets: sg/mace/sm global player/squad totals equal the hand-computed expected sums
      from the seeded corpus (the all-time bucket per type), and equal the sum of that type's replays only
      (no cross-type bleed, no double count of per-rotation + all-time).
    - mace<10 and sm<2023 replays contribute to NOTHING (assert a player who only appears in those replays
      is absent from all buckets).
    - is_show: a player below the per-scope threshold has `isShow=false` in the exported player object; a
      player above it has `isShow=true`; assert the otherPlayers/main split follows isShow.
    - Audit/report-shape guard (must-verify gate (b)): run a single-replay `*ForParserResult` recalc on one
      seeded replay and assert its return shape is unchanged; assert the full-run report keeps
      mode/reportVersion/summary keys. (If 01-03 added an optional per-type breakdown field, assert it is
      additive and existing keys are intact.)
    Compute expected values in-test from the seeded inputs (hand-rolled, deterministic) — this is the
    parity oracle. Keep assertions exact (toEqual on sorted arrays) consistent with the existing harness style.
  </action>
  <verify>
    <automated>pnpm vitest run src/modules/statistics/repository/tests/postgres.test.ts -t "per-type|all-time|is_show|parity"</automated>
  </verify>
  <done>
    Per-type + all-time aggregates and the is_show split match the hand-computed legacy expectations;
    excluded replays contribute nothing; the single-replay audit path and report shape are proven intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test oracle → asserted output | the hand-computed expectations are the parity oracle; wrong oracle = false green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Repudiation | false-green oracle | mitigate | Expectations hand-derived from seeded inputs (not from the code under test); exclusion cases assert absence, not just presence |
| T-01-14 | Tampering | non-deterministic seed | mitigate | Fixed timestamps/ids in seeds; truncate-per-test isolation; exact toEqual on sorted arrays |
| T-01-15 | Denial of Service | n/a | accept | Test-only; runs under the real-pg integration profile, not a request path |
</threat_model>

<verification>
- Extended (not replaced) real-pg harness proves per-type sg/mace/sm + all-time + sg per-rotation parity.
- mace<10 / sm<2023 / excludeReplays produce no rows; includeReplays forces sg.
- is_show split proven at both <125 and >=125 scope sizes.
- Single-replay audit path + ops report shape proven intact.
- `pnpm run test:integration` green (real-pg available).
</verification>

<success_criteria>
The real-pg comparison test proves the new side's per-type + all-time + is_show export matches legacy
sg-replay-parser semantics, unblocking F8 parity, with the audit path and report shape intact.
</success_criteria>

<output>
Create `.planning/phases/01-game-type-aware-statistics-parity/01-05-SUMMARY.md` when done.
</output>
