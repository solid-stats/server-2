---
phase: 12-legacy-public-export-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/statistics/export/legacy-public-export.ts
  - src/modules/statistics/export/tests/legacy-public-export.test.ts
  - src/modules/statistics/repository/legacy-export.ts
  - src/modules/statistics/repository/tests/legacy-export.test.ts
  - src/operations/export-legacy-public-stats.ts
  - src/operations/export-legacy-public-stats.test.ts
  - package.json
autonomous: true
requirements:
  - PUB-07
  - PUB-08
  - PUB-09
  - PUB-10
  - PUB-11
  - PUB-12
  - PUB-13
  - API-05
must_haves:
  truths:
    - Export output is deterministic and versioned as `legacy-public-export.v1`.
    - Export includes player global totals, squad totals, rotation-scoped totals, relationship details, weapon details, and weekly buckets.
    - Export uses persisted PostgreSQL/parser-result evidence, not raw OCAP parsing.
    - Export does not introduce public API/OpenAPI shape changes in this plan.
  artifacts:
    - path: src/modules/statistics/export/legacy-public-export.ts
      provides: Export contract types, deterministic assembly, and score/KD helpers
      contains: legacy-public-export.v1
    - path: src/modules/statistics/repository/legacy-export.ts
      provides: PostgreSQL read model for export evidence
      contains: LegacyPublicStatsExportRepository
    - path: src/operations/export-legacy-public-stats.ts
      provides: Operator CLI entrypoint
      contains: ops:stats:legacy-export
---

<objective>
Implement the Phase 12 legacy public export command and backend export contract.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add export contract and service</name>
  <files>src/modules/statistics/export/legacy-public-export.ts, src/modules/statistics/export/tests/legacy-public-export.test.ts</files>
  <read_first>src/modules/statistics/service/full-run-recalculation.ts, src/modules/statistics/readiness/readiness.ts</read_first>
  <action>Create `LegacyPublicStatsExportService`, export interfaces, metadata inputs, deterministic sorting, and math helpers for `kdRatio`, `killsFromVehicleCoef`, `totalScore`, and weekly `score`.</action>
  <verify>pnpm vitest run src/modules/statistics/export/tests/legacy-public-export.test.ts</verify>
  <acceptance_criteria>
    - Export metadata includes source database, command version, corpus scope, generated time, and contract version.
    - Arrays are sorted deterministically.
    - Tests cover zero-death KD, vehicle coefficient, score fields, and stable ordering.
  </acceptance_criteria>
  <done>Export assembly is deterministic and independently testable.</done>
</task>

<task type="auto">
  <name>Task 2: Add PostgreSQL export read model</name>
  <files>src/modules/statistics/repository/legacy-export.ts, src/modules/statistics/repository/tests/legacy-export.test.ts</files>
  <read_first>src/modules/public-stats/repository.ts, src/modules/statistics/repository/repository.ts, src/modules/statistics/parser-artifact.ts</read_first>
  <action>Add a read-only repository that returns player totals, squad totals, rotation-scoped totals, relationship counts, weapon counts, and weekly buckets from aggregate tables plus current parser-result events/snapshots. Normalize non-public parser differences in the export layer, not in aggregate storage.</action>
  <verify>pnpm vitest run src/modules/statistics/repository/tests/legacy-export.test.ts</verify>
  <acceptance_criteria>
    - Repository queries are read-only.
    - Relationship outputs include killed, killers, teamkilled, and teamkillers.
    - Weapon outputs distinguish firearms from vehicles where event evidence supports it.
    - Weekly outputs include week, start/end dates, total played games, counters, KD, vehicle coefficient, and score inputs.
  </acceptance_criteria>
  <done>Repository supplies the full legacy export evidence surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add operator CLI and package script</name>
  <files>src/operations/export-legacy-public-stats.ts, src/operations/export-legacy-public-stats.test.ts, package.json</files>
  <read_first>src/operations/recalculate-statistics.ts, src/operations/statistics-readiness.ts, package.json</read_first>
  <action>Add `pnpm run ops:stats:legacy-export` that prints the export JSON to stdout. Support `--corpus-scope <name>` and `--generated-at <iso>` for deterministic metadata.</action>
  <verify>pnpm vitest run src/operations/export-legacy-public-stats.test.ts && pnpm run typecheck && pnpm run lint</verify>
  <acceptance_criteria>
    - CLI returns exit code 0 on success.
    - CLI output is formatted deterministic JSON.
    - Package script exists and README can document it in plan 02.
  </acceptance_criteria>
  <done>Operators can run the legacy export command locally or in staging.</done>
</task>
</tasks>

<verification>
- [ ] pnpm vitest run src/modules/statistics/export/tests/legacy-public-export.test.ts src/modules/statistics/repository/tests/legacy-export.test.ts src/operations/export-legacy-public-stats.test.ts
- [ ] pnpm run openapi:check
- [ ] pnpm run typecheck
- [ ] pnpm run lint
</verification>

<success_criteria>
- Legacy export command exists and prints deterministic JSON.
- Export includes player, squad, rotation, relationship, weapon, and weekly surfaces.
- No public API/OpenAPI shape drift is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/12-legacy-public-export-contract/12-01-SUMMARY.md`.
</output>
