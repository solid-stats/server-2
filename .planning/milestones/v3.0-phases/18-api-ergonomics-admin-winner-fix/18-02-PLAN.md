---
phase: 18-api-ergonomics-admin-winner-fix
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/modules/public-stats/routes/schemas.ts
  - src/modules/public-stats/routes/filters.ts
  - src/modules/public-stats/routes/models.ts
  - src/modules/public-stats/routes/routes.ts
  - src/modules/public-stats/repository.ts
  - src/modules/public-stats/repository.test.ts
autonomous: true
requirements: [API-03]
must_haves:
  truths:
    - "GET /stats/commander-sides accepts an optional ?side=<value> query and returns only rows whose commander.side matches"
    - "GET /stats/commander-sides still accepts ?rotationId and the side+rotation filters compose (both applied with AND)"
    - "Commander-side responses still expose the existing unknownOutcomes field (explicit unknown outcome) — verified, not duplicated"
    - "Ordering stays commander.rotation_id desc, commander.side, players.display_name nulls last (unchanged); no pagination added"
  artifacts:
    - path: "src/modules/public-stats/routes/schemas.ts"
      provides: "side query field on the commander-sides querystring schema"
      contains: "side"
    - path: "src/modules/public-stats/routes/filters.ts"
      provides: "commander-side filter builder adding optional side via conditional-spread"
      contains: "side"
    - path: "src/modules/public-stats/repository.ts"
      provides: "listCommanderSides composes commander.side = $n::text alongside the rotation predicate"
      contains: "commander.side ="
  key_links:
    - from: "src/modules/public-stats/routes/routes.ts (/stats/commander-sides handler)"
      to: "listCommanderSides"
      via: "commander-side filter builder passes { rotationId?, side? }"
      pattern: "listCommanderSides"
    - from: "src/modules/public-stats/repository.ts (listCommanderSides)"
      to: "SQL WHERE"
      via: "rotationWhere.sqlWith(extra) combinator composes the side predicate"
      pattern: "commander\\.side = \\$"
---

<objective>
API-03: add a `side` filter to commander-side stats and verify the already-stored
`unknownOutcomes` explicit-unknown exposure. Per D-03 the unknown-outcome field is ALREADY
exposed (`CommanderSideRow.unknown_outcomes` → `CommanderSideResponse.unknownOutcomes`) — that
half is verify-with-a-test, do NOT duplicate. The new work is a single optional `side` query
predicate mirroring the existing `rotationId` filter SQL exactly.

Purpose: `web` needs filterable commander-side outcomes by rotation AND side. Rotation filtering
already exists; this adds side.
Output: `?side=<value>` on `/stats/commander-sides`, composed with the existing `?rotationId`.

NOTE on ordering: this plan depends on 18-01 because it also edits repository.ts/schemas.ts/
models.ts — run after 18-01 lands to avoid a merge conflict in those shared files.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-api-ergonomics-admin-winner-fix/18-CONTEXT.md
@.planning/phases/18-api-ergonomics-admin-winner-fix/18-PATTERNS.md

# Files this plan modifies:
@src/modules/public-stats/routes/schemas.ts
@src/modules/public-stats/routes/filters.ts
@src/modules/public-stats/routes/models.ts
@src/modules/public-stats/routes/routes.ts
@src/modules/public-stats/repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add side query field + commander-side filter builder + RotationFilters.side</name>
  <files>src/modules/public-stats/routes/schemas.ts, src/modules/public-stats/routes/filters.ts, src/modules/public-stats/routes/models.ts</files>
  <read_first>
    - src/modules/public-stats/routes/schemas.ts (RotationQuery ~36-38; the commander-sides route uses OverviewQueryType = Static of RotationQuery — see routes.ts ~543; the `/* eslint-disable ... new-cap */` header)
    - src/modules/public-stats/routes/filters.ts (rotationFilters ~124-126; replayListFilters ~137-145 for the `...(x === undefined ? {} : { x })` conditional-spread idiom)
    - src/modules/public-stats/routes/models.ts (RotationFilters ~147-149; PlayerListFilters ~151-153 extends it with search? as the precedent for adding an optional field)
    - src/modules/public-stats/routes/routes.ts (the /stats/commander-sides handler ~543-554)
  </read_first>
  <action>
    Add an optional `side` field for commander-side filtering. In schemas.ts, define a
    commander-sides querystring schema (e.g. `CommanderSideQuery = Type.Intersect([RotationQuery,
    Type.Object({ side: Type.Optional(Type.String({ minLength: 1 })) })])`) and export its Static
    type (e.g. `CommanderSideQueryType`) following the existing `BountyListQueryType` /
    `OverviewQueryType` export style; do NOT mutate the shared `RotationQuery` itself. In
    models.ts, add `side?: string` to `RotationFilters` (mirror how PlayerListFilters adds an
    optional field). In filters.ts, add a `commanderSideFilters(query: CommanderSideQueryType):
    RotationFilters` builder using the exact conditional-spread idiom: `{ ...rotationFilters(query),
    ...(query.side === undefined ? {} : { side: query.side }) }`. Keep the existing
    `rotationFilters` unchanged.
  </action>
  <verify>
    <automated>pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "side" src/modules/public-stats/routes/filters.ts` shows a commanderSideFilters builder spreading an optional side.
    - `grep -n "side?" src/modules/public-stats/routes/models.ts` shows RotationFilters.side added.
    - `grep -n "CommanderSideQuery" src/modules/public-stats/routes/schemas.ts` shows the new querystring schema + exported Static type.
    - `pnpm run typecheck` passes.
    - The shared `RotationQuery` schema is NOT mutated (rotationFilters still returns only rotationId).
  </acceptance_criteria>
  <done>An optional side filter exists at the schema, filter-builder, and filter-type layers; rotationFilters is untouched; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose commander.side predicate in listCommanderSides + wire route</name>
  <files>src/modules/public-stats/repository.ts, src/modules/public-stats/routes/routes.ts, src/modules/public-stats/repository.test.ts</files>
  <read_first>
    - src/modules/public-stats/repository.ts (listCommanderSides ~452-476; rotationWhere ~1317-1333 — note the `sqlWith(extra)` combinator and the `values` array it carries)
    - src/modules/public-stats/routes/routes.ts (commander-sides handler ~543-554 — currently passes rotationFilters(request.query); the querystring generic is OverviewQueryType)
    - src/modules/public-stats/routes/filters.ts (commanderSideFilters from Task 1)
    - src/modules/public-stats/repository.test.ts (existing listCommanderSides test style for the SQL-building assertions)
  </read_first>
  <behavior>
    - Test: listCommanderSides({}) builds SQL with no WHERE side predicate (only the base query) and binds zero filter values.
    - Test: listCommanderSides({ side: "west" }) builds SQL containing `commander.side = $1::text` (or the correct $n given no rotation) and binds "west".
    - Test: listCommanderSides({ rotationId, side: "east" }) composes BOTH predicates with AND — `where commander.rotation_id = $1 and commander.side = $2::text` — and binds [rotationId, "east"] in order.
    - Test: the existing ordering `order by commander.rotation_id desc, commander.side, players.display_name nulls last` is preserved.
  </behavior>
  <action>
    In `listCommanderSides`, after computing `condition = rotationWhere(filters,
    "commander.rotation_id")`, compose the side predicate ONLY when `filters.side !== undefined`:
    build the WHERE using `condition.sqlWith("commander.side = $" + String(condition.values.length
    + 1) + "::text")` and append `filters.side` to the bound values array (so it becomes the next
    `$n`); when side is undefined, keep the current `condition.sql` + `condition.values` unchanged.
    Bind every value as `$n` — never interpolate the side string. Keep the existing `order by`
    clause exactly. In routes.ts, change the commander-sides handler to (a) use the new
    `CommanderSideQuery` schema as the querystring and the `CommanderSideQueryType` generic, and
    (b) pass `commanderSideFilters(request.query)` to `listCommanderSides`. Add repository unit
    tests covering the four behaviors above (assert the generated SQL string + bound values via
    the same approach the existing listCommanderSides test uses — capture the query through the
    pool stub).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/modules/public-stats/repository.test.ts && pnpm run openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "commander.side = " src/modules/public-stats/repository.ts` shows the parameterized side predicate (with `::text` cast, bound as `$n`, never string-interpolated).
    - The commander-sides route handler passes `commanderSideFilters(request.query)` (grep routes.ts for commanderSideFilters).
    - `pnpm exec vitest run src/modules/public-stats/repository.test.ts` passes including the rotation+side composition test and the side-only test.
    - `pnpm run openapi:check` regenerates clean; the commander-sides querystring in the contract now includes an optional `side` parameter.
    - The ordering clause is byte-identical to the pre-change clause (no reordering).
    - unknownOutcomes is still present on CommanderSideResponse (no field removed) — confirmed by the contract regen and existing response schema.
  </acceptance_criteria>
  <done>listCommanderSides composes a parameterized side predicate alongside rotationId, the route passes the new filter builder, unknownOutcomes stays exposed, and SQL-composition behaviors are unit-tested green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query → SQL WHERE | the untrusted `?side=` value crosses into a SQL predicate |
| API → web consumer | commander-side response is public (anonymous) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering (SQL injection) | listCommanderSides side predicate | mitigate | Bind side as `$n::text` parameter via the rotationWhere sqlWith combinator — never string-interpolate; value validated as Type.String({ minLength: 1 }) at the schema layer |
| T-18-05 | Information Disclosure | commander-side filtered rows | mitigate | Filtering narrows the result set; rows expose only already-public aggregate counts + masked/omitted player reference (mapCommanderPlayer); no Steam64 — covered by 18-04 leak-guard sweep |
| T-18-06 | Denial of Service | unbounded filtered set | accept | Pattern returns the full filtered set as today (D-03: commander-side pagination deferred); side filter narrows rather than widens the set, so it cannot increase result size |
</threat_model>

<verification>
- `pnpm exec vitest run src/modules/public-stats/repository.test.ts` — side predicate composition + rotation+side AND + ordering preserved.
- `pnpm run typecheck` — filter type + schema lockstep.
- `pnpm run openapi:check` — contract shows the new optional side query param; unknownOutcomes unchanged.
</verification>

<success_criteria>
- Commander-side stats expose explicit unknown outcomes (verified, pre-existing) AND are filterable by rotation and side per API-03 / D-03.
- side composes with rotationId via AND; ordering and the no-pagination pattern are unchanged.
- The side value is parameter-bound (no injection); RotationQuery is not mutated.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 18-02)

- `src/modules/public-stats/routes/schemas.ts` — `CommanderSideQuery` schema + `CommanderSideQueryType` Static export (NEW).
- `src/modules/public-stats/routes/models.ts` — `RotationFilters.side?: string` (NEW optional field).
- `src/modules/public-stats/routes/filters.ts` — `commanderSideFilters(query): RotationFilters` builder (NEW function).
- `src/modules/public-stats/repository.ts` — `listCommanderSides` composes a parameterized `commander.side = $n::text` predicate (modified method).
- `src/modules/public-stats/routes/routes.ts` — `/stats/commander-sides` handler uses CommanderSideQuery + commanderSideFilters (modified handler).
- `src/modules/public-stats/repository.test.ts` — NEW unit tests for side/rotation predicate composition.
- No new routes (existing endpoint gains a query param), no new DB columns, no new migration.
</artifacts_produced>

<output>
Create `.planning/phases/18-api-ergonomics-admin-winner-fix/18-02-SUMMARY.md` when done
</output>
