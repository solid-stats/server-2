---
phase: 16-slug-resolution-history-provenance
plan: 06
type: execute
wave: 4
depends_on: [16-02, 16-04, 16-05]
files_modified:
  - src/modules/public-stats/tests/postgres.test.ts
  - src/modules/public-stats/routes/tests/players.test.ts
  - src/modules/public-stats/routes/tests/squads.test.ts
  - src/modules/public-stats/routes/tests/rotations.test.ts
  - src/modules/public-stats/routes/tests/history.test.ts
  - src/test/integration/steamid-leak-guard.test.ts
  - openapi/server-2.openapi.json
autonomous: true
requirements: [API-01, HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "Integration tests prove player/squad/rotation resolve by both UUID and slug, and unknown slug returns 404 not 500"
    - "Integration tests prove SQL slug_base() equals TS slugify() for fixture names and partial-unique rejects duplicate non-null slugs"
    - "Integration tests prove history endpoints return ordered entries with explicit unknown-gaps and provenance from returned rows"
    - "The Steam64 leak guard sweeps the new history + rotation-detail endpoints and finds zero 7656119\\d{10}"
    - "openapi/server-2.openapi.json is regenerated, committed, and openapi:check is green"
  artifacts:
    - path: "src/modules/public-stats/routes/tests/history.test.ts"
      provides: "integration coverage for name/membership history + provenance + gaps"
    - path: "openapi/server-2.openapi.json"
      provides: "regenerated additive contract with slug fields, history routes, provenance"
      contains: "name-history"
  key_links:
    - from: "src/test/integration/steamid-leak-guard.test.ts PUBLIC_DETAIL_ROUTES"
      to: "new history + rotation-detail routes"
      via: "route array entries swept by it.each"
      pattern: "name-history|membership-history|/stats/rotations/"
---

<objective>
Замкнуть фазу DB-зависимой верификацией (запускается на merge — worktree-executor без живой БД):
(1) интеграционные тесты slug-резолюции (uuid И slug, unknown→404), backfill-детерминизма
(SQL slug_base == TS slugify), partial-unique поведения; (2) интеграционные тесты history-эндпоинтов
(порядок, явные gaps, provenance) и rotation detail; (3) расширить Steam64 leak-guard на новые
history + rotation-detail роуты; (4) регенерировать и закоммитить `openapi/server-2.openapi.json`,
держать `openapi:check` зелёным; (5) подтвердить, что byte-identical parity Phase 15 осталась зелёной.

Purpose: автоматизированное доказательство всех трёх success-criteria фазы + SEC-01/02 + контрактного
гейта. Это единственный план, где все четыре требования сходятся в исполняемые проверки.

Output: 5 тест-файлов (новые/расширенные) + регенерированный openapi артефакт.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md
@.planning/phases/16-slug-resolution-history-provenance/16-VALIDATION.md
@.agents/skills/solidstats-backend-ts-tests/SKILL.md
@.agents/skills/openapi-to-typescript/SKILL.md
@src/test/integration/steamid-leak-guard.test.ts
@src/infra/db/migrate.ts
@src/modules/public-stats/repository.ts
@src/modules/public-stats/routes/slug.ts

<interfaces>
<!-- Real-pg harness pattern (steamid-leak-guard.test.ts 149-207): -->
<!--   runMigrations(config.databaseUrl) in beforeAll; seed via pool.query; app.inject; expectNoSteam64. -->
<!-- Leak guard route arrays (32-43): PUBLIC_LIST_ROUTES, PUBLIC_DETAIL_ROUTES; it.each sweep (90-105). -->
<!-- STEAM64_PATTERN = /7656119\d{10}/u ; expectNoSteam64 (26-30) — reuse unchanged. -->
<!-- test:integration script: vitest run src/test/integration + src/**/tests/postgres.test.ts --no-file-parallelism -->
<!-- openapi:export = tsx src/openapi/export-openapi.ts ; openapi:check = verify + openapi-typescript codegen. -->
<!-- DATABASE_URL default postgresql://solid:solid@localhost:15432/solid_stats -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: integration — slug resolution, backfill determinism, partial-unique, history, provenance</name>
  <files>src/modules/public-stats/tests/postgres.test.ts, src/modules/public-stats/routes/tests/players.test.ts, src/modules/public-stats/routes/tests/squads.test.ts, src/modules/public-stats/routes/tests/rotations.test.ts, src/modules/public-stats/routes/tests/history.test.ts</files>
  <read_first>
    - src/modules/public-stats/tests/postgres.test.ts (существующий real-pg harness — расширить)
    - src/modules/public-stats/routes/tests/players.test.ts и squads.test.ts (форма app.inject route-тестов)
    - src/test/integration/steamid-leak-guard.test.ts (real-pg seed/runMigrations/app.inject pattern lines 149-207)
    - src/modules/public-stats/routes/slug.ts (slugify — сверять с slug_base)
    - .planning/phases/16-slug-resolution-history-provenance/16-VALIDATION.md (Per-Requirement Verification Map)
  </read_first>
  <behavior>
    - resolve by uuid: GET /stats/players/<uuid> -> 200 with slug field; same for squads, rotations
    - resolve by slug: GET /stats/players/<slug> -> 200 same row; same for squads, rotations
    - unknown slug: GET /stats/players/non-existent-slug -> 404 (NOT 500)
    - backfill determinism: for fixture names incl. a Cyrillic one, `select slug_base($1)` == TS slugify($1) for each
    - partial-unique: inserting two rows with same non-null slug -> unique violation; two rows with null slug -> ok
    - name-history: seeded player_nicknames -> entries ascending by from, open window to=null, sourceReplayId nullable, explicit unknown-gap present between disjoint windows
    - membership-history (player & squad): counterpart carries {id,slug,name|displayName}, NO Steam64; entries ordered; gaps explicit
    - provenance: profile/history responses carry provenance.lastUpdatedAt = max over seeded rows; a player with no stat/nickname rows -> provenance.lastUpdatedAt null (never request-time)
  </behavior>
  <action>
    1. Расширить `tests/postgres.test.ts`: добавить тесты backfill-детерминизма (массив fixture-имён,
       включая `'Игрок Вася'`; для каждого `pool.query('select slug_base($1) as b',[name])` сверить с
       импортированным `slugify(name)`); partial-unique (попытка дубля non-null slug -> ожидать throw;
       два null slug -> ok). Использовать `runMigrations` в beforeAll (harness уже есть).
    2. Расширить `routes/tests/players.test.ts` и `squads.test.ts`: seed canonical row с известным slug;
       проверить resolve по uuid И по slug -> 200 + поле slug; unknown slug -> 404; provenance присутствует.
    3. Новый `routes/tests/rotations.test.ts`: seed rotation + связанные player_stats; GET /stats/rotations/:id
       по uuid и slug -> 200 + slug + provenance.lastUpdatedAt = max(calculated_at); unknown -> 404;
       rotation без stat-строк -> provenance.lastUpdatedAt = created_at floor либо null (по реализации Plan 04).
    4. Новый `routes/tests/history.test.ts`: seed player_nicknames (две disjoint observed-окна + одно открытое)
       и squad_memberships (для обеих сторон); проверить name-history/membership-history:
       порядок ascending nulls-first, явный `{kind:"unknown-gap"}` между disjoint, отсутствие trailing-gap
       для открытого окна, counterpart без Steam64, provenance из вернувшихся строк, пустой случай -> entries:[], provenance null.
    Все тесты по AAA, изолированы (cleanup своих seed-строк в afterAll/afterEach), детерминированы.
  </action>
  <verify>
    <automated>pnpm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run test:integration` зелёный, включая новые backfill/partial-unique/resolution/history/provenance тесты.
    - Тест unknown-slug возвращает статус 404 (явный assert `toBe(404)`), не 500.
    - Тест backfill-детерминизма сверяет `slug_base($1)` с TS `slugify($1)` минимум для одного Cyrillic-имени и проходит.
    - history-тест ассертит наличие `{kind:"unknown-gap"}` между disjoint-окнами и его отсутствие после открытого окна.
    - Существующая byte-identical parity (Phase 15) интеграция остаётся зелёной в том же прогоне.
  </acceptance_criteria>
  <done>Интеграционные тесты доказывают slug-резолюцию (uuid/slug/404), backfill-детерминизм, partial-unique, history-порядок+gaps, provenance из строк; parity Phase 15 не сломана.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend Steam64 leak-guard to new routes + regenerate & commit OpenAPI</name>
  <files>src/test/integration/steamid-leak-guard.test.ts, openapi/server-2.openapi.json</files>
  <read_first>
    - src/test/integration/steamid-leak-guard.test.ts (route arrays 32-43; it.each sweep 90-105; real-pg seed 149-207)
    - .planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md (Pitfall 5 OpenAPI regen lines 396-399; Runtime State Inventory lines 358-370)
    - .agents/skills/openapi-to-typescript/SKILL.md
  </read_first>
  <behavior>
    - leak-guard sweep covers new endpoints: /stats/players/:id/name-history, /stats/players/:id/membership-history, /stats/squads/:id/membership-history, /stats/rotations/:id
    - the real-pg seeded sweep (player with a planted Steam64 + a seeded membership/nickname) emits zero 7656119\d{10} over history/rotation bodies
    - openapi/server-2.openapi.json regenerated reflects slug fields, provenance, new routes; openapi:check green
  </behavior>
  <action>
    1. Расширить route-массивы в `steamid-leak-guard.test.ts`: добавить в `PUBLIC_DETAIL_ROUTES`
       (или новый массив, сметаемый тем же `it.each`) пути
       `/stats/players/${PLAYER_ID}/name-history`, `/stats/players/${PLAYER_ID}/membership-history`,
       `/stats/squads/${SQUAD_ID}/membership-history`, `/stats/rotations/${ROTATION_ID}`.
       Добавить `ROTATION_ID` константу. `STEAM64_PATTERN`/`expectNoSteam64` НЕ менять.
    2. Расширить real-pg сметание (блок 149-207): засеять для seeded player не только steam_id,
       но и player_nickname + squad_membership (counterpart), затем `app.inject` на новые history/rotation
       роуты и `expectNoSteam64(response.json())` + `expectNoSteam64(response.payload)`; cleanup в afterAll.
    3. Регенерировать контракт: `pnpm run openapi:export`, затем `pnpm run openapi:check` (verify byte-compare
       + openapi-typescript codegen). Закоммитить обновлённый `openapi/server-2.openapi.json`.
    Все изменения схем additive — никаких breaking-diff (это Phase 19); если openapi:check падает — значит
    артефакт не регенерирован, повторить export.
  </action>
  <verify>
    <automated>pnpm run test:integration -- steamid-leak-guard &amp;&amp; pnpm run openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "name-history|membership-history|/stats/rotations/" src/test/integration/steamid-leak-guard.test.ts` находит все четыре новых route в sweep-массивах.
    - `pnpm run test:integration -- steamid-leak-guard` зелёный; real-pg сметание с засеянным Steam64 на history/rotation-роутах эмитит ноль `7656119\d{10}`.
    - `pnpm run openapi:check` зелёный (verify byte-compare проходит — артефакт регенерирован и закоммичен).
    - `grep -c "name-history" openapi/server-2.openapi.json` >= 1 (контракт содержит новые роуты).
    - `git status --short openapi/server-2.openapi.json` показывает обновлённый артефакт перед commit-step фазы.
  </acceptance_criteria>
  <done>Leak-guard сметает новые history+rotation роуты (ноль Steam64); OpenAPI регенерирован/закоммичен; openapi:check зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded DB rows → new route bodies | Real-pg sweep verifies the masking + counterpart-shape boundary holds on new surfaces. |
| committed OpenAPI artifact | Stale artifact is a contract-drift risk; verify gate enforces freshness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Information Disclosure | Steam64 leak via new history/rotation endpoints | mitigate | Leak-guard route arrays extended; real-pg seeded sweep asserts zero 7656119\d{10}. |
| T-16-17 | Tampering | slug_base TS/SQL divergence reaching production | mitigate | Integration test asserts SQL slug_base == TS slugify on fixtures (incl. Cyrillic). |
| T-16-18 | Repudiation / Contract drift | stale committed OpenAPI artifact | mitigate | openapi:export regenerate + openapi:check byte-compare gate. |
| T-16-SC | Tampering | npm package installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm run test:integration` полностью зелёный (resolution, backfill, partial-unique, history, provenance, leak-guard, Phase-15 parity).
- `pnpm run openapi:check` зелёный с закоммиченным артефактом.
- `pnpm run verify` зелёный как финальный фазовый гейт (format, lint, typecheck, test, test:integration, openapi:check, ops, coverage).
</verification>

<success_criteria>
DB-зависимая верификация доказывает все три success-criteria фазы (slug-резолюция; history с явными
gaps + timestamps; provenance из вернувшихся строк) плюс SEC-01/02 (ноль Steam64 на новых surface) и
контрактный гейт (регенерированный OpenAPI, openapi:check зелёный); parity Phase 15 не сломана.
</success_criteria>

<output>
Create `.planning/phases/16-slug-resolution-history-provenance/16-06-SUMMARY.md` when done
</output>
