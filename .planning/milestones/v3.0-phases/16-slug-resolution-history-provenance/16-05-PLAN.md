---
phase: 16-slug-resolution-history-provenance
plan: 05
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - src/modules/public-stats/routes/routes.ts
autonomous: true
requirements: [API-01, HIST-01, HIST-02]
must_haves:
  truths:
    - "Detail routes /stats/players/:id and /stats/squads/:id accept a slug-or-uuid param"
    - "A new GET /stats/rotations/:id detail route resolves by slug or uuid and 404s on unknown"
    - "Three history sub-resource routes exist and return the discriminated-union history responses"
    - "All new/changed routes declare their TypeBox response schema and reuse the existing NotFound 404 path"
  artifacts:
    - path: "src/modules/public-stats/routes/routes.ts"
      provides: "relaxed detail params + rotation detail route + 3 history sub-resource routes"
      contains: "name-history"
  key_links:
    - from: "src/modules/public-stats/routes/routes.ts"
      to: "readModel.getRotation / getPlayerNameHistory / getPlayerMembershipHistory / getSquadMembershipHistory"
      via: "options.readModel.<method>(request.params.id)"
      pattern: "getRotation|getPlayerNameHistory|getPlayerMembershipHistory|getSquadMembershipHistory"
---

<objective>
Зарегистрировать публичные роуты фазы: (1) релаксировать `params` detail-роутов
`/stats/players/:id` и `/stats/squads/:id` с `UuidParameters` на `SlugOrUuidParameters`;
(2) добавить `GET /stats/rotations/:id` detail-роут (slug-or-uuid); (3) добавить три history
sub-resource роута. Все — additive, переиспользуя существующий error/NotFound-паттерн child-scope.

Purpose: HTTP-поверхность для API-01 (slug-резолюция detail + rotation detail) и HIST-01/HIST-02
(history-эндпоинты). Роуты вызывают read-model методы, объявленные в интерфейсе (Plan 03) и
реализованные (Plan 04); стабы empty-read-model (Plan 03) держат boot-without-DB зелёным.

Output: 1 модифицированный файл (routes.ts).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-slug-resolution-history-provenance/16-RESEARCH.md
@.planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md
@.agents/skills/solidstats-backend-ts-conventions/SKILL.md
@src/modules/public-stats/routes/routes.ts
@src/modules/public-stats/routes/schemas.ts
@src/modules/public-stats/routes/models.ts

<interfaces>
<!-- Existing route patterns (routes.ts): -->
<!--   detail: /stats/players/:id (178-200), /stats/squads/:id (293-313) -->
<!--   sub-resource: /stats/players/:id/weapons (202-217), /relationships (236-253) -->
<!--   rotation list: registerRotationRoutes (142-156) -->
<!--   child scope + mapPublicStatsError (72-79); NOT_FOUND const + reply.code(NOT_FOUND).send({message}) -->
<!--   import schemas/types from "./schemas.js" / "./models.js" -->

<!-- New schema/type names from Plan 03: -->
<!--   SlugOrUuidParameters / SlugOrUuidParametersType -->
<!--   RotationDetailResponse / RotationDetailResponseType -->
<!--   NameHistoryResponse / NameHistoryResponseType -->
<!--   PlayerMembershipHistoryResponse / PlayerMembershipHistoryResponseType -->
<!--   SquadMembershipHistoryResponse / SquadMembershipHistoryResponseType -->

<!-- History routes are reached via the already-resolved id; the parent detail route -->
<!-- resolves slug->id first, but each history route's :id is itself slug-or-uuid bound -->
<!-- (param schema relaxed) and the read-model method resolves it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: relax detail params + add rotation detail route + 3 history sub-resource routes</name>
  <files>src/modules/public-stats/routes/routes.ts</files>
  <read_first>
    - src/modules/public-stats/routes/routes.ts (весь модуль — self-extension; detail/sub-resource/rotation registration + child scope + NotFound pattern)
    - src/modules/public-stats/routes/schemas.ts (новые схемы из Plan 03)
    - src/modules/public-stats/routes/models.ts (новые типы из Plan 03)
    - .planning/phases/16-slug-resolution-history-provenance/16-PATTERNS.md (routes.ts section lines 270-300)
  </read_first>
  <action>
    1. Импортировать из `./schemas.js`: `SlugOrUuidParameters`, `RotationDetailResponse`,
       `NameHistoryResponse`, `PlayerMembershipHistoryResponse`, `SquadMembershipHistoryResponse`
       и соответствующие `...Type` из их Static-экспортов.
    2. Заменить `params: UuidParameters` -> `params: SlugOrUuidParameters` и generic
       `Params: UuidParametersType` -> `Params: SlugOrUuidParametersType` на detail-роутах
       `/stats/players/:id` и `/stats/squads/:id` (тело-резолюция в read-model уже ветвится — Plan 04).
       Sub-resource player/squad роуты (weapons/vehicles/relationships/weekly) тоже релаксировать
       до `SlugOrUuidParameters`, т.к. их `:id` теперь может быть slug.
    3. В `registerRotationRoutes` добавить detail-роут:
       `app.get<{ Params: SlugOrUuidParametersType }>("/stats/rotations/:id", { schema: { params: SlugOrUuidParameters, response: { 200: RotationDetailResponse, 404: NotFoundResponse }, tags: ["public-stats"] } }, async (request, reply) => { const item = await options.readModel.getRotation(request.params.id); return item ?? reply.code(NOT_FOUND).send({ message: "rotation not found" }); });`
    4. В `registerPlayerRoutes` добавить два history-роута (форма sub-resource):
       `GET /stats/players/:id/name-history` -> `options.readModel.getPlayerNameHistory(request.params.id)`, response 200: NameHistoryResponse, 404: NotFoundResponse.
       `GET /stats/players/:id/membership-history` -> `getPlayerMembershipHistory`, response 200: PlayerMembershipHistoryResponse.
    5. В `registerSquadRoutes` добавить:
       `GET /stats/squads/:id/membership-history` -> `getSquadMembershipHistory`, response 200: SquadMembershipHistoryResponse, 404: NotFoundResponse.
    Переиспользовать `reply.code(NOT_FOUND).send({ message: ... })`-паттерн и `mapPublicStatsError` child-scope как есть.
  </action>
  <verify>
    <automated>pnpm run typecheck &amp;&amp; pnpm run lint &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SlugOrUuidParameters" src/modules/public-stats/routes/routes.ts` покрывает оба detail-роута + sub-resources + rotation detail + 3 history (no remaining `UuidParameters` on detail/sub-resource/history routes).
    - `grep -n "/stats/rotations/:id" routes.ts` находит новый detail-роут.
    - `grep -nE "/stats/players/:id/name-history|/stats/players/:id/membership-history|/stats/squads/:id/membership-history" routes.ts` находит все три history-роута.
    - Каждый новый роут имеет `response: { 200: ..., 404: NotFoundResponse }` и `tags: ["public-stats"]`.
    - `pnpm run typecheck`, `pnpm run lint`, `pnpm test` (unit, использует empty-read-model стабы) зелёные.
  </acceptance_criteria>
  <done>Detail-параметры релаксированы до slug-or-uuid; rotation detail + 3 history роута зарегистрированы с объявленными схемами и NotFound-путём.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route param | `:id` (untrusted) bound by SlugOrUuidParameters (maxLength 128 + pattern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Denial of Service | unbounded path segment on new routes | mitigate | All detail/history/rotation routes use SlugOrUuidParameters (bounded). |
| T-16-14 | Information Disclosure | unknown slug → 500 instead of 404 | mitigate | read-model resolver (Plan 04) returns null; route maps null → 404 via existing NotFound path. |
| T-16-15 | Information Disclosure | Steam64 in new route responses | mitigate | Responses gated by TypeBox schemas (Plan 03) carrying no Steam64; verified by leak-guard sweep (Plan 06). |
| T-16-SC | Tampering | npm package installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm run typecheck`, `pnpm run lint`, `pnpm test` зелёные.
- Detail/sub-resource/history роуты используют SlugOrUuidParameters.
- rotation detail + 3 history роута присутствуют с response-схемами и 404-путём.
</verification>

<success_criteria>
HTTP-поверхность фазы зарегистрирована additive: detail-роуты принимают slug-or-uuid, добавлен
rotation detail и три history-эндпоинта, все с объявленными response-схемами и переиспользованием
существующего NotFound/error-паттерна — покрывая API-01 (routes) и HIST-01/HIST-02 (routes).
</success_criteria>

<output>
Create `.planning/phases/16-slug-resolution-history-provenance/16-05-SUMMARY.md` when done
</output>
