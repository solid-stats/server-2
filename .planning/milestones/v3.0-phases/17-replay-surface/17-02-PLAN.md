---
phase: 17-replay-surface
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/modules/public-stats/routes/schemas.ts
  - src/modules/public-stats/routes/filters.ts
  - src/modules/public-stats/routes/filters.test.ts
  - src/modules/public-stats/routes/routes.ts
  - src/modules/public-stats/routes/tests/routes.test.ts
  - src/test/integration/steamid-leak-guard.test.ts
  - openapi/server-2.openapi.json
autonomous: true
requirements: [REPLAY-01, REPLAY-02, REPLAY-03]
must_haves:
  truths:
    - "GET /stats/replays lists replays filtered by rotationId/fromDate/toDate with cursor pagination and the sort=date whitelist"
    - "GET /stats/replays/:id returns replay detail (map nullable, rotation, sides, participants, provenance) and 404 for unknown id, resolving slug-or-uuid"
    - "GET /stats/replays/:id/events pages the event timeline with cursor + clamped limit and 404 when the replay does not resolve"
    - "A mixed page+cursor request to /stats/replays is rejected 400 by the existing preValidation guard"
    - "No full Steam64 appears in any replay route body, cursor token, or error payload (leak-guard extended)"
  artifacts:
    - path: "src/modules/public-stats/routes/schemas.ts"
      provides: "ReplaySummaryResponse, ReplayDetailResponse, ReplayEventResponse, ReplayListQuery, ReplayEventsQuery"
      contains: "ReplayDetailResponse"
    - path: "src/modules/public-stats/routes/routes.ts"
      provides: "registerReplayRoutes wired into the public-stats child scope"
      contains: "/stats/replays"
    - path: "openapi/server-2.openapi.json"
      provides: "regenerated contract including the 3 replay JSON routes"
      contains: "/stats/replays"
  key_links:
    - from: "src/modules/public-stats/routes/routes.ts"
      to: "options.readModel.listReplays / getReplay / getReplayEvents"
      via: "route handlers"
      pattern: "listReplays|getReplay"
    - from: "src/test/integration/steamid-leak-guard.test.ts"
      to: "/stats/replays/:id and /events"
      via: "PUBLIC_DETAIL_ROUTES + real-pg seed sweep"
      pattern: "/stats/replays"
---

<objective>
Expose the three replay JSON routes on the existing `public-stats` child scope: TypeBox
schemas, filter/query wiring, route registration, route-level inject tests, leak-guard
extension (the defining security control), and OpenAPI regeneration.

Purpose: 17-01 landed the read-model methods + SQL; this plan turns them into the public
contract `web` consumes, inheriting the cursor mixed-param 400 guard and BadCursorError→400
handler from the child scope for free.
Output: replay schemas, `replayListFilters` + sort wiring, `registerReplayRoutes`, route +
leak-guard tests, regenerated `openapi/server-2.openapi.json`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-replay-surface/17-CONTEXT.md
@.planning/phases/17-replay-surface/17-RESEARCH.md
@.planning/phases/17-replay-surface/17-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor consumes directly. -->

From src/modules/public-stats/routes/schemas.ts:
- `SlugOrUuidParameters = Type.Object({ id: Type.String({ minLength:1, maxLength:128, pattern:"^[A-Za-z0-9-]+$" }) })` — reuse verbatim for `:id`.
- `PaginationQuery` (cursor/limit/order/sort optionals), `RotationQuery` ({ rotationId? uuid }), `ProvenanceResponse` ({ lastUpdatedAt: date-time|null }), `NotFoundResponse` ({ message }).
- `paginated<T>(item)` → `{ hasMore: boolean, items: T[], nextCursor: string|null }` — wrap list responses with this.
- Pattern: schemas declared in one `export const A = ..., B = ...` block; `export type XType = Static<typeof X>` at the bottom.

From src/modules/public-stats/routes/routes.ts:
- `registerPublicStatsRoutes` registers a CHILD scope with `rejectLegacyPaginationParameters` preValidation + `mapPublicStatsError` (BadCursorError→400). Add a `registerReplayRoutes(scope, options)` call inside that same `app.register((scope) => {...})` so replay routes inherit both.
- Detail/404 pattern: `const item = await options.readModel.getX(...); return item ?? reply.code(404).send({ message: "... not found" })`.
- List pattern: `async (request) => options.readModel.listX(filters(request.query), page(request.query, X_SORT, X_SORT_DEFAULT))`.

From src/modules/public-stats/routes/filters.ts:
- `page(query, whitelist, defaultSort)` builds the keyset PageQuery (decodes cursor, validates sort).
- `rotationFilters(query)` extracts rotationId. Add `replayListFilters(query)` returning `{ rotationId?, fromDate?, toDate? }`. For the events route build the PageQuery via `page(request.query, EVENT_SORT, EVENT_SORT_DEFAULT)` (the `EVENT_SORT` SortDescriptor whitelist + `EVENT_SORT_DEFAULT` from 17-01's sort.ts — `page()` takes a SortDescriptor whitelist, there is no separate `EVENT_KEYSET` type). The events default order is ASCENDING (NULLS FIRST) per 17-CONTEXT; if `page()` defaults to DESC, pass/normalize the events order to `asc` so the seek matches the `(occurred_at ASC NULLS FIRST, id ASC)` index. The hard limit clamp lives in the repository (`EVENT_PAGE_MAX`), so the helper need not clamp.

From 17-01 (models.ts): `listReplays(filters, page)`, `getReplay(id)`, `getReplayEvents(id, page)`; types `ReplaySummary`, `ReplayDetail`, `ReplayEvent`, `ReplayListFilters`. `getReplayEvents` returns `null` when the replay does not resolve (→ 404).

From src/test/integration/steamid-leak-guard.test.ts:
- `PUBLIC_DETAIL_ROUTES` array (add replay detail + events URLs) and the real-pg seeded sweep block (seed a replay + current parser_result whose `raw_snapshot.players[].sid` carries `76561198012347890`).
- `expectNoSteam64(value)` helper + `STEAM64_PATTERN = /7656119\d{10}/u`.

OpenAPI: after route changes run `pnpm run openapi:export`, commit `openapi/server-2.openapi.json`; `pnpm run openapi:check` fails on stale schema. Sitemap XML routes are NOT in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replay TypeBox schemas + filter/query wiring</name>
  <files>src/modules/public-stats/routes/schemas.ts, src/modules/public-stats/routes/filters.ts, src/modules/public-stats/routes/filters.test.ts</files>
  <read_first>
    - src/modules/public-stats/routes/schemas.ts (PaginationQuery, RotationQuery, ProvenanceResponse, paginated(), SlugOrUuidParameters, Static type export block)
    - src/modules/public-stats/routes/filters.ts (page(), rotationFilters(), playerListFilters() shape)
    - src/modules/public-stats/routes/filters.test.ts (filter unit-test style)
    - src/modules/public-stats/routes/pagination/sort.ts (REPLAY_SORT / EVENT_SORT whitelists + EVENT_PAGE_MAX added in 17-01)
    - .planning/phases/17-replay-surface/17-01-SUMMARY.md (final ReplayDetail/ReplaySummary/ReplayEvent field names)
  </read_first>
  <action>
    Add TypeBox schemas to `schemas.ts`: `ReplaySummaryResponse` (id, slug, rotationId
    nullable, replayTimestamp string|null, sourceSystem, sourceReplayId, status);
    `ReplayListResponse = paginated(ReplaySummaryResponse)`; `ReplayDetailResponse` (id, slug,
    rotation object|null with {id,slug,name}, replayTimestamp string|null, map string|null,
    sides array of {side, outcome:{status,winnerSide:string|null}, isWinner:boolean|null,
    participantCount}, participants array of {player:{id:string|null, slug:string|null,
    displayName}, steamId:string|null, kills, deaths, teamkills}, provenance: ProvenanceResponse);
    `ReplayEventResponse` (id, eventType, occurredAt string|null, actor object|null,
    payload: Type.Record/Unknown) and `ReplayEventsResponse = paginated(ReplayEventResponse)`;
    `ReplayListQuery = Type.Intersect([PaginationQuery, RotationQuery, Type.Object({ fromDate:
    Optional date-time, toDate: Optional date-time })])`; `ReplayEventsQuery = PaginationQuery`.
    Export the matching `Static` types. In `filters.ts` add `replayListFilters(query:
    ReplayListQueryType): ReplayListFilters` (spread rotationFilters + conditional fromDate/toDate,
    same conditional-spread style as playerListFilters). Cover `replayListFilters` in
    `filters.test.ts` (rotation only, date only, both, none).
    NOTE: steamId in participants is `Type.Union([Type.String(), Type.Null()])` — it is the masked
    `...NNNN` form, NEVER a full Steam64.
  </action>
  <verify>
    <automated>pnpm test -- filters.test && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.ts` exports `ReplaySummaryResponse`, `ReplayListResponse`, `ReplayDetailResponse`, `ReplayEventResponse`, `ReplayEventsResponse`, `ReplayListQuery`, `ReplayEventsQuery` and their `Static` types.
    - `ReplayDetailResponse.participants[].steamId` is a `string|null` union (masked form), not required.
    - `replayListFilters` returns only the keys present in the query (conditional spread); `filters.test.ts` asserts all four combinations.
    - `pnpm run typecheck` passes (schemas align with 17-01 model types).
  </acceptance_criteria>
  <done>Replay schemas + filter wiring land with green filter unit tests and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: registerReplayRoutes + route inject tests</name>
  <files>src/modules/public-stats/routes/routes.ts, src/modules/public-stats/routes/tests/routes.test.ts</files>
  <read_first>
    - src/modules/public-stats/routes/routes.ts (child-scope register, registerPlayerRoutes/registerRotationRoutes patterns, NOT_FOUND const, detail/list handler shapes)
    - src/modules/public-stats/routes/tests/routes.test.ts (FakePublicStatsReadModel inject-test style)
    - src/modules/public-stats/routes/tests/fixtures.ts (replay builders added in 17-01)
    - src/modules/public-stats/routes/schemas.ts (the schemas from Task 1)
  </read_first>
  <action>
    Add `registerReplayRoutes(app, options)` and call it inside the existing
    `app.register((scope) => {...})` block in `registerPublicStatsRoutes` (so replay routes
    inherit the `rejectLegacyPaginationParameters` preValidation 400 and the BadCursorError→400
    error handler). Register: `GET /stats/replays` (querystring `ReplayListQuery`, response 200
    `ReplayListResponse`, tags ["public-stats"]) → `options.readModel.listReplays(replayListFilters(
    request.query), page(request.query, REPLAY_SORT, REPLAY_SORT_DEFAULT))`; `GET /stats/replays/:id`
    (params `SlugOrUuidParameters`, response 200 `ReplayDetailResponse` + 404 `NotFoundResponse`) →
    getReplay then `item ?? reply.code(404).send({ message: "replay not found" })`; `GET
    /stats/replays/:id/events` (params `SlugOrUuidParameters`, querystring `ReplayEventsQuery`,
    response 200 `ReplayEventsResponse` + 404 `NotFoundResponse`) → getReplayEvents (using a
    page() built from the `EVENT_SORT` whitelist + `EVENT_SORT_DEFAULT`, ascending order) then 404 when the method returns null. Keep each register
    function within the max-lines-per-function limit (extract a helper if needed, mirroring
    `registerPlayerHistoryRoutes`). Add inject tests in `routes.test.ts` using
    `FakePublicStatsReadModel`: list returns the cursor shape; detail 200 + 404; events 200 + 404;
    `?page=1&cursor=x` on `/stats/replays` → 400; a malformed cursor → 400.
  </action>
  <verify>
    <automated>pnpm test -- routes.test && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `routes.ts` registers exactly the three routes `/stats/replays`, `/stats/replays/:id`, `/stats/replays/:id/events`, all tagged `public-stats`, registered inside the shared child scope.
    - Detail + events return 404 with `{ message }` when the read-model yields null/undefined.
    - Inject test proves `/stats/replays?page=1&cursor=abc` → 400 (inherited mixed-param guard) and a malformed cursor → 400.
    - Events route builds its PageQuery from `EVENT_SORT`/`EVENT_SORT_DEFAULT` (not the list REPLAY_SORT), ascending; test asserts a clamped limit is honored end-to-end via the fake.
    - `pnpm run typecheck` passes.
  </acceptance_criteria>
  <done>Three replay routes registered in the child scope with full inject coverage including 404 + 400 paths.</done>
</task>

<task type="auto">
  <name>Task 3: Steam64 leak-guard extension + OpenAPI regen</name>
  <files>src/test/integration/steamid-leak-guard.test.ts, openapi/server-2.openapi.json</files>
  <read_first>
    - src/test/integration/steamid-leak-guard.test.ts (PUBLIC_DETAIL_ROUTES, real-pg seed block, expectNoSteam64, LEAKED_STEAM64)
    - src/modules/statistics/parser-artifact.ts (raw_snapshot.players[].sid shape to seed)
    - .planning/phases/17-replay-surface/17-RESEARCH.md (Pitfall 2 — leak-guard route arrays; Pitfall 5 — OpenAPI stale)
  </read_first>
  <action>
    Extend `steamid-leak-guard.test.ts`: add `/stats/replays/:id` and `/stats/replays/:id/events`
    (using a seeded `REAL_PG_REPLAY_ID`) to `PUBLIC_DETAIL_ROUTES` for the buildApp() empty sweep,
    and add a real-pg seeded case that inserts a `replays` row (with slug) + a `parser_results` row
    `status='current'` whose `raw_snapshot.players[].sid` contains the planted `LEAKED_STEAM64`
    (`76561198012347890`). CRITICAL (B-1): the seeded `parser_events` row(s) MUST carry the planted
    `LEAKED_STEAM64` INSIDE `payload` at the real leak vector — set `payload.player.steam_id` AND
    `payload.attacker.steam_id` (and a deeply nested `payload.context.crew[2].steam_id`) to
    `LEAKED_STEAM64`, because the events route serializes `parser_events.payload` (NOT
    `raw_snapshot`). Without this, the events leak-guard passes vacuously. Then assert `expectNoSteam64`
    over both `response.json()` and `response.payload` for the detail route AND the events route
    (walking every page if the cursor returns more). Clean up the
    seeded replay/parser_results/parser_events rows in afterAll (mirror the existing delete pattern).
    Then regenerate the OpenAPI contract: run `pnpm run openapi:export` and commit the updated
    `openapi/server-2.openapi.json` (the three new JSON routes appear; sitemap XML routes do not —
    none exist yet).
  </action>
  <verify>
    <automated>pnpm run test:integration -- steamid-leak-guard && pnpm run openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - `PUBLIC_DETAIL_ROUTES` includes the replay detail + events routes: `grep -c "/stats/replays" src/test/integration/steamid-leak-guard.test.ts` ≥ 2.
    - The real-pg sweep seeds a `current` parser_result with a planted full Steam64 in `raw_snapshot.players[].sid` (detail vector) AND seeds `parser_events.payload` with the planted Steam64 at `payload.player.steam_id` + `payload.attacker.steam_id` + a nested array path (events vector — B-1), and asserts zero `7656119\d{10}` over the replay detail AND events bodies/payloads.
    - `pnpm run openapi:check` passes (no stale schema) and `openapi/server-2.openapi.json` contains the `/stats/replays` paths.
    - Seeded replay/parser_results/parser_events rows are cleaned up in afterAll.
  </acceptance_criteria>
  <done>Leak guard covers the new replay routes (empty + real-pg seeded), OpenAPI regenerated and committed clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route schema | `:id`, query filters, cursor, limit cross the HTTP boundary into TypeBox validation |
| read-model rows → JSON response | masked participant/event data serialized to `web` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering | `:id` path param | mitigate | `SlugOrUuidParameters` bounds the param (`^[A-Za-z0-9-]+$`, maxLength 128) — ReDoS/oversized-path DoS mitigation; repository `looksLikeUuid` branch avoids `::uuid` 500. |
| T-17-07 | Tampering | mixed `page`+`cursor` / forged cursor | mitigate | Replay routes registered inside the child scope → inherit `rejectLegacyPaginationParameters` (400) + `mapPublicStatsError` BadCursorError→400; error body is a fixed string, never echoes input. |
| T-17-08 | Information Disclosure | participant steam id (`raw_snapshot.players[].sid`, detail) AND event steam id (`parser_events.payload.*.steam_id`, events — the real leak vector since events serialize the jsonb payload) | mitigate | `mapReplayEvent.scrubPayload` deep-masks every steam id key/value in the payload before serialization; leak-guard extended: empty-DB sweep + real-pg seeded sweep planting `76561198012347890` in BOTH `raw_snapshot.players[].sid` and `parser_events.payload.{player,attacker,context.crew[]}.steam_id`; `expectNoSteam64` asserts zero `7656119\d{10}` in body, payload, and error path for detail + events. |
| T-17-09 | Information Disclosure | stale/incorrect OpenAPI contract leaking shapes | mitigate | `pnpm run openapi:check` gate enforces the committed schema matches the live routes. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (ROADMAP zero-new-deps; RESEARCH audit N/A). |
</threat_model>

<verification>
- `pnpm test` (filters + routes unit/inject tests green).
- `pnpm run test:integration` (leak-guard empty + real-pg replay sweep green).
- `pnpm run openapi:check` (contract regenerated, not stale).
- `pnpm run typecheck`.
</verification>

<success_criteria>
- Three replay JSON routes live, schema-validated, in the public-stats child scope.
- Mixed page+cursor and malformed cursor both 400 via inherited guards.
- Detail + events 404 for unknown id; slug-or-uuid resolved.
- Zero full Steam64 across replay route bodies/errors (empty + real-pg proven).
- OpenAPI contract regenerated and committed clean.
</success_criteria>

<output>
Create `.planning/phases/17-replay-surface/17-02-SUMMARY.md` when done.
</output>
